# Technical Rules & Bug Catalog

## Vite Build — CSS はビルド成果物である

**`public/build/assets/app-*.css` は `npm run build`（`vite build`）で生成されるビルド成果物**で、
git 管理外（`.gitignore`）。

**2026-07-15 に `deploy.sh` の先頭へ `npm run build` を組み込んだ**ので、
デプロイすれば CSS は必ず最新になる（ビルド失敗時は本番へ何も転送せず中断する）。

それ以前は `deploy.sh` が rsync するだけだったため、2026-04-23 のビルド以降 3 ヶ月弱
CSS が**凍結スナップショット**のままで、「Blade に足したクラスが無音で効かない」が
常態化していた。Bug #19 の inline style 回避も、RULES.md の「効かないクラス一覧」も、
すべてこの凍結の副作用だった。

**現状: アプリが使う Tailwind ユーティリティ 393 種は全てコンパイル済み＝無効クラスは 0。**

⚠ ローカルで開発サーバを使わず Blade を直接見ている場合、**クラスを足しただけでは効かない**
（ローカルの `public/build` も手で `npm run build` するまで古いまま）。デプロイ時は自動。

### 走査対象（docs/ は除外済み）

Tailwind v4 の**自動コンテンツ検出**は `.gitignore` 以外の**リポジトリ全体**を走査する。
これには `docs/*.md` も含まれるため、**2026-07-15 に `@source not` で docs/ を除外した**:

```css
/* resources/css/app.css */
@import "tailwindcss";
@source not "../../docs";   /* パスはスタイルシートからの相対 */
```

**なぜ必要だったか**——除外前は、この文書にクラス名を書いた事自体がビルドに取り込まれていた:

- 2026-04-17 に RULES.md へ「`gap-5` は壊れているから使うな」と書いた
  → 04-23 のビルドが RULES.md を走査 → `.gap-5{...}` が生成され **`gap-5` が実在してしまった**
- 2026-07-15 に「`w-7` `w-40` `sm:flex` は無い」と書いたら、同日の再ビルドで**全部実在した**
  （`w-40` はその「存在しない例」として挙げたものだった）

つまり **「クラス X は存在しない」と書くと次のビルドで自分自身が偽になる**状態だった。
過去の「効かないクラス一覧」が 12/12 誤りだったのはこれが原因で、
信じた人が「コンパイル済みのクラスをわざわざ inline style に書き換える」無駄をしていた。
除外により docs 由来の 42 クラスが CSS から消え、**ビルドはアプリの実使用だけを反映する**
ようになった（52,104 → 48,238 bytes）。

⚠ **走査対象は `resources/` だけではない。** `app/Enums/UnitStatus.php` は
フロアマップの色を `'bg-blue-50 border-blue-300'` のような**Tailwind クラス文字列で返す**ため
`app/` も走査が要る。`@source not` で除外先を増やす時は必ず実測してから
（`badgeStyle()` 系は inline style を返すので無関係）。

**それでも「効かないクラス一覧」は置かない。** Tailwind v4 の spacing scale は**動的**で
（`w-<任意の数>` は書けば生成される）、一覧は結局メンテできないため。使う前に測る。

### 新しいクラスを使いたい時

**普通に Tailwind クラスを書いてよい。`./deploy.sh` がビルドまでやる。**

- ローカルで見た目を確認したい時だけ、手で `npm run build`
- inline style での回避は**もう不要**（Bug #19 は凍結時代の話）
- 「今このクラスは効くか」を測りたい時は下記「Tailwind 監査」

### 旧バンドルの掃除（2026-07-15 に deploy.sh へ追加済み）

CSS/JS を変更するとハッシュ名が変わるため、旧 `app-*.css` が本番に孤児として残る
（`manifest.json` は新を指すので無害だが、デプロイの度に蓄積する）。
`deploy.sh` の `[4/6]` で `public/build/` だけを `--delete` 付きで再同期して掃除している:

```bash
rsync -avz --delete ./public/build/ ${SERVER}:${WEB_PATH}/build/
rsync -avz --delete ./public/build/ ${SERVER}:${APP_PATH}/public/build/
```

転送先が 2 つあるのは、**APP_PATH = Laravel が `public_path('build/manifest.json')` を読む側**、
**WEB_PATH = ブラウザが実ファイルを取りに行く側**の両方に配る必要があるため。

**⚠ `public/` 全体に `--delete` を付けるのは厳禁。**
`public/storage` は `storage/app/public` への symlink ＝ **本番のアップロード物を消しうる**。
`--delete` してよいのは Vite の出力しか入らない `public/build/` のみ。

⚠ 本番のファイルを消す変更を入れる時は、必ず先に `--dry-run` で消える物を確認すること:

```bash
rsync -avz --dry-run --delete ./public/build/ <server>:<web>/build/
```

## Tailwind 監査の落とし穴（クラス実在チェックの正しいやり方）

Bug #19 の「使う前にコンパイル済み CSS を grep して確かめる」は正しいが、**やり方を間違えると
逆方向に誤判定する**。2026-07-15 に両方を実測で踏んだので手順を確定させる。

### 正しいコマンド（main repo で実行すること）

**クラス名が英数字とハイフンだけなら**（`w-8` `gap-3` `items-center` 等）:

```bash
grep -oE "\.<class>[,{:>~+ ]" public/build/assets/app-*.css
```

**クラス名に `:` `.` `[` `]` を含むなら**（`hover:bg-red-50` `gap-1.5` `min-w-[140px]` 等）、
CSS 側はそれらをバックスラッシュでエスケープしているので `grep -oF` で**エスケープ後の literal**を探す:

```bash
grep -oF '.hover\:text-emerald-600' public/build/assets/app-*.css
grep -oF '.gap-1\.5'                public/build/assets/app-*.css
grep -oF '.min-w-\[140px\]'         public/build/assets/app-*.css
```

**`-o` は必須。** ビルド済み CSS はミニファイされていて**ファイル全体がほぼ1行**なので、
`-o` を省くと「マッチした行」＝ CSS 全体（約50KB）が流れてきて何も読み取れない。

**迷ったら実際のセレクタを目視するのが確実**（前方一致の誤判定も一目で分かる）:

```bash
grep -oE '\.w-8[^{]*\{[^}]*\}' public/build/assets/app-*.css
#=> .w-8{width:calc(var(--spacing) * 8)}
```

### 落とし穴 1: worktree では全クラスが「無い」ように見える

**`public/build` は `.gitignore` 済み（`.gitignore:16`）＝ worktree にビルド済み CSS が存在しない。**
worktree で grep すると全部 MISSING に見え、「コンパイル済みのクラスをわざわざ inline style に
書き換える」という無駄な作業に走る。**監査は必ず main repo（`/Users/masanori/site/manage`）で行う。**

### 落とし穴 2: アンカー無し = false positive（存在しないクラスを「ある」と誤判定）

Tailwind のクラス名は前方一致するものが多い。実測:

```
$ grep -oE "\.w-4" app-Df177yiQ.css     # ← アンカー無し
.w-4
.w-4          ← 実体は .w-48{...} の前半分にマッチしている
```

`.w-4{...}` と `.w-48{...}` の両方にヒットする。つまり **`w-40` のような存在しないクラスでも
`.w-4x` 系が1つでもあれば「ある」と誤判定**する。

### 落とし穴 3: `\{` アンカー = false negative（あるクラスを「無い」と誤判定）

false positive を潰そうと `\{` を付けると、今度は**セレクタリストに束ねられたルールを取りこぼす**。
Tailwind v4 は旧名と新名を1ルールにまとめるため、実体はこうなっている:

```
.flex-shrink-0,.shrink-0{flex-shrink:0}
```

`.flex-shrink-0` の直後は `{` ではなく `,` なので、`grep -oE "\.flex-shrink-0\{"` は**空を返す**。
コンパイル済みなのに「無い」と判断してしまう。

### 落とし穴 4: 小数を含むクラス名 = false negative

**クラス名の中の `.` を、CSS は `\.` にエスケープする。** `gap-1.5` の実体はこう:

```
.gap-1\.5{gap:calc(var(--spacing) * 1.5)}
```

ERE の `\.` は「リテラルのドット」であって「バックスラッシュ＋ドット」ではないので、
`grep -oE "\.gap-1\.5[,{: ]"` は**空を返す**。`gap-1.5` は実際にはコンパイル済みで
`attachment-section.blade.php` が現に使っている。
→ **小数入りは `grep -oF '.gap-1\.5'`**（エスケープ後の literal を探す）。

同様に該当し、いずれも実測でコンパイル済み: `gap-2.5` `py-1.5` `py-2.5` `w-3.5` `h-3.5`
`mb-3.5` `space-y-0.5` `space-y-1.5` `space-y-3.5`。

### 落とし穴 5: コンビネータ付きユーティリティ = false negative

`space-y-*` / `divide-*` は**複合セレクタ**になるため、クラス名の直後が `>` になる:

```
.space-y-1\.5>:not(:last-child){--tw-space-y-reverse:0; ...}
```

アンカー `[,{: ]` に `>` が無いので**空を返す**（`space-y-1.5` は小数と `>` の**二重**に該当）。
→ **アンカーは `[,{:>~+ ]`**（コンビネータ 3 種を含める）。

### 結論

| クラス名の形 | 使うコマンド |
|---|---|
| 英数字とハイフンのみ（`w-8` `gap-3`） | `grep -oE "\.<class>[,{:>~+ ]"` |
| `:` `.` `[` `]` を含む（`hover:bg-red-50` `gap-1.5` `min-w-[140px]`） | `grep -oF` でエスケープ後の literal |

妥当性確認済み（2026-07-15 実測）:

| 期待 | クラス | 確認したこと |
|---|---|---|
| 検出 | `w-4` / `w-48` | 前方一致の両方を正しく区別 |
| 検出 | `flex-shrink-0` | セレクタリスト `.flex-shrink-0,.shrink-0{...}` |
| 検出 | `gap-1.5` | 小数（`.gap-1\.5`） |
| 検出 | `space-y-1.5` | 小数＋コンビネータ（`>` が直後に来る） |
| 検出 | `hover:bg-red-50` | バリアント（`.hover\:bg-red-50`） |
| 検出 | `min-w-[140px]` | 任意値（`.min-w-\[140px\]`） |
| **非検出** | `w-40` `w-7` `hover:bg-red-100` | 実在しないクラスを誤検出しない |

**2026-07-15 に docs/ を `@source not` で除外したので、この表に非検出の例を書ける。**
除外前は、書いた時点で次のビルドがその文字列を拾って実在させてしまうため不可能だった
（冒頭「走査対象」参照）。⚠ ただし Tailwind v4 の spacing scale は動的なので、
`w-<数字>` 系は `resources/` や `app/` で使えば何でも生成される。上の非検出は
「**現在どこからも使われていない**」という意味であって、使えば実在するようになる。

## Past Bug Catalog

| # | Symptom | Root Cause | Fix |
|---|---------|-----------|-----|
| 1 | JS text displayed as HTML in x-data | `=>` arrow function's `>` parsed as HTML close tag | Extract to `<script>` named function |
| 2 | Styles disappear on Alpine toggle | `style=` + `:style=` conflict | Merge all into single `:style` |
| 3 | Form data lost on submit | Duplicate `name` attrs with `x-show` (hidden inputs still submit) | Use hidden + Alpine var or `:disabled` |
| 4 | Redirect fragment not working | Fragment passed as route param | `redirect(route(...) . '#fragment')` |
| 5 | Checkbox state lost after validation | `old()` returns string array | `.map(Number)` |
| 6 | Blade compile error | `@else` immediately followed by `<` or alphanumeric | Multi-line `@if/@else/@endif` |
| 7 | `@json()` error | PHP function inside `@json()` | Pre-format in Controller |
| 8 | SQL error on User query | `User::whereNull('deleted_at')` but no `deleted_at` column | Remove `whereNull` |
| 9 | Wrong column name error | `re_projects.name` doesn't exist | Use `project_name` |
| 10 | Controller argument not injected | `defaults()` doesn't work in Laravel 12 without URL param | Use `resolveDepartment()` |
| 11 | Kanji in furigana auto-input | `compositionupdate` event.data becomes kanji | Use `input` event + `compositionend` for katakana |
| 12 | Soft-deleted buyer not in dropdown | Buyer uses SoftDeletes | `->withTrashed()` on relation + include current buyer in edit |
| 13 | Building coverage shows `80.00%` | Model cast `decimal:2` | Change to `integer` |
| 14 | Cost item font too large after toggle | `style` + `:style` conflict on `<td>` | Merge into `:style` |
| 15 | Lot section cost not synced with Alpine | PHP rendered static value | Use Alpine `effectiveTotal` |
| 16 | `<option x-show>` not hiding + duplicate values in select | Browsers ignore `display:none` on `<option>` (spec-allowed inconsistency); also `<template x-for>` renders options AFTER `x-model` syncs | Use static `<option>` tags (not `x-for`) + filter data before rendering |
| 17 | Google Maps shows "For development purposes only" watermark + auth error dialog; `key=` empty in API URL | Blade called `env('GOOGLE_MAPS_API_KEY', '')` directly. After `php artisan config:cache` (run by deploy.sh on every deploy), Laravel disables direct `.env` reads outside config files — `env()` returns empty string in Blade | Register the value in `config/services.php` (`'google_maps' => ['api_key' => env('GOOGLE_MAPS_API_KEY')]`) and read it via `config('services.google_maps.api_key')` in Blade. Never call `env()` directly outside config files. |
| 18 | `<input type="file" class="form-input">` renders as a sharp-cornered, unstyled-looking box; the native "Choose File" button decoration disappears | The compiled `.form-input` rule contains `appearance: none; border-radius: 0`. On `<input type="file">` this strips the browser's native file-picker chrome and removes rounded corners, breaking visual consistency with surrounding rounded buttons | Do NOT apply `.form-input` to `<input type="file">`. Use an inline style instead, e.g. `style="display:block; width:100%; max-width:520px; padding:8px 12px; font-size:13px; color:#374151; background:white; border:1px solid #d1d5db; border-radius:6px; cursor:pointer; box-sizing:border-box;"`. Pages that override `.form-input` locally inside a `<style>` block (e.g. `zeal/members/_form.blade.php`) work, but only on that page — file inputs on other pages still get the bare `.form-input` definition |
| 19 | **【2026-07-15 解決済み】**Blade に足した Tailwind クラスが無音で効かない | `public/build/assets/app-*.css` はビルド成果物なのに、当時の `./deploy.sh` は rsync するだけでビルドしなかった。誰も手で `npm run build` しなかったため 2026-04-23〜07-15 の 3 ヶ月弱 CSS が凍結し、この症状が常態化していた | **`deploy.sh` の先頭に `npm run build` を組み込んで根治済み**（ビルド失敗時は本番へ何も転送せず中断）。**今は Tailwind クラスを普通に書いてよく、inline style での回避は不要**。ローカルで見た目を確認する時だけ手で `npm run build`。⚠ **かつてここに列挙していた「効かないクラス一覧」は 12/12 が誤りだった**——一覧に書いた事自体が Tailwind の走査対象（`docs/*.md` も含む）になりビルドへ含めてしまうため。**一覧は原理的に維持できないので置かない**（詳細は冒頭「Vite Build」）。現状を測りたい時は「Tailwind 監査の落とし穴」の手順で（⚠ 誤判定が4通りある。`public/build` は gitignore 済みで worktree には無いので main repo で） |
| 20 | ファイルアップロード時に「The route attachments/{type}/{id} could not be found.」で失敗する。例: `attachments/projects/11`, `attachments/ms_tenants/3`, `attachments/dad_projects/5` | `AttachmentController::TYPE_MAP` に新しい type を追加した際、`routes/web.php` の `Route::post('/attachments/{type}/{id}', ...)->where('type', '...')` の正規表現を更新し忘れた。`where` 正規表現に存在しない type は Laravel ルーターで弾かれ 404 になる（`procurements` 等の既存 type は通る） | `routes/web.php` の `where('type', '...')` 正規表現を `AttachmentController::TYPE_MAP` のキー全部と同期させる。新しい type を `TYPE_MAP` に追加するときは必ず `where` 正規表現にも同じ文字列を `\|` 区切りで追加すること。本番反映は `git push` だけでは不十分で `./deploy.sh` を実行する必要がある（rsync + `route:cache` 再生成） |
| 21 | `<x-form-actions :cancel-url="route(&quot;{$var}.xxx&quot;)" />` のように Blade コンポーネント属性式内に `&quot;` を含めたページが本番でだけ 500 `syntax error, unexpected token "&"` で落ちる。ローカルでは再現しない（例: `/housing/customers/create` が本番 500、`/realestate/customers/create` は同じ `_form.blade.php` でもアクセスタイミング次第で動いて見えてしまう） | Blade の Anonymous Component（`@props` のみで定義されたビュー型コンポーネント）の属性式に `&quot;` HTML エンティティを書くと、本番（PHP 8.3 + `view:cache` で全 Blade を precompile）経由ではデコードが漏れ、生のコンパイル済み PHP に literal `&quot;` が残って PHP 構文エラーになる。ローカル（lazy compile + PHP 8.5）では発火しないため気づけない | Blade コンポーネント属性で動的な route name を渡すときは `&quot;` を避け、PHP 文字列連結で組み立てる: `:cancel-url="route($department.'.customers.index')"`。route name が完全に静的ならシングルクォートで素直に書ける: `:cancel-url="route('tenant.customers.index')"`。残存検査は `grep -rn "&quot;" resources/views/` で一覧化。本番反映は `./deploy.sh`（`view:cache` 再生成）必須 |
| 22 | 一覧/詳細画面が本番でだけ 500 `TypeError: App\Enums\XxxStatus::tryFrom(): Argument #1 ($value) must be of type string\|int, App\Enums\XxxStatus given`。該当データ（例: 注文住宅の契約済レコード）が 1 件でも存在するときだけ発火し、空データのローカルでは該当行に到達せず素通りして気づけない（ローカル CLI も実 PHP 8.3 系なのでバージョン差ではなく **データ差**） | Eloquent の `casts()` で `'status' => XxxStatus::class` と enum キャストした属性は、読み出し時点で既に enum インスタンス。`tryFrom()` は `string\|int` のみ受け付けるため enum を渡すと TypeError。`Housing\HsContractListController::mapCustomOrderToDto` の `CustomOrderStatus::tryFrom($c->status)` が原因（2026-06-08 本番 500、commit c15812a1 で修正） | キャスト済み属性はそのまま使う: `$statusEnum = $model->status;`（null 可能性があれば `$model->status ? $model->status->label() : '—'` でガード）。生文字列からの変換が必要な場面だけ `tryFrom()` を使う。`whereIn('status', [Status::Foo->value, ...])` のようにクエリで `->value` を使うのは正しい。横展開検査: `grep -rn "::tryFrom(\$" app/` でキャスト属性への誤用を洗い出す |
| 23 | `x-data="someFunc({ data: @json($x) })"` のように `@json` を**二重引用符の `x-data="..."` 属性内**に入れると、Alpine が `... is not defined` を多発しコンポーネントが全く初期化されない（ボタン非表示・x-show/x-model/x-text 全滅。ただし x-cloak は外れるのでカード枠だけ表示され気づきにくい）。本番・ローカル両方で発火。例: 仕入れ案件/分譲地PJ 新規登録の原価管理（Excel取込・費用追加ボタンが出ない）、`tenant/repairs` の物件→区画絞り込み。原価管理は追加(7f0c9536)以来 create で一度も表示できていなかった | `@json`（= `json_encode($x, 15, 512)`）の `JSON_HEX_QUOT` は**文字列値の中の `"` のみ**エスケープし、**JSON の構造区切りの `"` は生のまま**出力する（`json_encode(['a'=>1],15)` → `{"a":1}`）。二重引用符の HTML 属性に入れると最初の構造 `"` で属性が途切れ、Alpine が受け取る式が `someFunc({ data: [{` のように壊れて評価不能 | ① データを `<script>` 内の named function で組み立て、属性は `x-data="someFunc()"` だけにする（詳細画面パターン。`_cost_section_form` を 511aa23b で修正）。または ② 属性に直接入れるなら `@json` でなく `{{ \Illuminate\Support\Js::from($x) }}`（`JSON.parse('...')` 形式で構造クォートも `"` 化＝属性安全。repairs を fffd9d47 で修正）。横展開検査: `grep -rn -A3 'x-data="' resources/views/ \| grep '@json'`。本番反映は `./deploy.sh`（view:cache 再生成）|
| 24 | 一覧画面のページネーションが番号付き（`< 1 2 3 … >`）でなく左右2ボタンだけになる（ZEALでは翻訳キー `pagination.previous` `pagination.next` が生表示）。本番・ローカル両方。`$paginator->hasPages()` が true（2ページ以上）の時だけ顕在化するので空データのローカルでは気づきにくい | デフォルトの `{{ $x->links() }}`（=`pagination::tailwind`）は番号付きを `hidden sm:flex` の desktop ブロックで描画するが、`sm:flex` `sm:hidden` 等のレスポンシブクラスは**プロジェクト Blade が使っておらず Tailwind JIT 未コンパイル**（フレームワーク vendor ビューはコンテンツスキャン対象外）。よって desktop ブロックが常時 hidden→`sm:hidden` のモバイル2ボタンだけ残る。ラベルも locale=ja で `lang/ja/pagination.php` 不在なら生キー表示（別件で lang ファイル追加 b26208e9） | `->links()` を使わず `resources/views/zeal/inquiries/index.blade.php` の**インライン番号付きマークアップ**を流用（`@foreach($x->getUrlRange(1, $x->lastPage()) as $page => $url)` ＋現在ページ `bg-emerald-600` ＋ `&lt;`/`&gt;`、コンテナ `flex justify-center gap-0.5`、ボタン `w-8 h-8 flex items-center justify-center rounded text-xs`）。2026-06-15 に旧 `->links()` 全10画面を統一（081ab5a7 + c8891f2c）しアプリ内 `->links()`=0。フィルタ保持はコントローラで `paginate()->withQueryString()` か Blade で `@php($x->withQueryString())` を番号付き出力の前に1回。横展開検査: `grep -rn "\->links()" resources/views/`。本番反映は `./deploy.sh`（view:cache 再生成）。⚠ **2026-07-15 追記**: 根本原因だった「`sm:flex` / `sm:hidden` 未コンパイル」は CSS 凍結（Bug #19）の副作用で、再ビルド後は repo 内にその文字列が在るかで決まる＝**もはや恒久的な事実ではない**。ただしアプリ内 `->links()` は 0 件でインライン番号付きが定着済みなので、**`->links()` に戻す理由は無い**（意匠も統一されている）。この行は「壊れるから避けろ」ではなく **規約**として読むこと |
| 25 | 不動産(realestate)の顧客詳細・新規登録後が本番でだけ 500 `Symfony\Component\Routing\Exception\RouteNotFoundException: Route [realestate.customers.surveys.create] not defined.`。一覧は表示できるが「詳細」ボタンと「登録」ボタンで落ちる。買主データが無い空ローカルでは詳細(show)を一度も開かず素通りするため気づけない（2026-06-17 本番 500、commit 5b119f1d で修正） | `resources/views/buyers/show.blade.php` は housing/realestate **共通ビュー**で、「アンケートを追加」ボタンを `@forelse` の外＝**無条件**に `route("{$department}.customers.surveys.create", $buyer)` で描画する。だが `routes/web.php` のアンケート(`customers.surveys.*` 5ルート)は **housing にしか定義されておらず realestate に欠落**していた → ルート未定義で 500。surveys が空でも無条件ボタンは必ず実行されるので新規買主で即発火。store 成功後は show へリダイレクトするため「登録ボタンで 500」「詳細ボタンで 500」は同一画面。一覧(index)は surveys リンクが無く無事。`CustomerSurveyController` と登録画面(`buyers/_form.blade.php`)は両部署対応済み（housing 固有処理は `if($department==='housing')` でガード）で、**ルート定義だけが漏れていた**不整合 | realestate に surveys ルート5本(create/store/edit/update/destroy)を housing と対称・同一 middleware(create/store/edit/update=`role:executive,manager` / destroy=`role:executive`)で追加。横展開検査: 部署共通 Blade が `route("{$department}.xxx")` を無条件で呼ぶ箇所は、全部署にそのルートが定義されているか確認する `grep -rn 'route("{$department}' resources/views/`。空データのローカルで再現しない本番 500 は `php artisan view:cache` を有効化しローカルで本番同等にレンダリングして特定できる（Bug #22 と同じ「データが有る時だけ発火」型）。本番反映は `./deploy.sh`（route:cache 再生成）必須（git push だけでは不十分。Bug #20 と同じ）|
| 26 | テナント契約編集が本番でだけ全件 500 `ParseError: Unclosed '[' on line N does not match ')'`（compiled view 由来 `storage/framework/views/<hash>.php`）。一覧/詳細/新規作成は無事で **edit だけ**落ちる。空データのローカルは edit を一度もレンダリングしないため素通り。`view:cache` は「Blade templates cached successfully」と**成功表示するのに**発火する（2026-06-18、投資回収紐付けデプロイ直後、commit 56430ecf で修正） | `@json($x ? [` 改行 `'k' => $x->y->method(),` … `] : null)` のように **`@json()` に多行の配列リテラル（特に `->method()` 呼び出しを含む）** を渡すと、Blade の引数パーサ（括弧カウント）が誤って `json_encode(...)` の引数を途中で打ち切り、`[` 未閉じの壊れた PHP を生成する（例: `json_encode($x ? [ 'id'=>.., 'pl'=>$x->pattern->label()) ?>` と `total_amount`/`] : null` が欠落）。`view:cache` はコンパイル済み PHP を **lint しない**ため成功扱いになり、実レンダリングで `require` された瞬間に ParseError → 500。`php -l <.blade>` も Blade を緩く見るだけで検出不可。Bug #7（@json 内に関数）・Bug #23（x-data 属性内 @json）の同系統 | 配列は**コントローラで組み立て**、Blade には単一変数だけ渡す: `$currentInvestment = $contract->investment ? [...] : null;` → Blade `@json($currentInvestment)`（内部に括弧/角括弧が無く誤パース不能）。横展開検査: `grep -rn '@json(' resources/views/ \| grep '\['`（`?? []` や `old(...,[])` の単一行デフォルトは安全。**多行の配列リテラル＋メソッド呼び出しが危険**）。**検証は `view:cache` 成功では不十分** → コンパイル済みビューを必ず `php -l` する: `php artisan view:cache && for f in storage/framework/views/*.php; do php -l "$f" >/dev/null \|\| echo "INVALID: $f"; done && php artisan view:clear`。あるいは実データでレンダリングして確認（空ローカルは不可＝Bug #22/#25 と同型）。本番反映は `./deploy.sh`（view:cache 再生成）|
| 27 | 契約管理一覧 `/housing/contracts` が「**注文住宅の契約が1件以上あり、かつ建売契約が0件**」になる年度・絞り込みでだけ 500 `Error: Call to a member function getKey() on array`。建売が1件でもあれば通るので**データ依存**（種別・担当者・年度フィルタの組み合わせでも発火しうる）。空ローカルは両方0件で素通りし、本番も両種別が揃っていると気づけない（2026-07-25、契約一覧の3ゾーン様式刷新中に発見、commit 0d4761e7 で同時修正） | `$query->get()->map(fn ($m) => [...配列DTO...])` は、返り値が Model でなければ結果を base `Support\Collection` にダウングレードするが、**空コレクションの場合はその判定（要素の走査）が空振りするため `Eloquent\Collection` のまま残る**。`EloquentCollection::merge()` は**引数側の全要素に `getKey()` を呼ぶ**実装なので、「空の Eloquent コレクション」に「配列 DTO のコレクション」を merge すると配列に対して `getKey()` が呼ばれて致命エラー → 500。建売が1件でもあれば左辺は base 化済みなので `Support\Collection::merge()`（キー結合のみ）が使われ、症状が出ない。Bug #22/#25/#26 と同じ「**実データが特定の形の時だけ発火**」型 | merge の**左辺を明示的に base 化**する: `$tateuriItems->toBase()->merge($customItems)`。非空時は既に base なので `toBase()` は no-op ＝挙動不変。回帰テストで固定: `test_custom_order_only_period_does_not_500`（`tests/Feature/Housing/HsContractListColumnsTest.php`。建売を作らず注文住宅だけ作って一覧が 200 を返すことを検証）。横展開検査: `grep -rn '\->merge(' app/`（2026-07-25 実測でアプリ内は当該1箇所のみ）。**予防**: 複数ソースのリストを統合する時は `$a->toBase()->merge($b)` と書くか、最初から `collect()` に詰め替える。⚠ 「`->get()->map()` は必ず base になる」という思い込みが原因なので、**空ケースを1件でもテストに入れる**のが最短の防御。本番反映は `./deploy.sh`|
| 28 | `@push('scripts')` に入れたスクリプトが**出力されず、例外も警告も出ない**。症状は「ボタンを押しても無反応」。注文住宅一覧 `/housing/custom-orders` の進捗バッジは `onclick="openStepBar(this)"` を持つのに `window.openStepBar` が `undefined` で、クリックしてもステップバーが開かない（一覧からのステータス変更 PATCH が起動不能）。建売契約編集 `/housing/properties/{property}/contract/edit` は `x-data="contractEditForm()"` の関数定義が失われ Alpine がコンポーネントを初期化できず顧客名の Ajax 検索が死ぬ（Bug #23 と同じ症状クラス）。**初期コミット 2046289d 以来ずっと動いていなかった**（2026-07-26 に本番のブラウザ検証で発覚、commit 683a09d6 で修正） | `resources/views/layouts/app.blade.php` に **`@stack('scripts')` が一度も存在しなかった**（`git log -S"@stack" -- resources/views/layouts/app.blade.php` が空）。Blade の `@push` は対応する `@stack` が無いと**スタックに積んだまま誰も展開せず、静かに捨てられる**（Laravel は警告を出さない）。コンパイルも `view:cache` も成功し、`php -l` も通り、テストも「`onclick` 属性がある」ことだけ見ていれば緑になる。**呼び出し側（属性）と定義側（`<script>`）が別の場所にあり、片方だけ消えても HTML として妥当**なのが気づけない理由 | `</body>` 直前に `@stack('scripts')` を追加（`@push` が無いページでは何も出力しないので約 200 ルートへの影響ゼロ）。位置はレイアウト内の既存 `<script>` より後・`@yield('content')` より後にする（`@vite` は module = defer なので Alpine 起動前に関数定義が間に合い、スクリプトが参照する DOM 要素も既に存在する）。回帰テストで固定: `tests/Feature/LayoutScriptStackTest.php` — **呼び出し側と定義側を必ず対で検証する**（`onclick="openStepBar(this)"` と `function openStepBar(badge)` の両方。属性だけ見ると「定義が無いのに通る」）＋「push が無いページで余計な出力が増えない」ことも固定。横展開検査: `grep -rln "@push(" resources/views/` と `grep -n "@stack" resources/views/layouts/app.blade.php` を**セットで**見る。⚠ **`styles` スタックは 2026-08-04 に追加して解消済み**（`@vite` の後・`</head>` の前）。`@push('styles')` は正常に `<head>` へ出る。それまでは同じ罠が残っており、`tenant/units/revise.blade.php` に「push すると消えるので `<style>` を直接置く」という回避コメントが実在した（同ビューを push へ移行済み）。回帰テストは `tests/Feature/LayoutStyleStackTest.php`。⚠ **位置まで固定すること** — body 側に置いても CSS は効いてしまうため「動くが意図と違う」状態を検出できない。`@vite` より前だとバンドルに負ける。⚠ **順序の検証は HTML では不可能**（`tests/TestCase.php` が `withoutVite()` を呼ぶのでテストの `@vite` は何も出力しない）→ Blade ソースの構造テストで測る。⚠ **その構造テストは Blade コメントを落としてから測ること** — レイアウトの注意書きに `@@stack('styles')` と書いてあるため、除去しないと**実体を消しても緑のまま通る**（2026-08-04 に新旧ロジックを差し替えて実測: 除去あり＝赤 / 除去なし＝緑。Bug #42 ② と同型）。⚠ **検証は「HTML に出るか」だけでは不十分** — この JS は一度も実行されたことがなかったので、ブラウザで実際にクリックして動くことまで確認する。本番反映は `./deploy.sh`（`view:cache` 再生成）|
| 29 | Chart.js の `<canvas>` を CSS Grid のトラック（`grid-template-columns: 1fr`）に置くと、**カードがコンテンツ幅を超えて横に膨らみ `<main>` に横スクロールが出て、グラフの右端（最新月）が見切れる**。超過幅はウィンドウ幅によらず**常に一定**（ZEAL では +220px。1800px 幅でも 1200px 幅でも同じ）なので「狭い画面だけの問題」に見えず、逆に**広い画面でも直らない**。`view:cache` も `php -l` もテストも全部通り、**単体 HTML のモックでは再現しない**（2026-07-27、ZEAL ダッシュボードのグラフ縦積み化＋過去1年化の直後に本番ブラウザ検証で発覚、commit ecc95c3d で修正）| **原因は 2 つあり、両方揃わないと直らない**。① `grid-template-columns: 1fr` は `minmax(auto, 1fr)` の略で、**最小値 `auto` が中身の min-content 幅で下限を作る**。`layouts/app.blade.php` の `<main class="flex-1 overflow-y-auto">` は flex 子で `min-width: auto` のため、この膨張を止める側もいない → トラックがコンテンツ幅を超えて広がる。② Chart.js（`responsive: true` / `maintainAspectRatio: false`）は **`<canvas>` に inline の `style.width` を px で書き込む**。CSS 側に `max-width` が無いと**一度広がった canvas が二度と縮まず**、その値が min-content 幅の下限になり続ける（`chart.resize()` を呼んでも縮まない）。①②が噛み合って「コンテンツ幅 + 定数」で安定する。⚠ **横並び（`1fr 1fr`）の時代は 1 トラックが半分幅だったので閾値が高く、顕在化していなかっただけ**で、罠自体は前からあった | **2 点セットで直す。片方だけでは直らない**（`minmax` だけだとカードは正しく 916px に縮むが canvas が 1094px のまま残ってカードから溢れる）: <br>`.zd-chart-stack { grid-template-columns: minmax(0, 1fr); }`<br>`.zd-chart-card canvas { max-height: 220px; max-width: 100%; }`<br>**検証はスクリーンショットでなく DOM 実測で**——ブラウザで `main.scrollWidth === main.clientWidth` を確認する（`document.querySelector('main')`）。広い幅と狭い幅の**両方**で測ること（定数超過なので片方だけでは判定できない）。横展開検査: `grep -rln "<canvas" resources/views/`（2026-07-27 実測 8 本）と `grep -rln "grid-template-columns:[^;]*1fr" resources/views/`（同 59 本）の**積集合**を見る。実測では canvas が bare `1fr` トラックの直下にあるのは `zeal/dashboard.blade.php` のみで、`dashboard/_executive_housing.blade.php` は canvas が `.chart-wrap` 側にあり該当しない。⚠ **モックでの確認は無効** — 本番は flex な `<main>` の中にあるのに単体 HTML モックにはその階層が無く、ブローアウトが起きない。**グリッド幅が絡む変更は本番同等のレイアウト階層で測る**。本番反映は `./deploy.sh` |
| 30 | `view:cache` が `ParseError: syntax error, unexpected token ","` で落ちる。原因の行は **JavaScript の `//` コメント**で、そこには PHP も Blade 式も書いていない。コメントを消すと直るので「コメントが悪い」とは思い至らず、周囲の `@json($var)` を疑って延々と時間を溶かす（2026-07-28、仕入れ案件一覧への分譲地統合で発生。**Bug #23 の注意書きをコメントとして書いた事が発火源**という自己参照的な事故） | **Blade のディレクティブコンパイラは JS のコメントを解釈しない。** `//` の中だろうと `@json` という文字列を見つけたらディレクティブとして展開する。直後が `(` でない（日本語の「は」等が続く）と**引数が空のまま** `<?php echo json_encode(, 15, 512) ?>` にコンパイルされ、PHP 構文エラーになる。実測: <br>`// ⚠ @json は <script> の中だけ` → `// ⚠ <?php echo json_encode(, 15, 512) ?> は <script> の中だけ`<br>`@json` に限らず `@if` `@foreach` など**全ディレクティブ名が対象**。Bug #7 / #23 / #26 と同じ「@json 一族」だが、**これまでの 3 件は式に問題があったのに対し、本件は式が一切無いコメント行で起きる**のが違い | コメント中でディレクティブ名を書くときは **`@@json` とエスケープする**（Blade は `@@` を リテラルの `@` として出力するので、表示は `@json` のまま）。Blade コメント `{{-- --}}` に逃がす手もあるが、`<script>` 内では JS コメントのほうが自然。**検出は `php artisan view:cache` の成功表示では不可**（compiled PHP を lint しないため）。必ず生成物を lint する:<br>`php artisan view:cache && for f in storage/framework/views/*.php; do php -l "$f" >/dev/null || echo "INVALID: $f"; done && php artisan view:clear`<br>横展開検査: `grep -rnE '^[[:space:]]*//.*@(json|if|foreach|php|include|section|yield|stack|push)' resources/views/ \| grep -v '@@'`。⚠ **`@php ... @endphp` の中は raw block なので Blade が statement-compile せず安全**——2026-07-28 実測でヒットした 8 件は全部 `@php` 内の PHP コメントで無害だった（279 ビューの lint も 0 件）。**危険なのは `<script>` など Blade がコンパイルする領域に置いた `//` コメント。** grep は目星をつけるだけに使い、判定は必ず compiled view の `php -l` で行う。<br>⚠ **2026-08-21 追記 — 同じ罠が `/* */` と「コンポーネントタグ」にもある。** 周辺ビル調査の地図タブで実際に踏んだ: JS のブロックコメントに 「単一対象しか持てない `<x-delete-confirm-modal>` は使えない」と**説明として**書いたら、Blade の**コンポーネントコンパイラ**がそれを展開し、対応の取れない `endif` を吐いて `view:cache` が壊れた（実測の compiled 出力: `<?php $component = Illuminate\View\AnonymousComponent::resolve(...)` と `<?php endif; ?>` がコメントの途中に生える）。#30 本体との違いは 2 つで、**① `//` だけでなく `/* */` も対象**（制御した再現で A=`//` / B=`/* */` の**両方**が展開された）**② ディレクティブ名だけでなく `<x-…>` も対象**。共通しているのは「Blade はコメントを解釈しない」こと。⚠ **検出は `view:cache` の成功表示では不可**（#30 と同じく `Blade templates cached successfully.` と出る）。compiled view を `php -l` して初めて分かる。⚠ **回避は Blade コメント `{{-- --}}` へ逃がすか、`<x-` と書かないこと**（同じ再現で C=`{{-- --}}` の中は展開されず落ちた。`show.blade.php` が同じ語を書けているのはこれが理由）。`@@json` のような `@@` エスケープはディレクティブ用で、**コンポーネントタグには効かない**。⚠ 横展開検査（2026-08-21 実測）: `resources/views` の **Blade 236 本 / `<script>` ブロック 114 個**を走査して `<script>` 内の `<x-` は **0 件**（`<x-` を使う Blade は 77 本あるが全て `<script>` の外）。判定はディレクティブ版と同じで、grep は目星をつけるだけに使い、**compiled view の `php -l` で確定させる**:<br>`python3 - <<'PY'`<br>`import glob, re`<br>`for p in glob.glob("resources/views/**/*.blade.php", recursive=True):`<br>`    for m in re.finditer(r"<script\b[^>]*>(.*?)</script", open(p).read(), re.S):`<br>`        for c in re.finditer(r"<x-[a-zA-Z0-9._-]+", m.group(1)): print(p, c.group(0))`<br>`PY`<br>本番反映は `./deploy.sh` |
| 31 | 一覧の「ステータス: 全て」（`?status=`）が **実 HTTP でだけ 0 件**になる。サービス層のテストは全部緑で、`Request::create('/x?status=')` で叩く限り再現しない（2026-07-28、仕入れ案件一覧への分譲地統合中に既存の回帰テストが拾って発覚） | `ConvertEmptyStringsToNull` は **HTTP ミドルウェア**なので、`Request::create()` で作ったリクエストには効かない。同じ `?status=` が<br>・実 HTTP → **`null`**<br>・`Request::create()` → **`''`**<br>と別の値で届く。`if (! is_string($status)) { return NO_MATCH; }` のような型ガードを`filled()` 判定より**前**に置くと、`''`（string）は通るのに `null` は「想定外の型」に落ちて 0 件になる。⚠ `$request->input('status', 'active')` は**キーが存在すれば既定値を返さない**ので、null は既定値 `'active'` にも救われない | 型ガードより先に **`if ($status === null) { return null; }`**（＝「全て」）を置く。**テストは `''` と `null` の両方を通すこと。** `Request::create()` に null は渡せないので`$request->query->set('status', null)` で明示注入する（実測でこの方法なら `input()` が null を返す）。⚠ **サービス層のテストだけでは原理的に検出できない**——ミドルウェアを通る HTTP レベルのテスト（`$this->get('/realestate/procurements?status=')`）を必ず 1 本残す。横展開検査: `grep -rn 'is_string(' app/Http/ app/Services/`。⚠ **同型の「ページ送りでフィルタが飛ぶ」欠陥は他の一覧に残っている。** `->paginate()->withQueryString()` は `appends()` → `Arr::query()`（= `http_build_query`）を通り、**null 値のキーを丸ごと捨てる**ため、`?status=`（全て）で 2 ページ目に行くと既定に戻る。2026-07-28 実測: `/realestate/projects?status=` は 1 ページ目 total=21 に対し 2 ページ目の URL に `status` キーが無く total=0 になる。**⚠ 2026-07-30 に全一覧を実測して決着（commit は下記）。「候補多数」は誤りで、実際に該当したのは `/realestate/projects` 1 本だけだった。** 判定条件は「**①「全て」オプションの値が空** かつ **②コントローラの既定が絞り込み**」の**両方**が揃うこと。実測: `/zeal/members` `/housing/properties` `/tenant/{contracts,inquiries}` `/dad/employees` 買主マスタは「全て」が `value="all"`（空でない）ので `http_build_query` に捨てられず**無事**。`/tenant/{units,investments}` `/zeal/inquiries` は空オプションだが既定も「全て」なので**キーが落ちても同じ結果**。`/realestate/procurements` は `ProcurementListService::paginationQuery()` で対策済み。**残っていた `/realestate/projects` を `->appends(array_map(fn ($v) => $v ?? '', $request->query()))` に修正**（`withQueryString()` をやめる）。⚠ **2026-07-28 に「total=21 → 2 ページ目 0 件」と記録したが、その後データが 16 件に減ってページ送り自体が出なくなり再現しなくなっていた**——件数依存で消えるので、コードを直してテストで固定するまで「直った」と判断しないこと。回帰テストで固定: `tests/Feature/RealEstate/ProjectListPaginationFilterTest.php`。⚠ **テストで `?status=&page=2` と URL を自分で組み立ててはいけない**——リンクが壊れていても status が付いた状態で届くので**必ず緑になる**。1 ページ目を描画させ **`$paginator->nextPageUrl()` を実際に辿る**こと（この誤りを実際に踏んだ。修正前は 3 本中 1 本しか変異を検出できなかった）。⚠ 「既定は従来どおり絞り込みのまま」も併せて固定する（正規化のついでに全件化していないことの確認）。**`withQueryString()` に戻す変異で 2 本が赤になることを確認済み**（2 ページ目が 25 → 20 件になり販売済 5 件が消える）。本番反映は `./deploy.sh` |
| 32 | Alpine の `x-show` で開くポップオーバーが**縦積みにならず横 1 列で開く**（実測 466px × 43px にバッジ 8〜9 個が並ぶ）。`:style` にはちゃんと `display: flex; flex-direction: column;` と書いてあり、Blade も `view:cache` も `php -l` もテストも全部通る。**壊れているのはブラウザだけ**（2026-07-28 本番ブラウザ検証で発見。仕入れ案件/分譲地/建売の 3 一覧で同一マークアップが稼働していた） | **`x-show` は `display` プロパティを自分のものとして扱う。** 同じ要素の `:style` に `display` を書いても Alpine に奪われる。実測した style 属性: <br>非表示 `... min-width: 130px; display: none; flex-direction: column; gap: 4px;` ← flex を none で**上書き**<br>表示   `... min-width: 130px; flex-direction: column; gap: 4px;` ← display を**丸ごと削除**<br>結果 computed display が `block` になり、残った `flex-direction: column` はflex コンテナが無いので無効。Bug #2（`style=` と `:style=` の競合）と同族だが、**相手が `x-show`** という別パターン | `display` / `flex-direction` / `gap` を**内側のラッパー div へ移す**:<br>`<div x-show="open" :style="'position: fixed; ... min-width: 130px;'">`<br>`  <div style="display: flex; flex-direction: column; gap: 4px;">`<br>`    <template x-for="...">...</template>`<br>`  </div>`<br>`</div>`<br>回帰テストで固定: `tests/Feature/AlpineXShowDisplayConflictTest.php`（`resources/views/` を走査し、`x-show` と `:style` を同一タグに持つ要素の `:style` に `display` が無いことを検証。**走査が空振りして緑になる事故を防ぐため「Blade を 100 本以上拾えていること」も併せて固定**）。横展開検査はテストが自動で行うので個別追加は不要。⚠ **検証は「HTML に出るか」では不可能** — 属性は正しく出ているのに Alpine が実行時に書き換える。ブラウザで開いて `getComputedStyle(el).display` を見るか、上記テストで見る。本番反映は `./deploy.sh` |
| 33 | 不動産・住宅事業の坪数表示が正しい値より 0.01 坪大きい。ユーザー報告: **132.69㎡ が 40.14坪 と出るが正は 40.13坪**（132.69 × 0.3025 = 40.138725 → 小数第3位以下切り捨て）。全画面で恒常的にズレるが「1 銭の差」なので長く気づかれなかった（2026-07-29 修正）| 5 モデル 6 メソッドが `round($sqm / 3.30579, 2)` と**四捨五入**で実装されていた（初期コミット 2046289d 以来）。仕様は切り捨て。⚠ **単に `round` を `floor` に替えるだけでは直らない**——`1 ÷ 3.30579 = 0.30249995…` で 0.3025 より僅かに小さく、四捨五入では吸収されていた差が切り捨てで表面化する（100㎡ → 30.24坪、正は 30.25坪 / 200㎡ → 60.49坪、正は 60.5坪）。⚠ さらに `floor($sqm * 0.3025 * 100) / 100` と float で書いても**二進誤差で下振れ**する（0.01㎡ 刻み 10 万件中 **41 件**が誤り: 28㎡ → 8.46坪、正は 8.47坪 / 44㎡ → 13.3坪、正は 13.31坪 / 60㎡ → 18.14坪、正は 18.15坪）| `App\Support\AreaConverter::sqmToTsubo()` に一本化し、**除算をやめて `× 0.3025`**、かつ **1/100㎡ 単位の整数演算**にする（㎡ カラムは全て `decimal(10,2)` なので厳密になる）: `intdiv((int) round($sqm * 100) * 3025, 10000) / 100`。bcmath の厳密解と 220,001 件照合して不一致 0 件を確認済み。回帰テストで固定: `tests/Unit/Support/AreaConverterTest.php`（**罠①②それぞれに戻したら落ちる値**を明示的に置いている。実際に 3 通りの誤実装へ変異させて赤になることを確認済み——値を消すと再発を検出できなくなる）＋ `app/Models/` に `3.30579` が残っていないことを走査で固定。⚠ **分譲地区画だけ `re_project_lots.area_tsubo` が DB 保存カラム**（`ProjectController` の store/update が書き込む）なので、コード修正に加えて既存行の一括更新が要る: `UPDATE re_project_lots SET area_tsubo = FLOOR(area_sqm * 0.3025 * 100) / 100`（MySQL の `DECIMAL` 演算は二進誤差が無いので SQL でも厳密）。⚠ **テナント管理の `units.area_tsubo` は坪を直接入力**しており㎡換算していないので無関係。本番反映は `./deploy.sh` |
| 34 | 坪単価の丸めが「必ず切り上げ」の仕様に反する。**ただし画面表示は一見正しく、本番 34 区画すべて仕様どおりに出ていた**ため、実データを掃引するまで欠陥が見えなかった（2026-07-29、Bug #33 に続けてユーザーが仕様を明示したことで発覚）| **欠陥は 2 つあり、どちらも「丸めが 2 回ある / float で丸めている」型**。⚠ **①二段階丸め**: `ProjectController` が保存時に `(int) round($price / $tsubo)` と**四捨五入**して円/坪を保存し、`ReProjectLot::getSellingPricePerTsuboFormatted()` がそこから `ceil($yen / 1000) / 10` と万円へ切り上げていた。真値が 1,000 円の倍数を **0.5 円未満だけ超える**とき前段の四捨五入が引き下げ、後段の切り上げが無効化される（19,990,000円 / 20.01坪 = 999,000.4998円/坪 → @99.9、正は @100.0）。実測 800 万通り中 **1,529 件**（0.019%）。⚠ **②float の `ceil`**: テナント賃料坪単価の `(int) ceil($rent / (float) $areaTsubo)` が、**割り切れる場合に 1 円上振れ**する（153,000円 ÷ 5.10坪 = ちょうど 30,000円 なのに 30,001円）。実測 877,851 通り中 **115 件**。⚠ さらに Bug #33 で `area_tsubo` を切り捨てに直した際、派生カラム `selling_price_per_tsubo` を再計算しなかったため本番 34 件中 **16 件**が stale 化していた（表示は偶然影響なし）| `App\Support\TsuboPrice` に一本化し、**丸めを 1 回だけにする＋除算を整数演算に置き換える**。坪カラムは `decimal(10,2)` / `decimal(8,2)` なので坪を 1/100 単位の整数にすれば厳密: <br>円/坪 `intdiv($amount * 100 + $h - 1, $h)`（$h = 坪×100）<br>万円/坪の1/10単位 `intdiv($price + $h * 10 - 1, $h * 10)`<br>**表示は保存済みの円/坪カラムを見ない**（見ると二段階丸めに戻る）。販売価格と坪数から直接 1 回で丸める。bcmath の厳密解と 53,109 通り照合し不一致 0 件。回帰テストで固定: `tests/Unit/Support/TsuboPriceTest.php`（**罠①②それぞれに戻したら落ちる値**＋「保存カラムにわざと嘘の値を入れても表示が変わらない」ことで経路も固定。実際に 2 通りの誤実装へ変異させ赤になることを確認済み）＋ `resources/views/` `app/` に坪単価の直書き `ceil` が残っていないことを走査で固定。⚠ **坪数は null がありうる**（テナント区画は坪を手入力するので未設定行がある）。ヘルパーの引数を非 null に狭めると区画詳細・契約詳細・フロアマップが 500 になる——実際に `tests/Feature/Tenant/UnitRentRevisionTest` が検出した。⚠ **`is_price_manual` は常に `true` が代入されるだけでどこからも読まれていない**（デッドフラグ。今回は未着手）。本番反映は `./deploy.sh` ＋ stale な中間カラムの再計算 |
| 35 | バリデーションエラーで差し戻されたとき、**フォームに戻らず生の JSON ページが表示され、入力が全部消える**。⚠ **「何も入力せず送信」では再現しない** — 画面上の Ajax（仕入れ先検索・仕入れ案件選択など）を**一度でも叩いた後**にだけ発火するため、素朴なエラー確認では素通りする。ローカルでもテストでも再現せず、`view:cache` も `php -l` も全部通る（2026-07-29、仕入れ案件一覧からの分譲地登録導線を実装中にコード品質レビューが指摘 → 実測で再現。commit 76a750b8 / f2e353f1 で修正）| Laravel の `StartSession::storeCurrentUrl()` は「**GET・ルートあり・非 Ajax・非 prefetch**」のリクエストごとにセッションの `_previous.url` を上書きする。`$request->ajax()` は **`X-Requested-With: XMLHttpRequest` ヘッダーの有無だけ**で決まる（`Accept: application/json` では効かない）。JSON API のルートは `routes/web.php` にあり `web` ミドルウェア（`StartSession`）配下なので該当する。よって**ヘッダーの無い `fetch` で JSON を GET すると、その URL が「直前 URL」として記録され**、その後 `$request->validate()` が失敗したときの `back()`（= `url()->previous()`）がそこへ飛ぶ。実測（2026-07-29）: <br>`/realestate/projects/create?from=procurements` → 仕入れ先検索 → POST 失敗 → back() が **`/api/realestate/suppliers/search?q=abc`** へ<br>`/realestate/contracts/create` → 仕入れ案件を選択 → POST 失敗 → back() が **`/api/realestate/procurement-cost/1`** へ<br>同じ操作でヘッダーを付けると正しくフォームへ戻る。⚠ **同一ファイル内で非対称だった** — `supplier-picker.blade.php` の簡易登録 POST には `X-Requested-With` が付いていたのに、すぐ上の検索 GET には無かった。契約画面の 6 箇所は**ヘッダーが一切無い** `fetch(url)` だった | JSON API を叩く `fetch` には必ず `headers: { 'Accept': 'application/json', 'X-Requested-With': 'XMLHttpRequest' }` を付ける。回帰テストで固定: `tests/Feature/RealEstate/AjaxFetchSessionGuardTest.php`（**`resources/views/` 全体を走査**し、`/api/realestate/` を叩く `fetch` すべてにヘッダーがあることを検証。走査が空振りして緑になる事故を防ぐため呼び出し箇所数の下限も併せて固定。Bug #32 のテストと同じ流儀）＋ `tests/Feature/RealEstate/SupplierSearchBackUrlTest.php`。⚠ **挙動テストだけでは原理的に検出できない** — テストは PHP からヘッダーを手で送るので、**Blade からヘッダーが消えても緑のまま通る**。呼び出し側（Blade）と挙動の**両方を対で**検証すること（Bug #28 と同じ構図）。⚠ **`Request::create()` でも検出できない**（ミドルウェアを通らない。Bug #31 と同型）。横展開検査は走査テストが自動で行うので個別追加は不要だが、手で見るなら `grep -rn "fetch(" resources/views/` で `/api/` を叩くものを洗う。⚠ **確認手順**: 本番/ローカルのブラウザで「**Ajax を一度叩いてから**必須項目を空で送信」する。これを省くと再発を見逃す。本番反映は `./deploy.sh`（`view:cache` 再生成）。⚠ **2026-08-03 追記 — 対象は GET だけ**（`storeCurrentUrl()` は GET のリクエストしか `_previous.url` を上書きしないので、POST / PUT / DELETE の `fetch` にヘッダーが無くてもこの欠陥は起きない）。⚠ **同じ欠陥がテナント・住宅事業・賃貸マンションの 15 箇所に残っていた** — 走査テストが `/api/realestate/` 限定だったため**原理的に検出できなかった**。走査を自社 API 全体へ広げて解消（実測 22 箇所）。走査自体の盲点は Bug #45 |
| 36 | バリデーションエラーの文言が **`validation.required` という生の翻訳キー**で画面に出る。**アプリ全体のフォームが該当**（約 200 ルート）。⚠ **画面を開くだけでは分からない** — 実際にエラーを起こさないと出ないため、初期コミット以来ずっと放置されていた（2026-07-29、Bug #35 の本番ブラウザ検証で「空送信 → 戻り先確認」をしたついでに初めて可視化された）| `APP_LOCALE=ja` / `APP_FALLBACK_LOCALE=ja` なのに **`lang/ja/validation.php` が存在しなかった**（`lang/ja/` の中身は `pagination.php` 1 本だけで、それも Bug #24 の対応でインライン番号付きページネーションへ統一した結果もう使われていない ＝ **実質 lang/ は空**）。fallback も ja なので **en の組み込みメッセージにも落ちない**（`__('validation.required')` が自分自身を返すことを tinker で確認）。⚠ **横展開 grep に出ない** — `$request->validate()` は **Laravel 内部**で `__('validation.*')` を呼ぶので、`grep -rn "__('\|@lang(" resources/ app/` では 1 件もヒットしない。⚠ **テストでは原理的に検出できなかった** — `phpunit.xml` が `APP_LOCALE` を設定しておらず、`config/app.php` の既定 `env('APP_LOCALE','en')` により**テストだけ locale=en** で走っていた。en には Laravel 組み込みの英語メッセージがあるため、テストは常に英語の正しい文を受け取り緑になる | `lang/ja/validation.php` を追加。**キー構造を `vendor/laravel/framework/.../lang/en/validation.php` と 1 対 1 に揃える**（135 キー。アップグレードで増減したとき diff で追えるように順序も原文どおり）＋ `attributes` に画面ラベル由来の和名。⚠ **`attributes` はアプリ全体で 1 つのマップしか持てない** — 実測で 259 キー中 **24 キーが画面ごとに別の意味**（`name` は 顧客名/発注者名/氏名/プラン名/物件名 等 **7 通り**で 32 箇所に出る）。中立な語を当てており、厳密な文言が要る画面は各コントローラの `validate()` **第 3 引数**で個別指定する（`validate($rules, $messages, $attributes)`。**第 2 引数は messages なので空配列を渡す**。第 3 引数が翻訳ファイルより優先されることは `FormatsMessages::getDisplayableAttribute()` で確認済み）。⚠ **2026-07-30 追記 — 項目名は別途 Bug #37 として洗い直した。**⚠ **`phpunit.xml` に `APP_LOCALE=ja` / `APP_FALLBACK_LOCALE=ja` を追加した**（本番と揃える。この 2 行を消すとロケール依存の欠陥が再び検出不能になる。追加後も既存 368 テストは全て green ＝ 英語メッセージに依存したテストは無かった）。回帰テストで固定: `tests/Feature/JapaneseValidationMessagesTest.php`（①**全 135 キーを直接引いて自分自身が返らないこと**＝ HTTP でいくつか踏むだけでは踏まなかったルールの欠落を見逃すため ②HTTP 経路で複数エラーが全部日本語になること ③フレームワークとのキー集合の一致＝**Laravel アップグレードでルールが増えたら落ちる** ④`app()->getLocale()==='ja'`）。⚠ 翻訳ファイルは `config:cache` / `view:cache` の対象外で**実行時読み込み**＝キャッシュクリア不要。`deploy.sh` の rsync 対象なので本番反映は `./deploy.sh` のみでよい |
| 37 | バリデーションの**項目名**（`:attribute`）が画面と食い違う。2 種類ある: ①**未定義キー 160 件** — `attributes` に無い項目は Laravel が snake_case を単語に開いて出すので、画面に **`guarantor1 name` `started at` `desired area min` のような英字**が出る（テナント契約の保証人欄・修繕の実施日・問合せの希望面積など）②**語のズレ 77 件** — 分譲地の画面ラベルは「所在地」なのにメッセージは「**住所**は必須です。」、DAD 工事案件は「工事名」なのに「プロジェクト名」、従業員は「在籍状況」なのに「ステータス」。⚠ **Bug #36 を直した直後の本番ブラウザ検証で発覚**（生キーが日本語になって初めて項目名の粗が見えた）。⚠ **画面を開くだけでは分からない**のは #36 と同じで、実際にエラーを起こす必要がある（2026-07-30 修正）| `attributes` は**アプリ全体で 1 つのマップしか持てない**ため、Bug #36 では「どの画面でも通じる中立な語」を当てて先送りしていた。未定義キーのほうは単純な追加漏れで、**Blade のラベルが `<label for="x">` と改行で分かれている画面（ZEAL / DAD）は素朴な grep に出ない**ため取りこぼしていた（最初の走査で 86 件、`<label>` の複数行対応を入れて再走査したら **160 件**あった）| **キーを 2 群に分けると大半はコントローラを触らずに済む。** ①**画面ラベルが 1 種類しかないキー**（実測 93 + 追加分）→ `lang/ja/validation.php` のグローバル値を直すだけ ②**画面ごとに語が変わるキー**（`name` は 物件名/顧客名/氏名/発注者名/専門分野名/名前 の 6 通り、`address` は 住所/所在地/市区町村・番地以降）→ グローバルは多数派のままにし、**少数派の画面だけコントローラの `validate()` 第3引数**で上書き（22 本）。⚠ **第 2 引数ではない**（`validate($rules, $messages, $attributes)`。間違えると messages として解釈され黙って無視される）。⚠ **括弧の注記は項目名に含めない**方針（「共益費（月額）」→ 共益費、「顧客（任意）」→ 顧客）。単位・税区分・任意/自動は項目名ではなく、「顧客（任意）は必須です」という矛盾も防げる。例外は `area_sqm`「面積（㎡）」/ `area_tsubo`「面積（坪）」で、単位が項目の区別そのもの。⚠ **保証人・緊急連絡先は画面ラベルが単に「氏名」「住所」**で複数エラー時にどちらか分からないため接頭辞を付けた（「保証人1 氏名」）。⚠ **`costs.*.estimated_amount` のようなワイルドカードは `attributes` 側でもそのまま書ける**（Laravel が `costs.0.…` を `costs.*.…` で解決する）。回帰テストで固定: `tests/Feature/JapaneseValidationMessagesTest.php` に 2 本追加 — ①**全コントローラの `validate()` キーを走査して和名が無いものが 0 件**（新しいフォームで和名を忘れたら落ちる。走査が空振りして緑になる事故を防ぐためキー数の下限 300 も固定）②**グローバル既定が「住所」のままであることと、分譲地の HTTP エラーが「所在地」であることを同時に検証**（片方だけ見ると「グローバルを所在地に書き換えただけ」で緑になり、住所ラベルの画面が壊れる）。**2 通りの変異（和名を 1 件消す / 第3引数を消す）で実際に赤になることを確認済み。** 本番反映は `./deploy.sh`（翻訳ファイルは実行時読み込みだがコントローラは `route:cache` 再生成が要る）|
| 38 | 物件種別を「仲介土地」に変更して保存しても、**DB に建物金額が残り続ける**。画面に建物欄は出ていないのに、一覧・詳細の合計金額と原価が建物込みのまま。契約ではさらに悪く、**「契約額（建物込み）」と「粗利（建物抜き）」が並んで表示され数字が食い違う**。⚠ **既存 408 テストのどれも検出しなかった**（2026-07-31、土地/建物分割のコード品質レビュー中に発見）| 画面は `x-show` で建物欄を隠し `:disabled` で送信対象から外していたが、**Laravel の `Validator::validated()` は未送信キーを結果に含めない**（`Validator.php` の `validated()` に `if ($value !== $missingValue) { Arr::set($results, $key, $value); }` があり、欠落キーはスキップされる）。よって `$model->update($validated)` は**そのカラムに触れず旧値を残す**。合計メソッド（`getAssessmentPriceTotal()` / `getContractAmountTotal()`）は `hasBuilding()` を見ない単純加算なので、残留した建物額が合計・粗利・原価同期（`syncPropertyPurchaseCost()`）・経営ダッシュボードに混ざる。契約側は `gross_profit` だけが `$validated` から再計算される（建物キーが無い → 0）ため、契約額と粗利が食い違う| **読み取り側を `hasBuilding()` 対応にするのではなく、書き込み側で正規化する**（読み取りで隠すと DB に嘘の値が残り続ける。Bug #34 の教訓）。モデルの `saving` フックで、建物欄を持たない構成のとき建物・消費税額の列を `null` にする。⚠ **条件に `hasBuilding()` を使ってはいけない** — 「土地のみ」と「**紐づけ不明**」を区別しないため。本番の FK は `ON DELETE SET NULL`（実測: `fk_re_contracts_procurement` ほか 4 本）で `ProcurementController::destroy()` に契約のガードが無く、**仕入れ案件を削除すると既存契約の `procurement_id` が NULL になる**。そこで正規化すると**記録済みの建物額・消費税額が失われる**（本番に該当契約が 1 件実在）。`ReProcurement::find()` で明示的に引き、**土地のみと確定できたときだけ**消す。`property_type` のように NOT NULL で常に確定する側にはこの区別は不要。回帰テスト 3 本で固定（①種別を土地のみに変えたら建物列が null ②紐づけ先を土地のみの案件に変えたら null ③**`procurement_id` が NULL でも消さない**）。⚠ **③が無いと①②の修正がデータ消失を生む。** 横展開検査: 条件付きで `:disabled` にする金額・数量入力を持つ画面は、`update()` で旧値が残らないか確認する（`grep -rn ':disabled' resources/views/`）。本番反映は `./deploy.sh` |
| 39 | `booted()` の `wasChanged([...])` の監視漏れを検出するはずの回帰テストが、**監視対象からカラムを削っても緑のまま**。テスト自体は正しく書けているように見え、「守られている」と誤認したまま先へ進む（2026-07-31、変異テストを省略しなかったことで発覚）| `Model::$wasRecentlyCreated` は `performInsert()`（`Model.php:1420`）で `true` になったあと、**フレームワークのどこでもリセットされない**（`performUpdate()` に該当処理は無く、`Model.php` 内の出現箇所は 129 行の宣言と 1420 行の代入だけ）。`create()` した**同じ PHP オブジェクト**で `update()` すると `wasRecentlyCreated` は `true` のままなので、`if (wasChanged([...]) \|\| $m->wasRecentlyCreated)` が常に真になり、`wasChanged()` 側の変異が完全に隠れる| `update()` の前に **`$m = $m->fresh();`** で取り直す（新しいインスタンスは `wasRecentlyCreated = false`）。実運用の編集フローはルートモデルバインディングで DB から取り直したインスタンスなので、`fresh()` のほうが**本番の経路にも忠実**。`$this->assertFalse($m->wasRecentlyCreated)` を併せて置くと意図が残る。⚠ **「テストが緑」は検証にならない** — 監視対象カラムを削る変異を入れて赤になることを確認し、さらに **`fresh()` を外すと同じ変異が素通りする**ことまで確かめる（2026-07-31 実測で `fresh()` の有無が検出可否を反転させた）。横展開検査: `grep -rn 'wasRecentlyCreated' app/` で該当する `booted()` を洗い、その回帰テストが `fresh()` を挟んでいるか見る |
| 40 | カラム名を変えたときの参照漏れを**テストが検出しない**。「SQL の `sum()` なら存在しないカラムで例外が出るから守られる」という前提が成り立たない（2026-07-30、土地/建物分割の変異テスト中に判明。当初プランはこの誤った前提で書かれていた）| テストは SQLite で走る。Laravel の `SQLiteGrammar` は `wrapValue()` を**上書きしない**ので識別子が基底クラス（`Grammar.php`）の**二重引用符**でラップされる（`MySqlGrammar` だけバッククォートに上書きしている）。SQLite は「二重引用符が既知の識別子に一致しないとき、黙って文字列リテラルとして解釈する」という MySQL 互換のフォールバックを持つため、`SUM("missing_col")` は**例外を出さず 0.0 を返す**。実測: `SUM("a")` → 300 / `SUM("missing_col")` → **0.0（例外なし）** / `` SUM(`missing_col`) `` → `SQLSTATE[HY000]: no such column`。コレクション側の `$collection->sum('col')` も Eloquent が未定義属性を null にするため同じく 0 を返す| **テスト環境では SQL sum もコレクション sum も等しく「静かな 0」に縮退する**と理解する（本番 MySQL なら `Unknown column` で落ちるが、**それはデプロイ後にしか分からない**）。カラムをリネームする改修では、**集計結果が実額であることを固定する回帰テストを必ず 1 本置く**（例: `tests/Feature/RealEstate/AmountAggregationNotZeroTest.php`）。⚠ **`assertSee` だけでは判定できない** — 一覧は各行にも金額を出すので、合計が 0 でも行の金額文字列に一致して **false-pass** する。**合計にしか現れない一意な値**を作り（30,000,000 + 12,000,000 = 42,000,000 のように）、`$response->viewData('salesAmountTotal')` で厳密に見る。⚠ 集計を `->sum('カラム名')` から `->sum(fn ($m) => $m->getXxxTotal())` のようにメソッド経由へ寄せると参照箇所が 1 つに集まり、直し忘れが起きにくい |
| 41 | 丸めの規約を 1 箇所直したのに、**画面が 1 ミリも変わらない**か、逆に**今まで無かった食い違いが生まれる**。2026-07-31 に両方を実測で踏んだ | **同じ計算の経路が複数あり、直した経路が「画面で使われている経路」とは限らない。** ⚠ **① 片方だけ直すと割れる**: 依頼は `HsContract::getBuildingTax()` の丸めだけだったが、`HsProperty::getBuildingSellingPrice()`（235-241 行）は成約時に `$this->contract->selling_price_building` を、`getEffectiveTaxRate()` は `$this->contract->tax_rate` を読む＝**同じ入力から同じ税額を出す 2 経路**。契約側だけ切り捨てにすると、物件一覧・物件詳細と契約詳細で消費税が 1 円食い違う（本番実測で `hs_contracts#1` と `hs_properties#12` が同額 18,345,455、`#2` と `#7` が 19,181,819 で衝突していた）。`HsCustomOrder` も揃えないと住宅事業の中で建売と注文住宅が別の丸めになる。⚠ **② 直した側が使われていない**: `ConsumptionTax::toExclusive()`（税込→税抜の逆算）を切り上げに直しても画面は変わらなかった。**税込入力欄は `name` 属性を持たず送信されない**ので、サーバは逆算を一度も実行していない（`grep -rn "toExclusive" app/` が **0 件**でテストからしか呼ばれていなかった）。実際に効いていたのは Alpine の JS `Math.floor(i * 10000 / (10000 + this.taxBp()))` が 3 箇所 | **丸めや計算規約を変える前に、その計算の経路を全部数える。** ① モデル間で同じ入力を読む経路が無いか見る（`grep -rn '\$this->contract->' app/Models/`）。あれば同時に直し、**両経路から引いて同値であることを 1 本のテストで固定**する（`HousingBuildingTaxRoundingTest::test_sold_property_and_its_contract_agree_on_building_tax`）。② **`grep -rn "<メソッド名>" app/` が 0 件なら、画面で効いているのは JS 側**。PHP は仕様の正本として揃えつつ、Blade の該当箇所と対で直す。JS は PHP のテストでは原理的に守れないので**走査テストを置く**（`tests/Feature/RealEstate/TaxExclusiveCeilingJsTest.php`。Bug #28 / #35 と同じ構図）。⚠ 消費税の丸めは**税額＝切り捨て / 税込→税抜の逆算＝切り上げ**で方向が違う（往復で 1 円落ちるのを防ぐため）。詳細は `app/Support/ConsumptionTax.php` の docblock。本番反映は `./deploy.sh` |
| 42 | 丸めを固定したはずの回帰テストが、**誤実装に戻しても緑のまま**通る。2 パターンあり、どちらもテスト自体は正しく書けているように見える（2026-07-31、変異テストを省略しなかったことで両方発見）| ⚠ **① 値テストは手書きの float `floor` を素通りさせる。** 切り捨ての境界として選ぶ値（端数がちょうど `.5` のもの）は **float でも正しく計算できてしまう**。実測: `12,345,675 × 10%` / `18,345,455 × 10%` / `10,000,007 × 8%` の 3 値とも `(int) floor($price * $rate / 100)` で正答する（`1234567.5` は二進で厳密に表せる）。だが float は Bug #33 で実測したとおり二進誤差で下振れするので、放置すれば再発源になる。⚠ **② 経路を固定する構造テストは、自分が書いた docblock に一致して false-pass する。** 実装が `ConsumptionTax::tax()` を経由していることをソース検索で確かめるテストを書いたが、**同じファイルの注意書き**に「⚠ `ConsumptionTax::tax()` は金額 null で null を返す」と書いてあったため、実装を `round` に戻しても文字列が残って緑のまま通った（実測: 変異後も `HsProperty.php` にコメント由来で 1 件残る）| **値テストと経路の構造テストを併置する。片方では足りない。** 構造テストは `token_get_all()` で `T_COMMENT` / `T_DOC_COMMENT` を落としてから検索する（`HousingBuildingTaxRoundingTest::sourceWithoutComments()`）。Blade の JS は正規表現で走査し、**税込→税抜の逆算（除数が `10000 + 税率bp`）は `Math.ceil`・税額（除数が `10000`）は `Math.floor`** を別々に固定する（片方を直すついでにもう片方を巻き込む誤りを防ぐ）。走査が空振りして緑になる事故を防ぐため**該当箇所数の下限**も併せて固定（Bug #32 / #35 と同じ流儀）。⚠ **「テストが緑」は検証にならない。変異を入れて赤になることを必ず実測する。** 2026-07-31 は計 9 通りを実測した（3 モデルの `round` 戻し ×3 / 手書き float `floor` ×2 / PHP を切り捨てに / PHP を常に +1 に / JS 1 箇所を `Math.floor` に / JS の税額を `Math.ceil` に）。**float `floor` の 2 通りは構造テストだけが検出し、値テストは全部緑だった** |
| 43 | 押せない理由を `title` に入れたのに、**ホバーしても何も出ない**。`title` は HTML に正しく出力されており、`view:cache` も `php -l` もテストも全部通る。区画一覧では画面に理由を示す要素が 1 つも無くなり、しかも `:disabled` にしたせいでサーバが返す 422 の理由文にも到達できず、**「押せば理由が出る」から「押せず理由も出ない」へ後退**していた（2026-08-03、削除ガード実装のコードレビューで Critical 指摘 → 本番ブラウザで実測確認）| **`disabled` なフォームコントロールはマウスイベントを発火しない**ため、ネイティブ tooltip が出ない。Firefox はかつて例外的に出していたが Bugzilla 274626 で他ブラウザに揃えており、**Chrome / Firefox / Safari / Edge のいずれでも出ない**。さらに `disabled` はフォーカス不能なので、キーボード・スクリーンリーダー経路にも届かない。Bug #28（`@push` が `@stack` 無しで静かに捨てられる）と同じ「**HTML には出るが実行時に効かない**」型。⚠ **同じ欠陥を 2 回作った** — 詳細画面で直した直後、**まだ実装していなかった区画一覧に同じ形が残っており**（設計書のサンプルコードが発生源）、そのまま実装されて再発した。Bug #41（同じ処理の経路が複数あり片方だけ直す）と同型で、**「1 箇所直したら、同じ形が他に無いか・これから書く箇所に無いかを必ず見る」** | **ホバーを受けられるラッパー `<span>` に `title` を移す**:<br>`<span title="…" style="display: inline-flex;"><button type="button" disabled aria-describedby="…">削除</button></span>`<br>ラッパーは `x-show` も `:style` も持たないので、静的 `style=` を置いても Bug #2 / #32 に触れない。Alpine で出し分けるなら **`:title="cond ? reason : null"`**（`''` だと依存なしの行に空の `title=""` が残る。`null` なら属性ごと削除される）。画面に理由を出す領域（パネル等）があれば **`aria-describedby` で紐づける**（tooltip だけではフォーカス不能な利用者に届かない）。`@click` は**残してよい** — `disabled` な要素は click を発火しないので無害で、別タブで状態が stale になった場合にサーバ側の拒否が効く二重防御になる。⚠ **検証は「HTML に `title` が出ているか」では不可能。** ブラウザで実際にホバーするか、`document.elementFromPoint()` の結果から祖先を辿って `title` を持つ要素に到達するかを見る（2026-08-03 本番実測でこの方法を使用）。⚠ 回帰テストは**ボタン自身が `:title` を持たないこと**と**ラッパー側に載っていること**を**対で**固定する。構造テストを「理由はボタンタグの中にある」と書くと、**正しい修正を入れたときに赤になる＝テストが不具合を守る**状態になる（実際に一度そうなった。Bug #42 ② と同型）。⚠ **`assertSee('建売物件 1 件')` は false-pass する** — パネルの件数行だけでなく**無効ボタンの `title` にも部分一致**する（`summarize()` の出力が「建売物件 1 件が参照しているため削除できません。」）。実測: 件数行だけ消すと**緑**、件数行と `title` の両方を消して初めて赤。タグ込みで見るか、役割ごとにアサートを分けること |
| 44 | 「契約・建売物件・注文住宅の 3 種で削除をブロックする」実装で、**ある入口のある参照元だけが一度もテストで実行されていない**。そのクエリを丸ごと消しても全テストが緑のまま。実装は仕様どおりで、テストも正しく書けているように見える。**3 回続けて別々の箇所で発生した**（2026-08-03、削除ガード実装中に毎ユニットのレビューが 1 件ずつ発見）| 判定の入口が複数あり（`DeletionBlockers` の `forLotIds` / `forEachLotId` / `forProcurementId` / `forProject`）、各々が 3 種の参照元を引く＝ **12 通りの組み合わせ**があるのに、テストは「代表的な 1〜2 種」しか作らない。**テスト名が `..._collect_all_three_kinds` でも実際は 2 種しか作っていない**、ということが起きる（実測で無防備だったのは `forProcurementId` の注文住宅 / `forEachLotId` の契約 / `forProject` の注文住宅 の 3 箇所）。⚠ 見つける度に個別に潰すやり方だと、**次の穴が別の組み合わせに残り続ける** | **入口 × 参照元の全組み合わせを機械的に測る。** 各消費箇所を `collect()` に潰し、`--filter` が赤になるかを表にして埋める。2026-08-03 実測で **4 入口 × 3 種 = 12 通りすべて検出**できることを確認した（手順とスクリプトは `docs/superpowers/plans/2026-08-02-realestate-deletion-guard.md` Task 10 Step 18）。⚠ **変異が本当に当たったか（`git diff` が非空か）を毎回確認する** — 当たっていない変異を「検出しない」と誤読する事故が実際に起きた。⚠ **同じ文字列がファイル内に複数あるときの一括置換に注意** — `lots.blade.php` の `err.message \|\| 'エラーが発生しました。'` は 3 箇所にあり、`/g` 無しの `sed` / `perl` だと**意図しない行**を書き換えるため、テストが正しく緑のまま残り「検出しない」と誤読する。狙う行を明示すること。⚠ 参照元が 4 種目に増えたら、表を 4 入口 × 4 種に広げて測り直す。⚠ **2026-08-17 追記 — 「当たったか」だけでなく「当たり先が正しいか」も見る。** 走査テスト（分類器）の変異で、無 SRI のスクリプトを**除外リストに載っているビュー**へ足してしまい「検出しない」と誤読した（分類器が拾わなくて当然だった）。**変異は「検査対象に入るはずの場所」へ当てること。** ⚠ **同じルール行が store / update の 2 箇所にある**型は 2 ラウンド連続で踏んだ。しかも `assertStringContainsString` は**片方だけ変えても緑**なので、**全出現を突き合わせる**形にする。⚠ **2026-08-17 追記 — 変異は「コミットしてから」当てる。** 未コミットのまま変異を当て、後始末に `git checkout --` を使うと**自分の編集ごと巻き戻る**（実測: SheetJS へ SRI を付けた 4 ファイル中 3 本とテスト 1 本が消え、そのまま気づかず次の変異へ進んだ）。さらに悪いことに、着弾判定を `git diff`（= HEAD との差分）で見ていると**自分の未コミット編集が「変異が当たった」と読める**ため、無効な測定が緑/赤どちらにも化ける。実測では 5 通り中 3 通りが「別の理由で赤」になっており、**理由まで見なければ成功と誤読していた**。手順を固定する: ①**先にコミット** ②各変異の**前**に `git status --porcelain` が空であることを確認（前の変異の残骸で測定が汚れるのを防ぐ）③**落ちた理由の文言まで**突き合わせる（「赤になった」だけでは、意図と別の機構が落としている可能性を排除できない）|
| 45 | **走査テスト（ラチェット）が、直したはずの欠陥と同型の欠陥を原理的に見逃す。** 盲点を 4 通り、1 セッションで実測した。テストは正しく書けているように見え、緑なので「守られている」と誤認したまま先へ進む（2026-08-03、Bug #35 の残り 19 本を潰す作業中に全部発覚）| ⚠ **① 列挙リスト方式は列挙漏れで無音になる** — 「直したファイル」を配列に並べる形だと、未修正のファイルは**そもそも検査対象に入らない**ので永遠に緑。実測で 19 本が野放しだった（しかも引き継ぎメモの「15 本」「19 本」がどちらも構成を間違えていた）。⚠ **② 検査文字列の引数名決め打ち** — `.then(function(r) { return r.json(); })` をリテラル比較していたため、`(res)` で書かれた `_buyer-select` の `checkDup` を見逃した（**「引数名が混在するので注意」と警告を書いた本人が、その罠をテストに作り込んでいた**）。⚠ **③ 正規表現の文字クラスが狭い** — `if \(![a-z]+\.ok\)` は camelCase（`roomsRes`）にマッチしない。⚠ **④ ブロック抽出の区切りが素朴** — `fetch(` から「最初の `.then(` まで」で切ると、`Promise.all` 内の `await fetch(…)` は近くに `.then(` が無いため固定長フォールバックへ落ち、**無関係な別ハンドラのヘッダーを拾って緑になる**。さらに `var url = '{{ url("/api/…") }}'` と組み立ててから `fetch(url, …)` する形は、呼び出し本体だけを見ても URL が分からず**原理的に拾えない**（実測 4 箇所）| **①「直した分を並べる」のをやめ、「対象を全件分類する」形にする。** `fetch` を持つ Blade を機械的に列挙し、どのリストにも入っていなければ落とす（`AjaxErrorFeedbackTest::test_every_fetch_view_is_classified`）。**新しいビューが無検査のまま増えることが原理的に無くなる**（対象 8 本 → 33 本）。**② 引数名は後方参照で**: `/\.then\(\s*function\s*\(\s*(\w+)\s*\)\s*\{\s*return\s+\1\.json\(\)/`。**③ 文字クラスは `[a-zA-Z_$][\w$]*`。④ ブロック抽出は括弧の対応で行い、URL 判定にだけ直前 5 行の先読みを入れる**（ヘッダーの有無はあくまで呼び出し本体だけで見る）。⚠ **分岐の「数」ではなく「中身」を見る** — `.ok` の分岐があっても中身が `console.error` だけなら画面は無音。波括弧の対応でブロック本体を切り出して表示先の有無を検査する（固定長で切ると隣のハンドラの `alert(` に一致して**誤って緑になる**）。⚠ **「テストが緑」は検証にならない。** 2026-08-03 は 7 通りの変異で実測し、うち**④の 2 件は新旧ロジックを差し替えて「旧ロジックでは緑・新ロジックでは赤」まで確認した**（改善が load-bearing であることの証明。片方だけ見ると「元から検出できていた」と誤読する）。⚠ **単一の「正準パターン」を機械適用しない** — 同じアプリに **null 返し / エンベロープ（`{ok, status, body}`）/ throw + 表示する `.catch`** の 3 方式が併存しており、どれも正当。既にエラー UI を持つ画面に `alert()` を当てると二重表示になり、デバウンス検索では打鍵ごとに `alert` が出る。**方式ごとに成立する不変条件だけを課す**（null 返しには「`.ok` 数 == `!data` 数」、それ以外には「`.ok` を見ている + 表示先がある」）|
| 46 | 詳細画面で**内訳と合計が別ソース**になっていて、無音で食い違う。罫線で仕切って「原価合計」と書いてあるので**内訳の積み上げに見える**が実体は保存カラム。本番実測(2026-08-03、契約 id 2 JG山西古澤邸): 内訳 7 行の合計 **26,700,000 円**に対し表示は **25,095,455 円**。粗利が **1,604,545 円過大**に出ており、契約一覧・経営ダッシュボードも `sum('cost_amount')` するのでその過大分が経営数字に乗っていた | `realestate/contracts/show.blade.php` の内訳は `$costBreakdown['costs']`（仕入れ案件・分譲地から毎回引く**ライブ値**）、合計は `$contract->cost_amount`（**契約時点のスナップショット**。利用者が手編集もできる必須項目）。⚠ **スナップショット自体は仕様として正しい** — 契約書に紐づく記録なので、仕入れ案件を後から直しても遡及しないのが会計上自然。壊れていたのは「別ソースの 2 つを、合計に見える形で並べた」こと。差 1,604,545 円の正体は、契約作成時の仕入れ案件が土地 7,350,000 円 / 建物 17,650,000 円(税込)に分かれていて `getPurchasePriceTotal()` が税抜(7,350,000 + ⌈17,650,000÷1.1⌉ = 23,395,455)を返していたこと。土地/建物分割の移行で建物が 0 に寄り、**土地は非課税なので割り戻しが消えて** 25,000,000 になり、スナップショットだけが取り残された | **合計をライブに寄せるのでも、スナップショットを捨てるのでもなく、両方出して乖離を見せる**（`ReContractController::show` で 1 度だけ算出し view へ渡す。Bug #26 の規約）。「原価合計」→「契約時点の原価」＋「現在の仕入れ原価」の 2 行に分け、差があるときだけ警告。粗利は従来どおりスナップショット基準（ライブに寄せると確定した契約の粗利が後から動く）。⚠ **同型を必ず探す** — 分譲地ブランチは表示上は無矛盾だったが同じ潜在乖離を持つので同時に直す。関連: memory「住宅事業の金額一覧で合計は内訳の積み上げにする」/ Bug #38（契約額と粗利が食い違う）。⚠ **変異テストで 2 回踏んだ**: ①**同じ乖離額が KPI の注記と内訳ブロックの警告文の 2 箇所に出る**ため、金額だけをアサートすると KPI 側を削っても緑のまま（Bug #43 と同型。KPI 固有の文言で見る）②**同じマークアップが 2 ブロックにある**ため、位置を曖昧に指定した置換が**仕入れ系の方を書き換え**、分譲地のテストが正しく緑のまま残って「検出しない」と誤読した（Bug #44 の「狙う行を明示する」を実際に踏んだ）。⚠ **入口ごとに測る** — 仕入れ系だけテストしていた間、分譲地側のライブ値を `0` に潰しても **471 テスト全部が緑**だった。回帰テスト: `tests/Feature/RealEstate/ContractCostDivergenceTest.php`（6 本。**7 通りの変異で赤を実測済み**）|
| 47 | **フォームの配線（HTTP メソッド・送信先・選択肢）が丸ごと無防備**。画面のリンクやボタンの URL を `assertSee` で固定してあるので「守られている」と読めるが、実測すると **18 通りの変異が全テストを素通り**した（2026-08-16、周辺ビル調査 Task 9 のコード品質レビュー）。`@method('DELETE')` を消すと削除ボタンが **405** で無反応、`@method('PUT')` を消すと「更新する」が 405、edit の `action` を store に差し替えると**編集したのに新規レコードが増える**、`<option value="">` を消すと**「未指定」に戻せなくなる** — どれもテストは全部緑 | **URL を見るテストは配線の半分しか押さえていない。** 呼び出し側（画面が描画した HTML）と受け側（ルート＋コントローラ）の間には、URL のほかに **HTTP メソッド・hidden フィールド・選択肢**があり、`assertSee('/tenant/…/surveys/1/edit')` はそのどれも見ていない。Bug #28（`@push` が `@stack` 無しで捨てられる）と同型で、**片方だけ壊れても HTML としては妥当**なのが気づけない理由。⚠ さらに `test_update_can_clear_the_surveyor` のように「画面からこの操作ができること」を謳うテストでも、**値を直接 POST していると画面からその操作を消しても緑**のまま通る | **画面が実際に描画したフォームを分解して、ブラウザと同じように送り返す**（往復テスト）。`tests/Feature/Tenant/AreaBuildingTestCase.php::parseForm($html, $needle)` が `[method, action, fields]` を返すので、`$this->post($form['action'], $form['fields'])` でそのまま送れる（`_method` で PUT / DELETE へ化ける）。⚠ **`$needle` は `action="…"` まで含めること** — 素の URL で探すと destroy の URL（`…/surveys/1`）が編集リンク（`…/surveys/1/edit`）に**前方一致して別のフォームを掴む**（実際に踏んだ）。⚠ **`@csrf` の欠落は Feature テストで原理的に検出できない** — `VerifyCsrfToken::handle()` が `$this->runningUnitTests()` で素通りするため（実測）。**描画された `_token` hidden の存在をアサートするのが唯一の手**。⚠ 「戻る」リンクと `cancel-url` のように**同じ URL を指す要素が 2 つある**箇所は、素朴なアサートが必ず false-pass するので**未カバーと明記する**（偽の安心より正直な穴のほうがよい。Bug #43 と同型）。⚠ **2026-08-17 追記 — JS の構造テストは「形」しか押さえられない。** ブラウザで走る JS は PHP のテストから原理的に守れないので、Blade ソースを走査する構造テストを置くことになるが、**3 段階で弱い**: ①**関数の存在と呼び出しだけ**を見ると、名前を残して body を戻した変異が素通りする → **波括弧の対応で body を切り出して**その中を検査する ②body を見ても**トークンの在否だけ**なら、`i < limit` を `i < 0` に変えた変異が素通りする → ループの形まで固定する ③それでも**振る舞いは押さえられない**。**振る舞いの正本は実駆動**（画面に載る `<script>` をそのまま node の `vm` で読み込んで入力を通す）。⚠ **PHP と JS の二重実装は、語彙・定数を突き合わせるだけでは足りない** — 語彙を触らずアルゴリズムだけ変異させると片側が無音で漂流する（実測: JS の `v = -v` を消すと `B15` がプレビュー +15 / サーバ -15 で無音破棄。Bug #41）。**両実装を同じ入力集合に通して突き合わせる**こと |
| 48 | **安全網（二重防御）を足すと、それが backstop している主機構の変異が検出できなくなる。** テストは緑のままなので「両方守られている」と誤読する（2026-08-17、周辺ビル調査 Task 9 で実際に踏んだ）| 調査回の重複年月は ①コントローラの事前チェック `monthTaken()` ②DB の UNIQUE 制約 の 2 段で守っている。②を捕まえて同じ差し戻しを返す try/catch を入れた**直後**、それまで赤になっていた変異「`monthTaken()` の `whereDate` を `where` に」が**緑になった** — ①が死んでも②が同じレスポンスを返すので、**HTTP の応答からは区別が付かない**ため。Bug #42 / #45 と同族の「テストが緑でも守られていない」型だが、**原因が「テストの書き方」ではなく「実装に安全網を足したこと」**なのが違う | **応答ではなく、主機構が仕事をしたかを直接見る。** `creating` / `updating` を監視して「**INSERT / UPDATE を試みていないこと**」までアサートする（`AreaBuildingSurveyCrudTest::watchWrites()`）。これで主機構と安全網が**独立に**固定される。⚠ **二重防御を入れたら、外側だけで緑にならないかを必ず測り直す** — 安全網は「守りが増える」と同時に「**測定器を鈍らせる**」。⚠ 逆に、安全網を入れないという判断（Task 12 の座標一括保存を `DB::transaction()` で囲まない等）にも理由を書き残すこと。書かないと次のレビューで必ず「囲むべき」と指摘され、囲むと**課金済みの API 呼び出しを巻き戻す**ような悪化が起きる |
| 49 | **`assertSessionHas*()` を呼ぶと、そのあと描画した画面からバリデーションエラーの表示が丸ごと消える。** 「エラー文が画面に出ること」を確かめたくてアサートを 1 行足すと、**その 1 行のせいで出なくなる**。⚠ **`old()` の復元だけは生き残る**ので、入力保持を見ているテストでは気づけない（2026-08-17、周辺ビル調査 Task 10 で発見・独立に再現）| **フラッシュされた `errors` バッグが消費されるため**（正確な機構までは詰めていない。挙動だけを実測で確定させた）。⚠ **`assertSessionHasErrors()` に限らない** — 実測で `assertSessionHas('errors')` でも同じ。`assertRedirect()` では起きない。<br>実測（POST でエラー → GET で再描画、間に挟む 1 行だけを変える）:<br>`（何も挟まない）` → サマリ**あり**<br>`assertSessionHasErrors('room_number')` → サマリ**なし**<br>`assertSessionHas('errors')` → サマリ**なし**<br>`assertRedirect()` → サマリ**あり**<br>いずれも `old()` の復元は**生存**。⚠ 関連: **`session('errors')` は `assertSessionHasErrors()` を通すまで生の配列**なので、`session('errors')->first(...)` を単独で呼ぶと `Call to a member function first() on array` で fatal になる（Task 9 で実測。上の探りでも再現した）| **画面の描画を検査するテストではセッションに触らない。** 期待文言はセッション経由でなく **`trans()` で組み立てる**（例: `trans('validation.max.string', ['attribute' => '部屋番号', 'max' => 50])`）。⚠ **サマリと `@error` は役割ごとに分けてアサートする** — 片方だけ消えても緑になる（Bug #43 / #46 と同型。実測で独立に赤くなることを確認すること）。⚠ **項目名だけを `assertStringContainsString('部屋番号', $html)` で見てはいけない** — `<label>部屋番号</label>` が常に出ているので `@error` を全削除しても緑になる（実測。**期待文言の全文**で見るか、`<li>…</li>` のようにタグ込みで見る）|
| 50 | **`git archive` で作った隔離コピーに `vendor` を symlink すると、変異が全部 no-op になり「検出しない＝カバレッジの穴」と誤読する。** 2 人のレビュアーが**それぞれ独立に**踏んだ（2026-08-17、周辺ビル調査 Task 10）| `vendor/composer/autoload_psr4.php` の `$baseDir = dirname(dirname(__DIR__))` が **symlink をたどって実 worktree に解決される**ため、`App\` / `Tests\` / `bootstrap/app.php` が全部 worktree 側から読まれる。`Application::inferBasePath()` も登録済み Composer loader の vendor ディレクトリから base path を導くので同じ。**隔離コピー側で書き換えた app / views / routes は一切読まれず、全部 GREEN に見える** | 隔離するなら **`cp -a` / `cp -Rc` で vendor を実体コピー**する。⚠ **さらにカナリア変異を先に通すこと** — Blade に未定義変数を足す等、**確実に赤くなるはずの変異**を 1 つ当てて、実際に赤くなることを確認してから本番の測定に入る。これを省くと「隔離できているつもり」のまま全部緑を集めてしまう。⚠ **scratchpad は同一セッションの他エージェントと共有される** — `scratchpad/iso/` のような汎用名は衝突する。`scratchpad/<用途>-<commit>/` のように一意にすること（実際に上書き事故が起きた）。⚠ そもそも**実ワークツリーで測るほうが確実**（並行レビュアーが居ないときは隔離不要） |
| 51 | **jsDelivr の `.min.js` に SRI を付けると、CDN が生成をやり直した瞬間に全画面のスクリプトが無音で死ぬ。** 付けた直後は動くので、テストもブラウザ確認も全部通る（2026-08-17、Chart.js 5 本へ SRI を付ける作業中に、貼る前の実測で発見）| **その URL のファイルが npm パッケージに実在しないことがある。** 実測: `chart.umd.min.js` は **unpkg で 404**（npm が配っているのは `chart.umd.js` だけ）。jsDelivr は要求されたら**その場で生成して返す**。生成物の先頭バナー自身がこう書いている:<br>`Skipped minification because the original files appears to be already minified.`<br>`Do NOT use SRI with dynamically generated files!`<br>⚠ **生成物のほうが大きいことすらある**（実測 205,399 bytes / 実ファイル 205,125 bytes ＝ バナーのぶん）。「min だから軽い」は成り立っていない。⚠ **SheetJS の `xlsx.full.min.js` は npm に実在する**ので同じ名前でも問題ない — **ファイル名では判断できない** | **publisher が実際に配っているファイルを使う**（`chart.umd.js`）。判定は簡単で、**unpkg で同じパスを引いて 404 なら jsDelivr の生成物**:<br>`curl -sL -o /dev/null -w '%{http_code}' https://unpkg.com/<pkg>@<ver>/<path>`<br>**ハッシュは 2 つの独立した CDN で突き合わせる**（jsdelivr / unpkg が一致 ＝ 片側の改竄でない裏取り）。生成物ではこの裏取りが**原理的に取れない**ので、取れないこと自体が「使ってはいけない」の判定になる。回帰テストで固定: `CdnScriptIntegrityTest::test_every_jsdelivr_script_is_pinned`（jsDelivr のスクリプトは全件 `PINNED_SRI` に載っていること。生成物 URL は実測の裏取りが取れず載せられない＝**`.min.js` へ戻す変更が自動で止まる**）。⚠ **変異で実測すること** — 「`.min.js` に戻す（SRI は付けたまま）」を当てて赤になり、`chart.umd.min.js` を名指しすることまで確認した。ここが止まらないと修正の意味の半分が守られていない。⚠ **検証は「HTML に `integrity` が出ているか」では不可能**（Bug #28 / #43 と同型）。ブラウザで `typeof Chart` と `Chart.getChart(canvas)` を見る ＋ **SRI 強制のカナリア**（正しいハッシュ = loaded / 1 文字違い = blocked）を通して、初めて「ハッシュが正しい」ことの証明になる |
| 52 | **画面に入力欄一式があるのに、サーバにもDBにも受け皿が無く、入力が黙って捨てられる。** 賃貸マンション 部屋契約の解約画面には敷金精算の入力（原状回復費・清掃費・差引項目の動的追加・解約理由）があり、Alpine が返金額を計算して出すので**入力中は正しく見える**。送信すると「契約を解約しました」と**成功表示が出る**。エラーも警告も一切出ない。初回実装（2026-04）以来ずっとこの状態で、**本番の `ms_contracts` が 0 件だったため誰も踏んでいなかった**（2026-08-17、賃貸マンション契約フローの監査で発見）| モックから起こした画面が仕様の全量を持っていたのに対し、**サーバ側が部分実装で止まっていた**。実測すると 3 方向とも欠けていた: ① `ContractController::terminate()` の `validate()` が `move_out_date` と `terminate_parkings` の **2 キーしか持たない** ② `ms_contracts` は 17 カラムで受け皿が**1 つも無い** ③ 差引項目の子テーブルも存在しない。**Laravel は列挙していないキーを黙って捨てる**（`validated()` は未定義・未送信のキーを結果に含めない）ので `update($validated)` は何事もなく成功する。Bug #38（`:disabled` で送らせず旧値が残る）と同族の「**画面にあるのにサーバへ届かない**」型だが、**#38 は受け皿があって旧値が残るのに対し、本件は受け皿自体が無く入力が消える** | 構成要素だけ保存し**合計は毎回積み上げる**（`ms_contracts` へ 4 列 ＋ `ms_contract_deductions` を新設。差引合計・返金額は**保存しない**＝ Bug #46）。敷金だけは `deposit_at_settlement` にスナップショットする（`deposit` は解約後も編集できるので、返金の根拠が後から動く）。⚠ **横展開検査は「画面の入力欄名とコントローラの `validate()` キーの突き合わせ」だが、素朴な正規表現は false positive を出す** — インラインの `validate([...])` リテラルしか拾わないと、`validateInput()` のようにメソッドへ切り出した実装が**全部「欠落」に見える**（実測でこれを踏み、賃貸マンションの他 6 画面を誤って疑った。本当の欠陥は解約画面 1 本だけだった）。⚠ **並行配列（`name[]` / `amount[]`）のテストは「真ん中の行が落ちる」データで書く** — `array_values()` で詰め直す変異を当てたら**緑のまま通った**。「末尾だけ不完全」なデータでは詰め直しても結果が同じで**原理的に区別が付かない**。名称 `['A','','C']` / 金額 `[100,200,300]` を送れば、正しい実装は C=300・詰め直す実装は C=**200** で赤くなる（実測で `Failed asserting that 200 is identical to 300` を確認）。回帰テスト: `tests/Feature/Mansion/ContractTerminationSettlementTest.php`（7 本。**画面が入力欄を描画していることも対で固定**＝ Bug #47）＋ `tests/Concerns/CreatesMansionSchema.php`（`ms_*` は raw SQL 管理でテスト用 trait が無かった。**DDL を変えたら trait も追従する**）。本番反映は **DB（`ALTER` ＋ `CREATE`）が先・`./deploy.sh` が後** |
| 53 | **CSV 取込のプレビューが 3 画面 12 経路すべてで 500。** ファイルを選んで取込ボタンを押すと `Call to a member function any() on array` で落ち、**取込機能が丸ごと使用不能**だった。⚠ **「エラー表示が無音の画面を直す」改修そのものが原因**（2026-08-17 の `846cdf9d`）で、翌日まで本番で継続していた。⚠ **画面を開くだけでは分からない** — 一覧は普通に開き、壊れるのは**ファイルを上げた後のプレビュー**だけ（2026-08-18、CSV 取込のテスト整備の下調べで発見）| **`$errors` は Blade の予約変数。** `ShareErrorsFromSession` が全ビューへ `ViewErrorBag` として共有しており、**view データに同名のキーを渡すと上書きされる**。3 つの取込コントローラは行エラーの**配列**を `'errors' => $errors` として渡していた（実測 12 箇所）。長らく無害だったが、`846cdf9d` が「validate() の失敗が無音」を直すため `index.blade.php` に `@if($errors->any())` を足した瞬間、配列に対して `any()` を呼んで fatal になった。⚠ **`846cdf9d` の検証は 500 する経路を通っていなかった** — 不正な拡張子を投げて差し戻し先を見る手順は、`validate()` で弾かれて `back()` するだけで**プレビューを一度も描画しない**。Bug #28 / #43 と同じ「HTML/コードは妥当なのに実行時に死ぬ」型| 行エラーのキーを **`rowErrors` へ改名**（コントローラ 12 箇所＋ビュー 3 本）。顧客取込ビューは16/20 行目が**本物の `ViewErrorBag` 用途**なのでそこは残す。回帰テストは `ImportPreviewRenderTest` の**挙動＋構造の対**（Bug #28 / #35 と同じ構図）: ①**挙動** — 取込の入口を全件機械的に列挙し、その画面自身のテンプレート CSV を落として**そのまま上げ直し**、プレビューが 200 を返すこと。列挙は**ルート名でなくコントローラのメソッド名**で行う（ルート名は `execute-property` / `property` / `execute` と 3 画面で不揃いだが、メソッド名は `execute{X}` ↔ `download{X}Template` で完全に規則的）＝**新しいタブが増えたら自動で検査対象に入る**（Bug #45 の「全件分類」）②**構造** — どのコントローラも view へ `'errors'` キーを渡していないこと（括弧の対応で `view(...)` を切り出し、**コメントを落として**から測る。Bug #42 ②）。⚠ **200 を見るだけでは足りない** — ビューだけ `$errors` に戻す変異は **830 テスト全部が緑のまま通った**（実測）。`ViewErrorBag` は `Countable` なので `count()` が 0 を返し、`@if(count(...) > 0)` が false になって**エラー行の一覧が画面から丸ごと消える**が例外は出ない。→ コントローラ側の件数（`viewData('rowErrors')`）と画面の表示を突き合わせる（Bug #43 / #46 / #49 と同じ「表示が消えても緑」型）。⚠ **列挙をデータプロバイダに置いてはいけない** — プロバイダは Laravel 起動前に評価されるので `Route::getRoutes()` が `A facade root has not been set.` で落ちる（実測）。列挙はテスト本体で行い全経路の結果をまとめて突き合わせる。**5 通りの変異で赤を実測済み**（コントローラ 1 箇所戻す / 走査の括弧対応を壊す / 列挙を 1 本に絞る / ビューだけ戻す / テンプレート配信ルートを消す）。⚠ **変異が意図と違う理由で落ちることがある** — テンプレートのルート登録は 2 行なので 1 行だけコメント化すると**構文エラーで赤**になり、成功と誤読する（実際に踏んだ。Bug #44 の「理由の文言まで見る」）。⚠ 副産物として **`survey_questions` はリポジトリに正本が無い**ことが判明した（migration にも `database/sql/` にも無く本番で直接作られたまま）。テスト用は `tests/Concerns/CreatesSurveyQuestionSchema.php` にモデルと `validate()` から起こしてある。本番反映は `./deploy.sh`（`view:cache` 再生成） |
| 54 | **CSV 取込の日付検証が `strtotime()` で、存在しない日付を素通りさせていた。** `2026-02-30` と 1 行打ち間違えるだけで**数百行の取込が丸ごと巻き戻る** — 本番 MySQL は strict mode なので `Incorrect date value` を投げ、取込の `catch` が `rollBack()` する ＝ 利用者には生の SQL エラーだけが出て、**正しかった残り全行も入らない**。逆に **`1970-01-01` は理由なく拒否**されていた。⚠ **テスト DB の SQLite は `'2026-02-30'` をそのまま格納する**（実測）＝「DB が弾いてくれる」前提では**原理的に検出できない**（Bug #40 と同族）。ヘルパは 2 つの取込コントローラが**逐語コピー**で持ち、呼び出しは **11 箇所**（`Admin/MansionImportController` 6 / `Admin/TenantImportController` 5）（2026-08-18、賃貸マンション CSV 取込のテスト整備で発見）| 条件が `preg_match('/^\d{4}-\d{1,2}-\d{1,2}$/', $value) && strtotime($value)` の 1 行で、**バグが 2 つ同居していた**。① **`strtotime()` は存在しない日付を繰り上げて成功を返す** — `2026-02-30` は 3/2 と解釈されるのに関数は入力文字列 `'2026-02-30'` を**そのまま返す**ので誰も気づかない（`2026-04-31` / `2026-02-29`（2026 は閏年でない）/ `0000-01-01` も同じ）② **`strtotime('1970-01-01')` は `0` ＝ falsy** なので、アプリの timezone（UTC）では Unix epoch だけが弾かれる。**書式の検査（`preg_match`）と存在の検査（暦）は別物**なのに、`strtotime()` の戻り値を後者の代わりに使っていた | **`checkdate()` で存在を判定する** — `app/Support/CsvDate::normalize()` に 1 本化し `strtotime()` を使わない（`0` が falsy の件も同時に消える）。直せば**既存の行別エラー経路**（`$errors[] = …; continue;`）に落ち、**その 1 行だけ報告され残りは全部入る**（実測 `totalRows=2, validCount=1`／不正行は `create()` に到達しない）。⚠ **抽出と修正はコミットを分ける** — 「動かして壊した」と「直して変えた」が混ざると切り分け不能（抽出 `537bc330` / `42d61609` / `91030aab` / `7b216c6c` は振る舞い不変、修正は `723b4018`）。逐語コピー 4 本を `CsvDate` / `CsvImportReader` ＋ `CsvImportException` / `CsvImportTemplate` へ寄せた（例外の `getMessage()` は既存の画面文言そのままで UI 不変）。⚠ **`getNextPropertyCodeNum()` は意図的に統合しない** — 2 実装は**わざと違う**（賃貸マンションは `MS-` を `orderByDesc('id')`、テナントは `T-` を `T-%` 絞り込み ＋ `orderByDesc('code')`）。⚠ 修正の検証は **23,100,000 通りの全掃引**を独立実装のグレゴリオ暦（**`checkdate()` を `checkdate()` で検証しない**ためわざと別実装）と突き合わせた: **正当な日付の取りこぼし 0 件**、回復 1 件（`1970-01-01`）、新たに拒否した実在しない日付 約 254 万件。<br>**以下「テストが緑でも測っていない」型の実測。ここが本件の本体。**<br>⚠ **① Laravel を起動しない Unit テストは `config/app.php` でなく `php.ini` に支配される。** `CsvDateTest` は `PHPUnit\Framework\TestCase` 継承なので `'timezone' => 'UTC'` が効かず、効くのは `php.ini` の `date.timezone`（`phpunit.xml` も無指定 ＝ **走らせるマシン任せ**）。実測 `strtotime('1970-01-01')`: UTC `0`（falsy）/ Asia/Tokyo `-32400` / America/New_York `18000` / Europe/London `-3600`（いずれも truthy）＝ **epoch の回帰テストは 6 環境中 5 環境で無音のまま検出力を失っていた**（macOS 上の日本語プロジェクトで `Asia/Tokyo` は十分あり得る）。`setUp()` で `date_default_timezone_set('UTC')` を固定し **`tearDown()` で戻す**（戻さないと同一プロセスの後続テストへ UTC が漏れて別の検出力を削る）。実測 **5/6 で緑 → 6/6 で赤**（`a8a8436a`）。<br>⚠ **② リクエストを手で組み立てた往復テストは、手で書いた部分を守らない。** 確定ヘルパの初版はプレビューから `csv_data` hidden だけを取り、`'confirmed' => '1'` と POST 先は**手書き**だった。実測: `name="confirmed"` の改名 / `action` を別タブへ差し替え / `@csrf` 削除 の **3 通りとも緑**。本番では `confirmed` の破損がとりわけ悪質で、「インポート実行」を押しても**プレビューが再描画されるだけ**＝何も書かれず何のエラーも出ない。**描画されたフォーム（`action` と全 hidden）を解析してそのまま送り返す**形に直し 3 通りとも赤（`d679e800` / `dfbc7f79`）。Bug #47 を 2 段ウィザードへ適用した形。⚠ **抽出のアンカーで 2 つ踏んだ** — 「インポート実行」は**ページに 2 回出る**（フォーム外の見出しにも）ので素の語で遡ると**レイアウトのログアウトフォーム**を掴む → `<button>` を起点にする。プレビューには**フォームが 7 本**あり各タブのアップロードフォームがそのタブの URL を共有するので `action="…"` を needle にするのも安全でない。⚠ **`@csrf` の欠落は Feature テストでは原理的に検出できない**（`VerifyCsrfToken::handle()` が `runningUnitTests()` で素通り）→ **描画された `_token` hidden の存在**を見るのが唯一の手。<br>⚠ **③ 画面が出せないリクエストのテストは書かない。** `_preview.blade.php` は取込フォームを `@if($validCount > 0)` で包むので、**全行エラーの CSV ではフォームが 1 本も描画されない**（代わりに「インポート可能なデータがありません。CSVを修正してください。」が出る）。プランはその場合に `'部屋インポート完了: 0件を登録しました'` を期待していたが、その文言は実在しても**画面が決して送らない POST を手で作らないと到達しない**。「プレビューが取込を提示しないこと」を見るヘルパへ置き換えた。<br>⚠ **④ `assertSee` は警告とエラーを区別できない。** `test_a_second_active_contract_warns_but_still_imports` は「警告 → エラー」変異を守っていると自称していたが、実測で `$warnings[] =` を `$errors[] =` に変えても**全テスト緑**。原因が 2 つ重なる: その push には `continue` が無いので**行は取り込まれたまま**で件数アサートが動かない／`assertSee('には既に契約中の入居者がいます')` は**どちらのチャネルでも一致する**。`viewData('warnings')` / `viewData('rowErrors')` で**役割**を見たうえで、画面表示（警告リストの行 と 件数チップ）も**別々に**アサートする（実測で**独立に** load-bearing）。Bug #43 / #46 / #49 と同族（`89c8d8ed`）。⚠ 記録として `MansionImportController` の本物の `$errors[] =` は **60 箇所すべてが `continue` を伴う** — `continue` 欠落は変異の性質であって実在の欠陥ではなかった。<br>⚠ **⑤ 足した DB 制約が効いているとは限らない。書く前に測る。** テスト用スキーマ（`tests/Concerns/CreatesMansionSchema.php`。`ms_*` は raw SQL 管理で migration が無く手で維持）へ本番と同じ UNIQUE 3 本（`ms_properties.property_code` / `ms_rooms (property_id, room_number)` / `ms_parkings (property_id, parking_number)`）を足した（`08ed6801`）。**だが実測では 3 本とも消しても全テスト緑**だった — 部屋の重複はコントローラの**プレビュー段階の事前チェック**で止まり INSERT に到達しないので制約が発火しない。制約自体は**将来の false-green を止める**ので持っておくのが正しい（Bug #40）が、「load-bearing だ」という主張のほうが誤りだった。**思い込みでなく実測を書き残すこと。** ⚠ 同 trait は `ms_*` を **8 本**作るが本番は **9 本** — `ms_parking_contract_revisions` が無い。今は遅延 `hasMany` からしか参照されないので落ちていないだけ。<br>⚠ **⑥ 広い走査テストがあっても、それが自分を守っているとは限らない。** Bug #53 の `ImportPreviewRenderTest`（12 経路の全件分類）は `csv_data` hidden を改名しても**緑のまま** — 確定の往復を一度もしないため。守っているのは新設の `MansionImportTest` だけ。**重なって見える 2 本が互いに素な範囲を見ている**ことがある。<br>⚠ **⑦ `getNextPropertyCodeNum()` は脆い**（記録のみ・未修正）: `MsProperty::orderByDesc('id')->first()` の**そのレコード**の連番を +1 する ＝ **最新行であって最大コードではない**。`property_code` は UNIQUE なので id 順と食い違うコードが 1 つでも入る（データ移行・手動 INSERT・一括投入）と次の取込がトランザクション内で例外を投げ、`rollBack()` が**取込全体**を捨てる。現状 UI からは到達しない（`property_code` は `PropertyController::validateInput()` に無い）し `Mansion\PropertyController::generateNextCode()` も同方式なので 2 経路は相互に整合している。`MAX(property_code)` にすればこの型ごと消える。<br>⚠ **検出力は 13 通りの変異で実測し 12 検出 / 1 未検出**（未検出は ④。その後修正）。作法は Bug #44 どおり: ①**先にコミット** ②各変異の前に `git status --porcelain` が空 ③`git diff --stat` が非空で着弾を確認 ④`git checkout -- <当該ファイル>` で戻す ⑤**赤/緑でなく落ちた理由の文言**を突き合わせる。全結果は `docs/superpowers/plans/2026-08-18-mansion-csv-import-tests.md`（`dc98e0de`）。回帰テスト: `tests/Unit/Support/CsvDateTest.php` / `CsvImportReaderTest.php` / `CsvImportTemplateTest.php` ＋ `tests/Feature/Admin/MansionImportTest.php`（21 本。**6 取込経路すべてをプレビュー → 確定の往復で見る**）。全体 830 → **868 テスト**。DB 変更は無いので本番反映は `./deploy.sh` のみ |
| 55 | **Excel 取込の列名自動判定が、実運用の見出しに当たらない。** 周辺ビル調査の「営業（空室）状況調査表」は見出しが **`階` `空` の 1 文字**で、`/(階数\|総階)/` `/(空き\|空室\|空店)/` のどちらにも当たらず「— 使わない —」のまま残る。気づかず進むと **総階数が空・空き 0 件**で 187 棟が入り、**空室率が全棟 0%** になる。⚠ **完全成功に見える** — プレビューは警告 0 行、結果も「ビル新規 187 件 / 調査追加 187 件 / 値が不正でスキップ 0 件」。⚠ **正しい設定で取り直しても直らない** — 同一ビル・同一年月は既存としてスキップするので**調査回が追加されない**（ビルの `total_floors` だけは「空の項目だけ補完」で後から埋まるが、営業/空き/不明は据え置き）。直すには調査回を 1 件ずつ消すか編集するしかない（2026-08-19、利用者が列マッピングの画面を見て「おかしい」と気づいて発覚。実行前だったので実害なし） | 判定語彙を**設計時に想像した見出し**（階数 / 総階数 / 空き / 空室）だけで書いていた。日本語の業務 Excel は**見出しが 1 文字**のことがある（`階` `空` `計`）。判定は `areaImportCellText(...).replace(/\s/g, '')` で**空白を除去してから**当てるので、`ビ　ル　名`（全角空白入り）は `ビル名` になって当たる一方、1 文字の見出しは語彙に無いので落ちる。⚠ **JS の `\s` は U+3000 を含む**ので全角空白は勝手に潰れる（`AreaBuilding::normalizeName` の PCRE2_UCP と同じ結論）。Bug #28 / #43 と同じ「**HTML もコードも妥当なのに実行時に無音で壊れる**」型だが、**壊れるのが「入るデータ」なので画面のどこにも異常が出ない**のが違う | `AREA_IMPORT_TARGETS` の guess に **`^階$` `^空$` を足す**（`import.blade.php`）。**空白除去後に当てるので `^…$` は 1 文字の見出しにだけ効き**、既存語彙は不変。回帰テストは **Blade から guess 表を取り出して PCRE として当てる**（PHP から JS の正規表現は原理的に実行できない。`TaxExclusiveCeilingJsTest` と同じ走査の流儀）。⚠ **走査が空振りして緑になる事故を防ぐため取り出せた項目数（7）も固定**する（Bug #45）。⚠ **合計列 `計` がどれにも当たらないことを対で見る** — 件数列に当たると合計値が営業/空きとして入り、空室率が静かに壊れる。⚠ **`used` を持つ形でテストする** — 実装は「先に当たった target が勝つ」ので、1 列ずつ独立に見るだけでは列同士の取り合いを検出できない。**4 通りの変異で赤を実測**（`^階$` 除去 / `^空$` 除去 / 走査の空振り / vacant に `計` を足す）。⚠ **1 回目の変異が「当たらなかった」のを危うく「検出しない」と誤読しかけた** — シェル経由の置換でエスケープが合わず 0 箇所置換だったのに `OK` が出た。`git diff --stat` が空なのを見て無効測定と判定した（Bug #44 の作法がそのまま効いた）。⚠ **運用の作法**: 取込前に**列マッピングの画面を必ず読む**。件数列が 1 つでも「使わない」なら、プレビューの数字が正しく見えても台帳は 0 で埋まる。⚠ **同名のビルは無音で 1 棟に畳まれる** — 2 行目は「同一年月のためスキップ」に計上されるが、結果メッセージは**理由が「同名」だとは言わない**（実測: 187 行中 3 組が同名。`雅ビル` 4F/5F・`武智ビル` 4F/3F・`相原ビル`）。**取込前に重複名を数え、別棟なら名前を区別してから流す**。回帰テスト: `AreaBuildingImportTest::test_column_guesses_hit_the_headers_of_the_real_survey_sheet` / `..._cover_the_common_header_vocabulary`。本番反映は `./deploy.sh`（`view:cache` 再生成）|
| 56 | ページを開いた直後、**1024px 以上のある幅でだけ**、①賃貸マンション契約一覧・ZEAL 体験予約一覧・ZEAL 会員一覧で**スクロールできるのに「横にスクロールして全項目を表示」と右端のフェードが出ない** ②工程表ボードの**初期スクロールが 220px 手前で止まり**当月が見えない。スクロールするか画面幅を変えると直る。テストも `view:cache` も HTML も全部正しい。症状が出る幅は「表の実幅 + 66〜286px」で、ローカル実測（2026-09-11）は 1100px の部屋契約一覧（`scrollWidth` 880 > `clientWidth` 814 なのに非表示）・1280px の体験予約一覧（1109 > 994 なのに非表示）。②は本番の住宅ボードで `scrollLeft` 146 ／ 右端 366。③（2026-09-13 のレビューで追加）賃貸マンション入居者一覧は判定の script 自体が無く、表が収まる幅でもヒントとフェードが常に出ていた（`</table>` の後の閉じタグも無く、ページ送りと注記が表の枠の中に入っていた） | **ページの `<script>` はパース中に同期で走り、Alpine（`@vite` の module ＝ defer）より前に動く。** その瞬間は PC 展開サイドバー（`layouts/partials/sidebar.blade.php`）が `x-cloak` ＝ `[x-cloak]{display:none !important}` で幅 0 ＝ 表示領域が **220px 広い**（一時計測で実測: `readyState` loading・Alpine 未起動・`aside[x-cloak]` 3 本・領域 1034px）。①は以後 scroll / resize でしか測り直さないので、Alpine がサイドバーを出して領域が 814px に縮んでも「スクロール不要」の判定が残る。②はブラウザが目標をその時点の（220px 小さい）右端で丸め、右端が伸びても**上方向へは丸め直さない**。⚠ **根はサイドバーの側** — 展開サイドバーは `body` の `x-data` で `sidebarExpanded: true` 固定（永続化なし）＝ 起動後は必ず表示されるのに `x-cloak` が付いていて、**起動前だけ別のレイアウト**を作っていた。⚠ **1024px 未満では再現しない**（サイドバーが `hidden lg:flex` でパース中も後も幅 0）ので、狭い幅だけの確認では見逃す | **① 根本: PC 展開サイドバーの `x-cloak` を外した**（f11229c2）。パース中と起動後で `main` の幅が一致し、読み込み時にサイドバーが後から出るガタつきも消えた（`app.js` を遅らせても最初の描画から 220px・グループの中身は 0 個を実測）。⚠ 外すのは**展開サイドバーの外枠だけ** — 折りたたみ版（`x-show="!sidebarExpanded"`）・モバイルのドロワー（`x-show="sidebarOpen"`）・グループの中身（`components/sidebar-group.blade.php`）は起動前に隠れている必要があるので残す。**② 4 一覧はパース中に判定し、DOMContentLoaded でも測り直す**（`update();` の後に `document.addEventListener('DOMContentLoaded', update);`。646c1245。入居者一覧は 25bfe0c6 で script と閉じタグを足した）。⚠ **DOMContentLoaded だけにしてはいけない** — 一度そうした（14e400a1）が退行だった。JS の取得が遅いと最初の描画は DCL より前になり、DCL まで既定の「表示」のまま描かれる（`app.js` を 300ms 遅らせる中継プロキシで、表が収まる 1440px でも 51〜345ms の 19 描画すべてに出た。FCP 88ms ／ DCL 370ms）。14e400a1 に書いた「最初の描画は DCL より後なので一瞬出て消えることは起きない」は遅延なしの手元条件でしか成り立たない誤りだった。直した後は同じ条件で 4 画面 × 1440 / 1100px を計 20 回読み込み、DCL より前の描画が最終状態と食い違ったのは 1 回だけ（入居者一覧 1100px の最初の 1 描画 ＝ この script より前に描画が挟まった。既定が「表示」なので避けられず、修正前の 3 画面も同じ構造）。工程表ボード・区画一覧は DOMContentLoaded だけのまま（① で幅は同じになったので安全側）。**③ サイドバーの開閉でも測り直す** — 開閉は window の resize を起こさないので、`body` の `x-init` で `$watch('sidebarExpanded', …)` → `$nextTick` → `requestAnimationFrame` → `resize` を送る（e0b2140f）。⚠ **`$nextTick` だけでは足りない** — Alpine 3.15 の x-show は起動後の切り替えを、画面が見えているとき `requestAnimationFrame` まで遅らせる（`_x_toggleAndCascadeWithTransitions`）。`$nextTick` は setTimeout なので切り替えの前に開閉前の幅で測り、**閉じる → 開くだけでスクロールできるのにヒントが消える退行**になっていた（c400511b。見えているブラウザで実測: 「閉じる」で resize 2.6ms ／ 切り替えは次のフレームの 8〜9ms）。逆に `requestAnimationFrame` だけ（`$nextTick` なし）だと、片方のサイドバーが出てもう片方がまだ消えていない瞬間（領域 758px）に測り、閉じた後は収まるのにヒントが残る（実測）。⚠ **Alpine のタイミングは画面が見えているブラウザで測る** — 非表示のタブでは x-show が setTimeout 経由になり順序が入れ替わるので、c400511b の形でも**正しく動いて見えた**（ブラウザペインで実測）。構造テストも緑のままだった。回帰テスト: `tests/Feature/LayoutMeasuringScriptTest.php`（幅・スクロール位置に触るインライン script を**全件分類**し（2026-09-13 時点で 12 本。Top trap #13）方針を持たせる: `DCL_ONLY` ＝ 工程表ボード・区画一覧 ／ `PARSE_AND_DCL` ＝ 4 一覧 ／ `null` ＝ 操作の後にだけ測る。`class="scroll-fade-right"` を持つビューは `PARSE_AND_DCL` 必須。コメントと文字列を落としてから波括弧の対応で解析し、釣り合わない script は解析できないとして落とす。実行されない `type` の script は数えない）＋ `tests/Feature/LayoutSidebarCloakTest.php`（展開サイドバーに `x-cloak` が無い ／ 残す 3 種には有る ／ 開閉の `x-init` を入れ子まで固定）。⚠ **構造しか見られない** — Chart.js の生成・Alpine の `init()` の中の計測は走査に見えない。実際の動きはブラウザで幅と開閉を変えて測る（4 画面 × 1024〜1800・375px の 8 幅と、開閉 3 幅で「`scrollWidth − clientWidth > 2` ＝ ヒント ＝ フェード」）。変異は 1 回目 13 通り ／ 2 回目 19 通り ／ 3 回目 10 通り（2・3 回目はカナリアを含む）がすべて期待どおり（緑は等価変異だけ）。⚠ **非表示のタブではスクロールイベントが配られない**（`scrollLeft` は右端まで動くのにハンドラが 1 回も走らない）ので、スクロール後の挙動を自動操作で見るときはイベントを明示的に送るか、見えているブラウザ（Playwright）で測る。本番反映は `./deploy.sh`（`view:cache` 再生成） |
| 57 | 地下の区画（表示名 `B1A`）が、テナント契約の一覧・詳細・編集・賃料改定・解約・削除確認、物件詳細の契約／解約タブ、問合せの希望区画、問合せ・投資の区画選択で **`-1B1A`** と出る。賃料改定と削除確認は**同じ画面の中で**パンくず・注意書きが `B1A`、本文が `-1B1A` と食い違っていた。さらに修繕の登録・編集の区画選択は**地上も `11A` `33A`**（本番の削除されていない 151 区画すべて）、物件詳細のフロアマップの階ラベルは `-1F`（2026-09-12 修正） | 表示名 `units.display_name` は**保存時点で既に階を含む**（`Unit::generateDisplayName()`: 3 階 A → `3A` ／ 地下 1 階 A → `B1A` ／ 階なし A → `A`）のに、表示側 11 か所が「表示名が数字で始まらなければ階を前に付ける」（初期コミット 2046289d から）、修繕の 2 か所が**条件なしで**階を付けていた。2026-04-13（8bd52915）に地下（階 -1〜-3）を足したとき表示側が直されず、`B` で始まる地下だけ前置きが働くようになった。本番の区画 158 件を読み取りで数えると、**表示名は全件 `generateDisplayName(階, 号室)` と一致**し（食い違い 0・階なし 0）、前置きが働くのは地下の 3 件だけ ＝ **正しいデータには一度も役立っていなかった**。表示名を書く経路は区画の登録・編集（`UnitController`）と CSV 取込（`TenantImportController`）の 2 つだけで、どちらも同じ規則 | **表示名をそのまま出す**（計算そのものが消える。区画詳細・区画一覧・顧客詳細など 20 か所以上は元から表示名を直接出していた）。フロアマップは `(is_int($floor) && $floor < 0 ? 'B' . abs($floor) : $floor) . 'F'`（周辺ビル調査の `AreaBuildingTenant::floorLabel()` と同じ `B1F`）。⚠ **`is_int` を外すと物件詳細が 500 になる** — 階なしの区画は `groupBy('floor')` で `''` のキーになり、PHP 8 では `'' < 0` が true・`abs('')` は TypeError（実測。変異 F3 で `abs(): Argument #1` の赤を確認）。回帰テスト `tests/Feature/Tenant/UnitLabelDisplayTest.php`（画面ごとに 1 本 ＋ フロアマップ 2 本 ＋ 走査 1 本）。⚠ **`assertSee('B1A')` は `-1B1A` に部分一致して素通りする**（Bug #43 の型）— 前後の文脈ごと見て `assertDontSee('-1B1A')` を対で置く。物件詳細はフロアマップも区画カードに表示名を出すので、**契約番号のリンク → 日付セル → 区画セル**を 1 本の正規表現で辿って、その行だけを見る。⚠ 走査（`->floor` の直後の連結（`. 'F'` の階ラベルは除く・`?->` も）／ Blade の `{{ …->floor }}{{` の並び ／ 文字列展開 `{$…->floor}{$` が `app/` `resources/views/` に 0 件。**右辺の変数名に依存しない**。2026-09-12 の版は右辺を `$dn` / `$displayName` / `…display_name` に決め打ちしていて、`$u->floor . $name`・`?->`・Blade の並べ書き・文字列展開を素通りさせた（2026-09-13 のレビューで実測。Top trap #13 ②）。「数字で始まらなければ」の判定も見ていたが、表示名と無関係な正当なコードを落とすので外した。既知の悪い形 7 通りに当たり、正当な階ラベル 3 通りに当たらないことを自己テストで固定）は**既知の書き方しか守らない** — 区画を空にする・`階 . 号室` にする変異（L14 / L15）は走査に見えず、画面ごとのテストだけが検出した。走査は修正後に書くと最初から緑なので、**修正前のファイルを一時的に戻して 13 か所を名指しして落ちる**ことで赤を確かめた。変異 34 通り（依頼 1・2 の合計。カナリア 2 を含む）はすべて期待どおり — 期待どおりの集合のテストが期待どおりの文言で落ち、等価変異 2 通りだけが緑。 走査を直した後の新しい形の変異 6 通り（2026-09-13）も期待どおり（走査と、画面のテストがある箇所ではその画面のテストだけが落ちた）。⚠ **zsh は引用なしの変数を単語分割しない** — `git checkout <rev> -- $FILES` がパス全体を 1 つの名前として失敗し、2 回目の「緑」が修正後のコードに対する**無効な測定**になりかけた（配列 `"${FILES[@]}"` で渡す。`git diff --stat` で着弾を確かめる Bug #44 の作法で気づいた）。本番反映は `./deploy.sh`（`view:cache` 再生成。DB 変更なし） |
| 58 | テナントの区画を論理削除すると、その区画を参照する画面が壊れる。**投資**は一覧・詳細・**物件詳細（ページ丸ごと）が 500**。**修繕**は詳細・一覧・物件詳細で**「共用部」と誤表示**し、編集画面では区画の選択肢から今の区画が消えて先頭の「共用部」が選ばれるので、**どの項目を直して保存しても区画が共用部に書き換わる**。**問合せ**は希望区画が表示から**黙って消え**（全部消えると「未定」）、編集画面の選択済みからも抜けるので**保存すると中間テーブルからも消える**。**契約**は落ちないが削除済みと分からない。⚠ 本番では発火前だった（削除済み区画 7 件・参照は解約済み契約 1 件だけ。投資・修繕・問合せは 0 件。2026-09-13 に利用者の承認のうえ読み取りで確認）（2026-09-14 修正） | 区画は SoftDeletes なのに、削除の歯止めは「契約中の契約がある」だけ（`UnitController::destroy()`）で、投資・修繕・問合せが付いた区画も削除できる。一方、参照する側のリレーション（`Investment::unit` / `Repair::unit` / `Inquiry::units` / `UnitRentRevision::unit`）は `withTrashed()` を持たず（`Contract::unit` だけは付いていた）、論理削除の全域スコープで null（BelongsToMany は行ごと欠落）になっていた。編集画面の選択肢も削除済みを除いて組んでいたので、今の区画が選択肢に無く Alpine の `:selected` が一致しない。⚠ **`exists:units,id` は論理削除を見ない** — 登録で削除済みの区画を送ると入力チェックを素通りし、投資・修繕は `Unit::findOrFail` の 404、問合せは**黙って紐づく**。Buyer（Bug #12）・担当者（`User::assignableWith()`）で踏んだのと同じ型 | ①**子から区画を読むリレーション（belongsTo / belongsToMany）はすべて `withTrashed()`**。物件から区画を並べる `Property::units`（hasMany）には付けない（フロアマップ・区画数・入居率に混ざる）②表示は `Unit::display_label`（表示名 ＋ 削除済みなら「（削除済み）」）。**DB に保存せず毎回 `trashed()` で決める**（同じ表示名で登録し直すと `UnitController::store` が削除済みの行を復元するので、保存すると復元後も印が残る）。削除済みの区画には区画ページ（404）へのリンクを張らない ③編集画面の選択肢は `Unit::includingTrashed($keepIds)`（削除済みでない区画 ＋ そのレコードが今持っている区画。`withTrashed()` はクエリ全体の除外を外すので条件は括弧にくくり、問合せの「空室・商談中 OR 選択済み」とは AND でつなぐ）。⚠ **列を絞る `get([...])` に `deleted_at` を必ず含める** — Eloquent の厳格モードが無いので、取り忘れると `trashed()` が黙って false になり印が消える（変異で実測。問合せでは削除済みの区画に「商談中」まで付く）④入力チェック: 登録は `Rule::exists('units', 'id')->withoutTrashed()`、更新は `->where(fn ($q) => $q->whereNull('deleted_at')->orWhere('id', 今の区画))`（閉じた条件は `DatabasePresenceVerifier::addConditions` が括弧でくくる。括弧が無いと今の区画が一致してほかの削除済みも通るが、「ほかの削除済みを拒否する」テストが緑なので効いている）。所属チェックも `Unit::withTrashed()`。⚠ **問合せは所属チェックが削除済みを読まないと、「今の区画なら通す」入力チェックと組み合わさり、物件を変えた問合せに旧物件の区画が付く**（画面では物件を変えると選択が空になるので、古い画面や手で組んだ送信だけが通る経路）。回帰テスト `tests/Feature/Tenant/DeletedUnitReferenceTest.php`（38 本。画面・選択肢・画面のデータから組んだ保存の往復・拒否）。構造テストは `app/Models` で `Unit::class` を指すリレーションを**全件分類**する（belongsTo / belongsToMany は削除済みを読む・hasMany / hasOne は読まない・それ以外は分類できないとして落とす。**コメントを落としてから走査**し、拾えた数の下限つき。新しいリレーションは自動で検査対象に入る。Top trap #13）。⚠ **変異 44 通りを実測し、2 通りが緑のまま通った** — 投資・修繕の詳細で区画の欄から印を消しても、**同じ「B1A（削除済み）」が削除確認モーダルの対象にも出る**ので `assertSee` がそちらに当たる（Bug #43 / #46 と同型）→ 欄の見出しから辿って欄の中身を見る形に直して検出（当て直した 9 通りも期待どおり）。緑が正しい 3 通り（登録側の所属チェックは入力チェックが先に削除済みを弾くので等価 ／ 契約中タブは削除済み区画では到達しない ／ コメント中の `Unit::class` は走査が無視する）も確かめた。⚠ 契約の編集・賃料改定・解約の画面は、削除済み区画の契約（必ず解約済み）では開けない（どれも解約済みを詳細へ戻す）ので、表記の置き換えだけでテストはしない。⚠ 物件も同じ型が残っていた（投資・修繕・問合せ・区画の `property()` に `withTrashed()` が無く、物件の削除も契約中の契約しか止めない）→ 2026-09-14 に**削除を止める**方式で対応した（Bug #59。区画と違い画面側に `withTrashed()` は足さない）。区画の CSV 取込も、重複チェックが削除済みを見ないのに一意制約（物件＋表示名）は削除済みも含んでいた → 2026-09-14 に削除済みの同名区画を**復元して上書きする**方式で対応した（Bug #60）。本番反映は `./deploy.sh`（DB 変更なし） |
| 59 | テナントの**物件**を論理削除すると、その物件を参照する画面が壊れる。**投資・修繕（区画あり・共用部とも）・問合せの詳細、問合せの編集、区画の詳細・編集・賃料改定が 500**、区画の更新は保存前に 500・区画の削除は**削除が保存された後に** 500、顧客詳細はその顧客の問合せが削除した物件を指すとページ全体が 500（計 11 か所）。投資・修繕・問合せ・区画の一覧からは**黙って消える**。投資・修繕の編集画面は物件の選択肢に今の物件が無い。`exists:properties,id` は論理削除を見ないので、手組みの送信（画面を開いたあとの削除を含む）で削除済みの物件に投資・修繕・問合せ・契約を作れる。⚠ 本番では発火前（物件 17 件中 削除済み 1 件・関連データ 0 件。2026-09-14 に利用者の承認のうえ読み取りで確認）（2026-09-14 修正） | 物件の削除の歯止めが「契約中の契約がある」だけ（`Tenant\PropertyController::destroy()`）で、区画・解約済み契約・投資・修繕・問合せが残る物件も消せ、何も連鎖しない。子から物件へのリレーション（Unit / Investment / Repair / Inquiry / Transaction / PropertyChangeLog）は `withTrashed()` を持たない（Contract だけ持つ）ので、消した瞬間に `$x->property` が null になる。一覧は `whereHas('property')` が削除済みを除く。Bug #58（区画）と同じ型 | **区画とは別の方式: 削除を止める**（利用者の判断）。守る不変条件は「**削除済みの物件は、生きている区画・契約・投資・修繕・問合せを持たない**」。①`Property::deletionBlockers()` が `DELETION_BLOCKING_RELATIONS`（区画・契約（解約済みも含む）・投資・修繕・問合せ。論理削除済みは数えない）を数え、1 件でもあれば「この物件には区画 2 件・投資 1 件があるため削除できません。使わなくなった物件は、編集画面で稼働状態を「非稼働」にしてください。」（`DeletionBlockers::summarize()` と同じ形・「非稼働」は `OperationStatus::Inactive->label()`）②保存 7 か所（契約の登録・投資／修繕／問合せの登録と更新）の `property_id` を `Rule::exists('properties', 'id')->withoutTrashed()`。ほかの経路（区画の登録はルートで物件を受ける・CSV 取込 4 種は既定のスコープ・Ajax 2 本もルート・`restore()` は生きている物件の下だけ）から子は付かない。⚠ **子→物件のリレーションに `withTrashed()` を足さない**（Top trap #18 の区画の流儀と逆）— 不変条件のもとでは画面の 11 か所に到達せず、足すと `whereHas('property')` が削除済みの物件まで拾って一覧・ダッシュボードの意味が変わる。理由は `Property` の docblock に書いた。⚠ 数えない 3 種は理由つきで `DELETION_IGNORED_RELATIONS`（変更履歴＝物件ページでしか出ない・取引＝画面が無い・添付＝物件に添付する経路が無い）。⚠ ロックはかけない（子を書く側はロックなしで確かめるので `destroy` だけ固めても競合は閉じない。理由は `destroy()` のコメント。Bug #48 の末尾）。回帰テスト `tests/Feature/Tenant/PropertyDeletionGuardTest.php`（20 本）: 種類ごとに 1 本ずつ（Bug #44）／削除済みの子・数えない 3 種では止めない／描画された削除フォームの往復と**赤帯の要素の中**の文言（Bug #43 / #46 / #47 / #49）／保存 7 本／**構造は両側から全件分類**（物件の側: `Property.php` のリレーションがちょうど 1 つのリストに入り、実在しない名前が残らない。子の側: `Property::class` を指すモデル（`HsProperty` / `MsProperty` は後読みで除く）が分類済みのリレーションの相手に含まれる。拾えた数の下限を**先に**見る — 後ろに置くと空振りが「実在しない名前」という別の理由で報告される）。走査の部品は `tests/Concerns/ScansModelRelations.php` に切り出して区画の構造テストと共用。変異 26 通り（カナリア 1・対照 2 を含む）はすべて期待どおりの集合と文言。⚠ **対照の実測で注記を 1 つ直した** — 投資の入力チェックを外し、再現状態の区画を生きている物件の下に替えても、`property_id` を名指しで見るアサートは赤のままだった（区画の所属チェックが代わりに止めても落ちる）。削除済みの物件の下に生きている区画を残す再現状態が効くのは「何も書かれない」のアサートのほう（所属チェックで満たされてしまう。Bug #48 の型）。⚠ 変異の実行役がテストの出力の途中で切れた多バイト文字で落ち、**変異を作業ツリーに残した**（戻して空を確かめた）。実行役は復号を `errors='replace'` にし、戻しを `finally` で必ず行う形にした。本番反映は `./deploy.sh`（DB 変更なし） |
| 60 | テナントの**区画の CSV 取込**で、削除済みの区画と同じ区画（物件＋表示名）の行がプレビューでは「取込可能」と出るのに、確定すると一意制約 `idx_units_property_display` に当たり、`rollBack()` で**正しい行も含めて全行が消える**（画面には生の SQL エラーだけが出る）。同じ CSV の中の `(3, A)` と `(空欄, 3A)`（どちらも区画 3A）も重複チェックを素通りして同じ結果になる。契約・過去契約の取込は、削除済みの区画に「見つかりません。先に区画インポートを実行してください」と案内し、**案内どおり区画の取込を流すと上の理由で失敗する行き止まり**だった。⚠ 本番では発火前（一意制約は本番に実在。削除済み区画 7 件＝物件 12 の 2A 3A 4A 4B 5A 5B 5C。物件 12 の区画を取り込み直すと、この 7 つと同じ名前の 1 行で取込全体が失敗する。2026-09-14 に利用者の承認のうえ読み取りで確認）。⚠ **区画・契約の取込の確定はテストで一度も通っていなかった**（下記のスキーマの漂流）（2026-09-14 修正） | 重複チェック（`Admin\TenantImportController::executeUnit()`）が `Unit::where(物件, 表示名)`＝既定のスコープで**削除済みを除く**のに、一意制約は削除済みの行も含む。CSV 内の重複は表示名でなく生の「階\|号室」で比べていた。画面からの登録（`UnitController::store`）は削除済みの同名区画を復元するので、**画面と CSV で結果が違っていた**。さらにテスト用スキーマ（migration は SQLite のテストのための鏡。本番は raw SQL で管理）が本番から漂流しており、`units.usage_type_id` と契約の初月・最終月の 4 列（`initial_month_type` / `initial_month_amount` / `final_month_type` / `final_month_amount`。リポジトリに DDL が無く本番で直接作られたまま）が無かったので、確定の書き込みが `no column named …` で落ち、**確定の経路を誰もテストできなかった**（Bug #54 ⑤・#40 と同族） | 利用者の判断で 4 つ。①**区画の取込は削除済みの同名区画を復元して、CSV の行の内容で上書きする**（画面の登録と同じ）— 同名を `Unit::withTrashed()` で 1 回引き、生きている→今までどおりエラー行、削除済み→確定で `fill()` → `restore()`（1 回の UPDATE。同じ id のまま戻るので過去の契約・投資もその区画に戻る。CSV に無い `notes` は残る）。予告（「区画「2A」は削除済みです。取り込むと復元して、この行の内容で上書きします（…）」）は**その行を取り込むと決めた位置でだけ** `warnings` に積む（途中で積むと予告とエラーが同じ行に並ぶ）。要約「区画 N件を新規作成・M件を削除済みから復元」・完了「…（うち削除済みから復元 M件）」。復元する id はブラウザから受け取らず、確定でも検査をやり直してサーバで決める。復元した区画も `total_units` に数える ②**CSV 内の重複は表示名で比べる**ため、階の検査を重複チェックより先に置く。⚠ 順番を戻すと、地下を「B1」と書いた行で `Unit::generateDisplayName(?int)` が TypeError になりプレビューごと 500（変異 M12 で、元のテストでは緑のまま通ることを実測してからテストを足した）③**契約の取込は削除済みの区画に入れず**「削除済みです。使う場合は、区画の取込で同じ区画を取り込むか、区画の画面から登録し直すと復元されます」（本当に無い区画は今の文言のまま）④**過去契約の取込は削除済みの区画のまま紐づける**（復元すると取り壊した区画が「空室」として生き返り、フロアマップ・入居率に混ざる）。注意をプレビューに出し、画面では「5A（削除済み）」（Bug #58）。⑤テスト用スキーマに `units.usage_type_id` と契約の 4 列を migration で足した。⚠ `usage_type_id` に**外部キーは付けない** — SQLite は外部キーを足すとテーブルを作り直し、`units.status` の CHECK が黙って消える（本番の `fk_units_usage_type` と正本 `database/sql/add_usage_type_id_to_units.sql` は docblock に書いた）。契約の 4 列は反映前の読み取りで本番の定義を確かめて合わせた（`initial_month_type` は varchar(10) NOT NULL DEFAULT 'full'・ほかの 3 列は NULL 可。最初は NULL 可の enum で置いており、null を書く経路が本番だけで落ちる食い違いになっていた）。回帰テスト `tests/Feature/Admin/TenantUnitImportTest.php`（19 本。すべて「プレビュー → 描画された『インポート実行』フォームをそのまま確定」の往復。予告・注意は**役割（`viewData('warnings')`）と表示（`⚠ 行N: …` の行）を別々に**見る＝Bug #54 ④。スキーマのカナリア 5 本＝一意制約が削除済みも含む・`status` の CHECK が残る・列がある・初月の種類が NOT NULL DEFAULT 'full'）。往復のヘルパは `MansionImportTest` から `tests/Concerns/SubmitsImportPreview.php` へ切り出して共用（複製すると drift する）。変異 33 通り＋スキーマを揃えたあとの 3 通りすべて、期待どおりの集合のテストが期待どおりの文言で落ちた（初回は M12 だけ緑 → テストを足して赤）。⚠ **確定が差し戻しで落ちる変異は、テストの失敗文が「差し戻し先が違う」「success が null」までしか言わない**ので、使い捨ての探りでフラッシュの `error` を出し、一意制約違反（M01・M17）・行が見つからない（M19）・列が無い（M26）と理由まで確かめた（Bug #44 ⑤）。⚠ 範囲外: テスト用スキーマの `contracts.customer_id` / `rent_start_date` は NOT NULL だが、アプリはどちらも空のまま契約を作る経路を持つ（テナント名が空の契約の取込・賃料開始日が空の過去契約）。本番はどちらも NULL 可（反映前の読み取りで確認）＝テスト用スキーマのほうが漂流しており、その経路は確定までテストで通せない。テストは両方を埋めている。本番反映は `./deploy.sh`（DB 変更なし） |
| 61 | 保存された日時（登録・更新日時・最終ログイン・変更履歴・添付・ファイルの登録日）が**一日中 9 時間ずれて出る**（UTC の壁時計がそのまま画面に出る）。さらに「今日」「今月」「今年度」（画面の「〇年〇月〇日 時点」・フォームの既定の日付・自動の対応履歴の日付・問合せ番号の年・5/1 と 6/1 の年度の切り替え・工程表の今日・年齢・キャンペーン期間）が**日本時間の 0:00〜8:59 に前日**になる。⚠ **「今日」のずれは 9:00 以降には再現しない**（再現するのは 1 日のうち 9 時間だけ）ので、日中に画面を開いても分からない。最初のコミット以来ずっとこの状態で、2026-09-19 の決裁 段階1 の実ブラウザ確認（Task 16 の F5・F6）で発覚（2026-09-22〜23 修正） | `config/app.php` の `timezone` が `'UTC'` の直書き（最初のコミットから）。保存は UTC の壁時計で正しいのに**表示が変換していない**ので `->format()` がそのまま UTC を出す。同じく `now()` / `today()` / `date()` が返す**暦の値**（年・月・日）は UTC の日付なので、日本時間の 0:00〜8:59 は前日になる。Carbon の `->age` と `createFromFormat('Y-m')`（**無い「日」を今日から補う**）も同じ経路 | `App\Support\JapanTime` の 2 つだけに集約する（`format()` ＝ 保存された日時 → 日本時間の文字列 ／ `today()` ＝ 日本の今日）。**アプリの timezone・保存・ログは UTC のまま**（利用者の判断＝案 (a)。`tests/Feature/Ops/ScheduleTest.php::test_application_timezone_stays_utc` はそのまま）。置き換えた実測は 表示 **24 か所 / 13 ファイル**（ビュー 16・コントローラ 8）／「今日」の表示・既定値・保存 **33 行**（ビュー 19 ファイル 20 行・PHP 8 ファイル 13 行）／ 判定・集計 **31 か所 / 21 ファイル**。<br>⚠ **`today()` は日本の日付の「UTC の 0:00」を返す**（日本時間の 0:00 ＝ UTC では前日 15:00 で返さない）。返すと date キャストの属性・`Carbon::create()` / `createFromFormat('Y-m-d', …)` と 9 時間ずれ、`ZealFiscalYear::isFutureMonth()` が当月を来月と判定する（**変異 M03 で 13 本が赤**）。<br>⚠ **可変の `Carbon` を返す**（`now()` / `today()` のドロップイン置換のため。不変にすると `$x->startOfMonth()` のような**戻り値を捨てる書き方が無音の no-op** になる）。`CarbonImmutable` を要求する 2 つのサービスだけ `->toImmutable()` を付ける（落とすと TypeError）。<br>⚠ **`format()` を date キャストの列に既定の書式で当てない** — UTC の 0:00 が日本時間の 9:00 になり**存在しない時刻**が出る（実測 `2026/05/01 09:00`）。走査は仕様上 date キャストの `_at` を対象から外すので**原理的に止まらない**。日付だけの列は第 2 引数で `'Y/m/d'` を渡す。<br>⚠ **TIMESTAMP 列への保存・期限・運用は `now()` のまま。**<br>⚠ **振る舞いが変わるのは 2 か所**（計画は「唯一」と書いていたが実測で 2 つ）: ①`ZealPlan::isCampaignActive()` は**終了日の当日も適用中**になった（以前は終了日の 9:00 以降が対象外。開始も 9 時間早まる。本番の呼び出しは `zeal/plans/index.blade.php:103` の「適用中」バッジ **1 か所だけ**で、保存される金額には影響しない。`effectivePriceExcl()` は呼び出し 0 件の死にコード）②`ZealMember::age()` は**未来の誕生日（入力ミス）の 243 件**だけ旧実装と 1 違う（688,128 通りの掃引。全件「差がちょうど整数年」で、うち 24 件は Carbon が 2/29 → 3/1 を 1 年ちょうどと数えるもの。**過去の誕生日の食い違い 378 件はすべて日本時間 0:00〜8:59 の帯 ＝ この改修が直した時差そのもの**）。<br>守り手は走査テスト 3 本 — `StoredTimestampDisplayScanTest`（属性名を**モデルから機械的に集める**。date キャストの `_at` は外れる）／ `ClockReadScanTest`（**ビューの時計の読み取りは 0 件**・PHP は **11 件 / 8 ファイル**をファイルごとの件数と理由で全件分類）／ `JapanBusinessDayTest`（5 月始まりの年度の式を**全件分類**。実測 **8 か所**＋ ZEAL の 6 月始まり 1）。走査の規模は PHP **267 本** / Blade **249 本**（実測）。<br>⚠ **走査の死角**（docblock に列挙済み）: 表示は 変数に入れる ／ 配列の添字（実在: `Tenant/UnitController.php:529`。並び替えキーなので対象外）／ `{{ $x->created_at }}` の素出し ／ `->setTimezone()` `->copy()` `->addHours(9)` `->startOfDay()` を挟む形 → これらを捕まえるのは**件数の下限 `MIN_FORMAT_CALLS = 24` だけ**（**変異 M26 で実測**。死角の形に書き換えると走査本体は緑で下限だけが赤になる）。下げる前に「消した」のか「化けた」のかを確かめること。時計側の死角は 先頭 `\` 付きの呼び出し ／ `Carbon::parse('now')` ／ `new DateTime('+1 day')` ／ 一覧に無い判定。⚠ **ビューの注意書きは `{{-- --}}` に書く**（行中の `//` はあえて落とさない ＝ 落とすようにすると `<p>x</p> // {{ now() }}` の形で本物を隠す）。<br>⚠ **この改修で踏んだ「テストが緑でも守られていない」型が 6 つ**: ①**走査のコメント除去が文字列の中の `/*` から始まり実コードを飲み込む** — `request()->is('tenant/*')` 型の文字列が `/*` を作る（`sidebar.blade.php` に 14 個あり、うち 12 個は対になる `*/` を持たない。残り 2 個は `tenant/transactions/*/edit` の中で `*/` と重なる）ので、あとから `<style>` に CSS コメントを 1 つ足すだけで**実コード 196 行が走査の目から消える**（実測）。`(?<![\w\x27"])` の**後読みの否定**（negative lookbehind）で解消し、249 ビューで出力不変を確認（**変異 M27** で固定資産 2 本が赤 ＝ 修正が load-bearing）②**期待値を実装と同じ式で組み立てると同義反復になる** — `AreaBuildingImportTest` が調査年月の期待値を `JapanTime::today()->format('Y-m')` で作っていたため、ビューを `now()` に戻しても**日本時間 9:00 以降は緑**（赤になるのは 0:00〜8:59 の 9 時間だけ）。時刻を凍結して literal で見る形に直した ③**検出器の正規表現の枝は、サンプルも実出現も無いと「消しても緑」**（表示 12 種・時計 17 種が該当）→ 自己テストで全部の枝を通す ④**列挙リスト方式**（Bug #45 ①）— `MAY_FISCAL_YEAR_METHODS` を「5 つのコントローラ」と書いていたが実測 **8 か所**で、網の外の 3 つは**どのテストも実行していなかった** → 全件分類に直した ⑤**検出器がメソッド宣言に自己一致する** — `(?:now\|today)\s*\(` が `public static function today(): Carbon` を拾う（計画の ALLOWED `[1]` は必ず赤になった。実測 2 件。**検出器を弱めず件数で吸収**する）⑥**被覆の非対称** — 建売と注文住宅は逐語で同じ形なのに建売しかテストが無く、注文住宅の更新日時の行は**一度も実行されていなかった**（`@if($o->updatedBy)` の中で `updated_by` を入れるテストが 0 件）。<br>**変異 32 通りを実測**し、期待と違ったのは **M15 の 1 件だけ** — `timestampAttributes()` の SoftDeletes の分岐を消しても全件が緑で、調べると Laravel の `SoftDeletes::initializeSoftDeletes()` が `deleted_at` を `datetime` キャストへ足すので下の casts のループと**重なっている**（4 モデルで実測）＝ **真の等価変異**（次の測定で誤読されないよう 3 行に理由を書き添えた）。**意図して緑にした 3 通り**: 書式の第 2 引数 `'Y/m/d'` を落とす変異は走査が**形しか見ない**ので緑（同じ形でも振る舞いのテストがある建売側は赤 ＝ **守り手の有無がそのまま出た**）／ 引数 2 つの `date()` を足しても緑 ＝ 走査が**過剰に拾わない**ことの証明 ／ `CarbonImmutable::instance` → `Carbon::instance` は `Carbon::instance()` も clone するので等価。全表は `docs/superpowers/plans/2026-09-19-japan-time.md` の「実測記録」。本番反映は `./deploy.sh`（**DB 変更なし**） |
| 62 | 「前月」「来月」「過去 18 か月」が**月末のある日にだけ** 1 か月ずれる。① テナントダッシュボードのビル別カードが 3/31 に「**3月実績**」と出て、3 月の未確定の契約を前月として集計する（ラベルと数字が同じ誤りで揃うので画面から気づけない）② ZEAL ダッシュボードの先月比が 0 ③ 経営試算表の項目マスタの「適用開始月」の既定が 1 か月飛ぶ（3/31 に 2026-05。既定のまま保存すると 4 月の既存セルが自動反映から漏れる）④ ZEAL 体験予約一覧の月の絞り込みが 18 か月のうち重複 7・欠落 7。⚠ **起きるのは月の 29〜31 日だけ**（月の途中では再現しない）— 2026 年に溢れるのは前月が 7 日（3/29・3/30・3/31・5/31・7/31・10/31・12/31）、来月が 7 日（1/29・1/30・1/31・3/31・5/31・8/31・10/31）、18 か月の一覧が 29 日（全日を掃引）。テストも `view:cache` も緑（2026-09-23 修正） | Carbon の `subMonth()` / `addMonth()` / `subMonths($i)` は、移った先の月にその日が無いと**翌月へ溢れる**（3/31 の 1 か月前 = 2/31 → 3/3、1 か月後 = 4/31 → 5/1）。「今日」から直接足し引きしていた 5 行（4 ファイル）が該当: `DashboardController` のラベル（`tenant()`）と集計（`aggregateBuildingStats()`）・`Zeal\DashboardController` の `$lastMonth`・`Zeal\InquiryController` の月の選択肢・`admin/master/zeal-simulation-categories/_form.blade.php` の適用開始月。⚠ **Bug #61 との関係**（2026 年を 1 時間ごとに掃引して実測）: 「今日」を日本の暦日にした結果、前月側は 3/29・5/31・7/31・10/31・12/31 の 5 日で日本時間の丸一日溢れるようになった（それまでは 9:00〜23:59 の 15 時間。3/30・3/31 は前から終日）。一方で 7 つの月の 1 日（2/1・3/1・5/1・7/1・9/1・10/1・12/1）の 0:00〜8:59 の誤り（UTC の日付が前月末なので前々月を指していた。計 63 h。残る 5 つの月は前月末の溢れと打ち消し合っていた）は Bug #61 で消えていたので、前月の誤りの時間は年間で **186 h → 168 h → 今回 0 h** | **月初へ寄せてから足し引きする** — `JapanTime::today()->startOfMonth()->subMonth()`。並べるときは起点をループの外で 1 回作り、中で `$base->copy()->subMonths($i)`（前例: `Zeal\DashboardController` の月次グラフ・`ScheduleBoardService`・`ScheduleCardService`）。⚠ **順序が load-bearing** — `subMonth()->startOfMonth()` は溢れてから寄せるので直らない（3/31 → 3/3 → 3/1）。⚠ ビューへ渡す `$now` から求めるときは必ず `copy()` が先（`JapanTime::today()` は可変の Carbon。落とすと画面の「〇年〇月〇日 時点」が「2026年2月1日」になる。変異 M04 で実測）。⚠ 走査の実測: PHP（Carbon）の月の足し引きは `app/` `resources/` で **22 件**（JS の `setMonth` 7 件は別）で、直したのは 5 件。残り 17 件は起点が `startOfMonth()` か `Carbon::create(年, 月, 1)`（1 件ずつ起点まで遡った）。日の無い `createFromFormat('Y-m')`・`strtotime('+1 month')`・`P1M` はアプリに 0 件（シーダーの `strtotime('+6 months')` など 4 件は開発用）。⚠ **PHP（Carbon）の足し引きを新しく足したときに止める走査テストは無い** — 書くときは起点が月初かを確かめる（JS はビューの `.setMonth(` を 0 件に保つ。下の追記）。回帰テスト `tests/Feature/MonthEndOverflowTest.php`（4 本）: ⚠ **「今日」を 2026-03-31 に固定するのが load-bearing** — 正しい順序と誤った順序が同じ値になる日では、修正を戻しても緑のまま（3/15 は前月・来月とも、8/31 は前月だけ、7/31 は来月だけが同じ値）。各テストの先頭で「その日が前月・来月とも溢れる日か」をテスト自身が確かめる（`assertTodayOverflows()`。日付を 3/15 にすると前月側、7/31 にすると来月側の自己検査で 4 本とも落ちる。自己検査を外して 3/15 にすると #1 を戻してもテスト 1 は緑 ＝ 変異 M10 で実測）。⚠ ラベル・集計・実績期間（`buildProjectionLabels()`）という**同じ画面の 3 つの「前月」**を 1 本で見る（Bug #41）。⚠ 体験予約一覧は option の `value` とラベルが**別の式**なので両方見る（ラベルを日の無い `createFromFormat('Y-m', …)` に変えると 3/31 には 2 月が「2026年3月」と出る。この形は `ClockReadScanTest` も「ビューで時計を読んでいる」として止める）。⚠ 体験予約（`GymInquiry`）は外部 DB（`'zeal'` 接続・MySQL）を読むので、テストでは SQLite のメモリへ向け直し、ダッシュボードの件数が 0（null でない）ことで向け直しが効いたと確かめる（ダッシュボードは接続の失敗を握りつぶすので、見ないと手元の MySQL へ接続しに行っても緑）。変異は **17 通りすべて期待どおり**（カナリア 1・等価 1 を含む。全表は `docs/superpowers/plans/2026-09-23-month-end-overflow.md`）。⚠ **2026-09-24 追記 — 日付ピッカーの「1ヶ月前」（JS）も同じ形で直した**（7 か所の `setMonthAgo` が月だけを 1 つ戻しており、3/31 に押すと 3/3。ブラウザのローカル日なので Bug #61 の影響は無し）。利用者の決定で**前月の末日で止める**（Excel の EDATE と同じ）: `new Date(年, 月, 0).getDate()`（月は JS の 0 始まり＝`getMonth()` の値）で前月の末日を取り、`Math.min(今日の日, 末日)` で止める。回帰テスト `tests/Feature/DatePickerMonthAgoTest.php`: Blade のソースから `datePicker()` を波括弧の対応で切り出し、node の vm で実際に動かす（コピーは機械的に列挙・下限 7／リテラル 8 通り（初期値ありで開いても基準は今日）／2026〜2028 の全 1,096 日を月の日数の表と閏年の規則で求めた答えと突き合わせる）。ビューの `.setMonth(` / `.setUTCMonth(` の字面は 0 件に保つ（ラチェット）。⚠ **偽の Date は `new` なしの呼び出しで文字列を返すこと**（ブラウザと同じ。返さないと `new` を落とした変異が素通りした＝レビューで発見）。⚠ 検出器（定義・`.setMonth(`・Blade 構文・`</script`）には全部の枝を通す自己テストを置く（実データが通らない枝は消しても緑）。Blade はディレクティブを大文字小文字を区別せずに展開し（`@If(...)`）、コンポーネントのタグは `< x-` のように空白を挟んでも展開する（vendor の `ComponentTagCompiler`）。⚠ 死角（テストの docblock に列挙）: ソースから切り出すので `<script>` が描画・実行されるか（`type="text/template"`・`@if` の中・`@push` と `@stack` の対）は見えない／走査は `.setMonth(` の字面だけで、`new Date(y, m - 1, d)` のようにコンストラクタで月を足し引きする形は見えない。変異 29 通りすべて期待どおり（緑は死角の 2 通りと等価 1 通りだけ）。実ブラウザ（Playwright で時計を 2026-03-31 に固定）で 2 系統とも 2026/02/28 を確認。本番反映は `./deploy.sh`（DB 変更なし・新規 PHP クラスなし） |
| 63 | 決裁のみ利用者（`role = approval_only`）が**パスワードを変えると、決裁のホームに「決裁以外の画面は使えません。」が出て「パスワードを変更しました。」が出ない**（初回ログインで全員が踏む）。同じ形で、①パスワード変更の「キャンセル」（差し戻された直後など、直前の画面が無いとき）②ログインしたまま `/login` を開く（ログイン案内の QR・ブックマーク）③`/` を開く ④パンくずの「ホーム」でも、**行き先は正しいのに毎回警告が出た**。基幹を使う人も、パスワードを変えたあと着いた画面に完了の表示が出ていなかった。テストは全部緑（行き先の URL しか見ていなかった）（2026-09-19 の実ブラウザ確認で発見、2026-09-24 修正） | 行き先を `route('dashboard')` に固定していた。`/dashboard` はロールごとのダッシュボードへ転送するだけの「ホームの別名」だが、①決裁のみ利用者には門番（`RestrictApprovalOnlyUsers`。web グループ）が「決裁以外の画面」として跳ね返し、警告のフラッシュを付けて決裁のホームへ送る ②**フラッシュは次の 1 回の要求までしか残らない**ので、2 段の転送（302 → 302 → 200）では先に付けた「パスワードを変更しました。」が着いた画面では消えている（実測）。基幹の人は警告こそ出ないが、同じ 2 段の転送で完了の表示が消えていた | **行き先は `route($user->homeRouteName())`**（ログイン直後と同じ 1 箇所の規則。経営層 → `dashboard.executive`・管理者／一般担当 → `dashboard.tenant`・決裁のみ → `approvals.home`）。パスワード変更のあと・キャンセルの代わりの行き先・パンくずの「ホーム」をこれにした。`/` と `/dashboard`（ログイン中の `/login` は `RedirectIfAuthenticated` が `/dashboard` へ送る）は、門番が**警告なしで**決裁のホームへ送る（`isHomeAlias()`）。⚠ **通す（`ALLOWED_NAMES` に足す）のではない** — 行き先は今までどおり門番が決める。⚠ 別名の判定は **403 の分岐（Ajax・GET 以外）の後**に置く（前へ動かすと Ajax の GET `/dashboard` が 403 でなく 302 で HTML を読まされる）。⚠ **行き先の URL を見るだけのテストは、フラッシュが途中で消えるのを捕まえられない** — 転送をたどって**着いた画面**で文言を見る（`$this->followRedirects($response)`。`followingRedirects()` は次の要求 1 回にしか効かず、フォームを読む GET で使い切っていた）。⚠ **警告が出ない入口を全件分類で固定する** — 全 GET ルートを分類するループで「警告なしで送るのはちょうど `/` と `dashboard` の 2 本」と見る（転送先しか見ない分類では、別名の判定を `dashboard.executive` まで広げても緑だった。レビューで指摘）。回帰テスト: `PasswordChangeTest`（4 ロールの行き先・着いた画面の完了表示・キャンセルの代わりの行き先）／ `ApprovalOnlyLockoutTest`（ホームの別名 3 入口・全件分類・Ajax の 403 を `/dashboard/tenant` `/dashboard` `/` で）／ `LayoutBreadcrumbHomeTest`（描画された href をたどって 200）。変異 8 通り（計画書 `docs/superpowers/plans/2026-09-24-approval-phase1-fixes.md` の M01〜M05d）はすべて狙ったテストが狙った文言で落ちた。本番反映は `./deploy.sh` |
| 64 | 「元の画面へ戻る」「キャンセル」や、入力を断られて差し戻された先が **405 Method Not Allowed** になる（本番の `APP_DEBUG=false` では英語の素のエラー画面）。①決裁の CSV 取込を確定したあとのログイン案内の「元の画面へ戻る」（通常の操作で必ず踏む）②確認画面からアップロードし直して断られたとき（ファイルの選び忘れ・xlsx・見出しだけ・行数の上限超え。確認画面は「CSV を直して、もう一度アップロードしてください」と案内し、ファイル欄は空なので普通に踏む）③確定の送信が断られたとき ④確認画面のヘッダーからパスワード変更を開いて「キャンセル」。テストは全部緑（2026-09-19 の実ブラウザ確認とレビューで発見、2026-09-24 修正） | `url()->previous()`（`back()` と、入力チェックで断られたときの既定の戻り先も同じ）は、セッションに記録した直前の URL より **Referer ヘッダーを優先**する。CSV 取込の確認画面は **POST の応答**で、その URL は POST 専用（`…/users/import/preview`）。そこに載ったフォームやリンクから先へ進むと Referer が POST 専用の URL になり、戻るとき GET で 405。⚠ **テストの HTTP クライアントは Referer を送らない**（`from()` を付けたときだけ送る）ので、原理的に見えなかった | **戻り先は入口が決めて渡し、ビューで `url()->previous()` を呼ばない。** ログイン案内（`App\Support\Approval\LoginGuide`）は `$backUrl` を必須にし、フォームが GET の画面に載っている入口（基幹の新規登録・基幹の再発行・決裁の再発行・まとめて再発行）はリファラー（＝絞り込み・ページ番号が残る）、CSV の確定は取込の画面を渡す。CSV 取込の断り（入力チェックと `CsvImportException`）は取込の画面に固定する（`try { $request->validate([...]); } catch (ValidationException $e) { throw $e->redirectTo(route('…import')); }`。`validate([` の形が残るので `JapaneseValidationMessagesTest` の走査もそのまま通る）。パスワード変更の「キャンセル」は `session()->previousUrl()`（GET かつ Ajax でない要求だけが記録される＝ POST 専用の URL にならない。Referer を見ない）で、それが無いかこの画面自身ならその人のホーム（Bug #63）。⚠ テストは **`from(<フォームが載っていた画面の URL>)`** で送る（`from()` は Referer とセッションの直前の URL の両方を設定する。Referer だけを確かめたいときは `withHeader('referer', …)`）。描画されたリンクを取り出して**実際に GET して 200** まで見る（`tests/Concerns/ReadsLoginGuide.php`。Bug #47 の往復）。⚠ **基幹の取込にも同じ形があった**（2026-09-24 に実測して直した。計画書 `docs/superpowers/plans/2026-09-24-import-preview-back.md`）。確認画面から送る経路の `back()` と try で包まない入力チェックが、テナント（5 タブ）・賃貸マンション（6 タブ）・ZEAL 会員・工程表の 4 本で 405 になっていた（確認画面から送り直して断られる・確定の書き込みに失敗する・hidden を書き換えた確定。工程表のガント形式の選び間違いは普通の操作で踏む）。戻り先は取込の画面（タブがあればそのタブ）に固定した。⚠ テナント・賃貸マンションの確認画面は**開いているタブの枠だけ**が確認に置き換わり、ほかのタブのアップロードのフォームは残る＝実際の画面で踏むのは「確認画面から**別のタブ**でアップロードし直して断られる」経路（実ブラウザで、リファラーが確認画面の URL になることまで確かめた）。テストもこの形で、物件タブの確認画面に載っている区画（部屋）タブのフォームを分解して送り、**送ったタブ**へ戻ることを見る（同じタブの URL から同じタブへ送る形では、タブを既定やリファラーから決める誤りを区別できない）。⚠ 計画に無かった経路: ZEAL 会員の確定の送信から hidden の `confirmed` が抜けると、確定なのにファイル必須の入力チェックに落ち、その既定の戻り先も確認画面の URL になる。顧客（確認画面の URL が GET の取込画面と同じ `/admin/customers/import`）と周辺ビル（確認は画面の中の SheetJS で、送信元は GET の取込画面）は実測 200 で直していない。走査テスト `ImportControllerReturnPathScanTest` が `*ImportController.php` を全件分類する（許すのはこの 2 本だけ・件数と理由つき。コメントを落としてから `back(`・`->previous(`・`referer`・`redirect()->refresh(`・`$request->url(` などを数える）。⚠ **走査に見えない形がある** — try で包まない入力チェック（`validate()`・`Validator::make()->validate()`・`ValidationException::withMessages()`。既定の戻り先がリファラー）は字面に `back(` が現れないので、挙動のテスト（`from(確認画面の URL)` で送る）が守る: `TenantImportRejectionTest` / `MansionImportRejectionTest` / `ZealMemberImportControllerTest` / `ScheduleImportTest`（新しい取込には守り手がいない）。トレイトやサービスへ切り出した `back()` も見えない。⚠ **断りの文言が画面の決まった文にも出ていると、理由の帯が消えても緑になる**（Bug #43 の型）— 工程表の「「一覧」形式で書き出したファイルを選んでください」はアップロード欄の案内文にも常に出ており、文言を差し替える変異で 5 ケースとも緑だった（`ImportValidationFeedbackTest` の `/<li>[^<]*(ファイル\|csv)[^<]*<\/li>/ui` も案内文の `<li>CSVの「物件名」…</li>` に一致していた）→ 期待する文言を `<li>` 込みの全文にした。⚠ 確定時の書き込みの失敗は **2 行目**で起こす — テンプレートの見本が 1 行だけだと最初の INSERT より前に落ち、`DB::rollBack()` を消しても 0 件のまま緑だった。変異は 1 回目 36 通り＋レビュー後 19 通り（どちらもカナリアを含む）がすべて期待どおり（計画書の実測記録）。回帰テスト: `ApprovalUserImportTest`（確定後の案内の往復・送り直して断られる 4 通り・確定の送信が壊れている）／ `ApprovalUserManagementTest` `UserManagementApprovalTest`（4 入口の往復）／ `PasswordChangeTest::test_the_cancel_link_ignores_a_post_only_referer`。変異 11 通り（同じ計画書の M03b・M03c・M06〜M09e）はすべて狙ったテストが狙った文言で落ちた（M09c・M09d は送り直しの 2 ケースのうち、その経路を通る 1 ケースだけが落ちる＝経路ごとに守られている）。本番反映は `./deploy.sh` |
| 65 | 決裁の利用者の管理で、絞り込みのプルダウンを変えて（変えた瞬間に送る）からブラウザの「戻る」で戻ると、**プルダウンは変えた後の値なのに、表と「絞り込んだ全員（N 人）を再発行」が運ぶ条件は前のまま**。表示を信じて押すと、見えている条件と違う人たちが対象になる（確認のモーダルに氏名は出る）。テストも HTML も正しい（2026-09-24、レビューの指摘を実ブラウザで再現して修正） | ブラウザは「戻る」で前の画面の**入力欄の値を戻す**。経路は 2 つ: ①bfcache（画面をまるごと記憶から戻す。`pageshow` の `event.persisted` が true）②読み込み直したうえで入力欄の値を戻す（`persisted` は false・`performance.getEntriesByType('navigation')[0].type` が `back_forward`）。サーバーが描いた hidden と表は戻らず、利用者が変えた select と checkbox だけが戻る。実測（Playwright）: 区分を「決裁のみ」にして戻ると select は `approval`・hidden は空・表は 7 行。このとき `window` に置いた目印は消えていた＝②の経路（Playwright の Chromium・アプリ内のブラウザとも bfcache を使わなかった） | `window.addEventListener('pageshow', function () { document.getElementById('filter-form').reset(); });`。`reset()` はサーバーが描いた `selected` / `checked` / `value`（＝適用済みの条件）へ戻す。`pageshow` は `load` の後に来るので、②で戻された値はもう入っている。⚠ **`event.persisted` で絞らない** — 絞ると①しか直らず、自動操作のブラウザで再現した②が残る。⚠ `load` では待たない（①では `load` が来ない）・`document` では待たない（`pageshow` は `window` にだけ届く）。⚠ ほかの一覧の即時フィルタでも「戻る」で select と表がずれるが見た目だけ。**適用済みの条件を hidden で運ぶ一括操作がある画面**では対象が食い違うので必須（CLAUDE.md の即時フィルタの注記）。回帰テスト `ApprovalUserManagementTest::test_the_filter_form_is_reset_whenever_the_page_is_shown`（実行される script の中の `pageshow` のリスナーがちょうど 1 つ・本体で絞り込みのフォームを `reset()`・`persisted` を見ない）。⚠ **構造しか見られない** — 実際に戻して揃うかはブラウザで測る（Playwright で「区分を変えて戻る」「部門とチェックを変えて 2 回戻る」「進む」、アプリ内のブラウザで「戻る」、合成した `pageshow`（`persisted: true`）で①の経路）。変異 7 通り（同じ計画書の M24〜M24g。`persisted` に絞る・`load` や `document` で待つ・パース中に 1 回だけ戻す・script を実行されない型にする を含む）はすべて検出。本番反映は `./deploy.sh` |

## Postal Code APIs

- Forward lookup (zip → address): zipcloud API (frontend JS direct call)
- Reverse lookup (address → zip): HeartRails GeoAPI `getTowns` (server-side cURL, `reverseZipLookup` method)

## Google Maps

- Used for 仕入れ案件 and 分譲地PJ (geocoding + draggable/clickable pin)
- DB columns: `latitude`/`longitude` as `DECIMAL(10,7)`
- API key in `.env` as `GOOGLE_MAPS_API_KEY`

## Full-width → Half-width Numeric Conversion

Global listener in `resources/views/layouts/app.blade.php` automatically converts full-width digits (`０-９`) to half-width (`0-9`) on every numeric input across the entire application (all 185 routes inherit via single layout).

**Target elements:** `input[inputmode="numeric"]` or `input[type="number"]`

**Mechanism:**
- `document.addEventListener('input', fn, true)` — capture phase, runs before Alpine sync
- Preserves caret position via `selectionStart` / `selectionEnd`
- Digit transliteration only — no other character stripping

**Effect:** User types `１０００００` → immediately becomes `100000`. Alpine `x-model` values are always half-width, so downstream calculations work without per-field normalization.

**Scope:** Single injection point — no per-form wiring needed. Applies automatically to any new form added to the app.

## Excel Import (Client-side SheetJS → Server-side PhpSpreadsheet)

Mockup pattern for Excel cost-breakdown upload (DAD 工事案件 `projects/create.html`):

- Library: SheetJS (`cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js`) — `cdn.jsdelivr.net` only
- Flow: File → Sheet/Header selection → Column mapping (auto-guess from header text) → Preview with warnings → Commit (append to target array)
- Category alias normalization: `材料/資材 → 材料費`, `外注/下請 → 外注費`, `労務 → 人件費`, `機械/重機 → 機械経費`, `経費 → 諸経費`
- Amount normalization: full-width digits, comma/space/円/¥ stripped; non-numeric flagged
- UI: inline expansion inside the target card (not modal), 3-step indicator
- Alpine caveat: `<option>` list MUST be static — `<template x-for>` renders after `x-model` syncs and causes select value mismatch (see Bug #16)

Server-side swap path: replace `commitImport()` with `<form>` submit to `ProjectImportController@preview` + `@execute` using PhpSpreadsheet. UI panel HTML stays unchanged.

## Fiscal Year Calculation

```php
$month = now()->month;
$year = now()->year;
$fiscalYear = $month >= 5 ? $year : $year - 1;
$start = "{$fiscalYear}-05-01";
$end = ($fiscalYear + 1) . "-04-30";
```

## Development Workflow

1. Requirements definition with structured Q&A — lock all decisions before coding
2. Design mock (HTML) creation and review — explicit approval required
3. Implementation — check against all rules in this document
4. 30-point quality audit before delivery
5. Package as handoff ZIP with updated HANDOFF_PROMPT.md
6. Deploy: file placement → SQL execution → cache clear → browser verification
