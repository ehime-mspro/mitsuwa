# 周辺ビル調査の並び替え ＋ 並び替え見出しの視認性 — 設計書

作成日: 2026-08-28
対象: `/tenant/area-buildings`（周辺ビル調査）＋ 並び替え見出しを持つ全 3 一覧
モック: @docs/mockups/tenant/sortable-header-affordance.html（ユーザー承認済み・案A ＋ 案C のバー）
前提となる設計書: @docs/superpowers/specs/2026-08-25-tenant-list-sorting-design.md（以下「前設計書」）

---

## 1. 背景と依頼

利用者からの依頼は 2 つ:

> テナント管理の「周辺ビル調査」のテーブルを並び替えできるようにして下さい。
> 「部屋一覧」のテーブルと同様にして下さい。
> 並び替えしたい項目は「総階数、営業、空き、入居率、最終調査」です。

> 前回もテナント管理のテーブルを並び替えできるように改修しましたが
> 「並び替えができる列がわかりにくい」です。

追って、**初期表示の並び順**についても依頼があった（2026-08-28 追加）:

> 地図から位置を追加するときに、追加するビルの一覧が左側に出ますが並び順がバラバラなので
> 「ビル名を降順」にして表示して下さい。
> あと表で表示する場合も「ビル名を降順」で初期表示するように変更して下さい。

⚠ **この「降順」は確認したところ「昇順」だった。** 名前の一覧としては昇順が一般的なので
念のため両方の実例（`雅 → 相原 → …` と `三番町 → 久万ノ台 → …`）を出して尋ねたところ、
**昇順**との回答。**確認せず字面どおり作っていたら 187 棟が全部逆順になっていた。**

**依頼①は前設計書の規約をそのまま 3 画面目へ広げる話**で、新しい流儀は要らない。
**依頼②は前回作った意匠そのものの欠陥**で、共通部品 `x-sortable-th` を直すため
**物件一覧・部屋一覧・周辺ビル調査の 3 画面すべてに同時に効く**。

対象列は確認のうえ **7 列**に決めた（§4.1）。依頼の 5 列に「空室率」「不明」を足している。
「営業・空きは押せるのに不明だけ押せない」「入居率は押せるのに真隣の空室率は押せない」が
画面で説明できないため。

---

## 2. 調査で分かった事実（2026-08-28 実測）

### 2.1 周辺ビル調査の一覧はすでに PHP で全件並べ替えている

`AreaBuildingListService::rows()` は **SQL で最新調査回を引き → PHP で率を計算 →
PHP で絞り込み → PHP で並べ替え → `paginateRows()` でページを切る**という構造で、
前設計書 §3.2 が物件一覧に選んだ方式と同じ。**並び替えの追加は `rows()` に分岐を 1 つ足すだけ**で、
SQL は 1 行も触らない。

- **現在の**既定順は `sortByDesc([$row['rate'] === null ? 0 : 1, $row['rate'] ?? 0.0])`
  ＝ **空室率の降順・未調査は末尾**。同点は `baseQuery()` の `ORDER BY name, id` が
  安定ソートで残る（サービスの docblock に明記済み）
  → ⚠ **本設計でこの行を削除し、既定順をビル名の昇順に変える**（§4.4）
- 全件をメモリに載せる件は計測済みで **数万棟まで問題ない**（同 docblock の表。実運用は 187 棟）
- `rows()` の呼び出し元は `AreaBuildingController::index()` **1 箇所だけ**（実測）
  ＝ シグネチャを変えても波及しない

### 2.2 「—」になる条件は列ごとに 2 種類ある

| 列 | 値の出どころ | `null` の画面表示 |
|---|---|---|
| 総階数 | `area_buildings.total_floors`（`INT NULL`） | `—`（`totalFloorsLabel()`） |
| 営業 / 空き / 不明 | `$row['operating'] / ['vacant'] / ['unknown']` | `—`（ビューの `?? '—'`） |
| 入居率 / 空室率 | `$row['rate']`（`VacancyRate::percent()`） | `—`（`label()` / `occupancyLabel()`） |
| 最終調査 | `$row['month']` | `—` |

⚠ **「調査回はあるが総区画が 0」という行がありうる。** `VacancyRate::percent()` は
総数 0 で `null` を返すので、そのとき **`rate` だけ `null`** になり、
`operating` / `vacant` / `unknown` は **`0`**（画面も「0」）、`month` は実在する日付のまま。
つまり同じ行が「入居率・空室率では末尾／営業・空き・不明・最終調査では普通に並ぶ」。
これは前設計書 §4.4 の原則（**揃える相手は NULL ではなく画面の表示**）どおりで正しい。
§8 でテストに固定する。

⚠ 総階数だけは調査回と無関係に `null` になりうる（Bug #55 の取込で総階数が空のまま入った経緯がある）。

### 2.3 現状の矢印はコントラストが 1.41:1 しかない（ブラウザで実測）

見出しの背景 `#F9FAFB`（gray-50）に対して:

| 色 | 用途 | コントラスト比 | 判定 |
|---|---|---|---|
| `#D1D5DB` gray-300 | **現状**の未使用 ⇅ | **1.41:1** | UI 部品の要件 3:1 を大きく下回る |
| `#9CA3AF` gray-400 | — | 2.43:1 | まだ不足 |
| `#6B7280` gray-500 | **案A** の未使用 ⇅ | **4.63:1** | 満たす |
| `#059669` emerald-600 | 並び替え中の矢印（現状のまま） | 3.61:1 | 満たす |

**「分かりにくい」の実体はこれ。** 手掛かりは他に「ホバーすると背景が薄く変わる」しか無く、
**マウスを乗せるまで押せると気づけない**。既定順のときはどの列にも印が出ないので、
**今どう並んでいるかも画面のどこにも出ていない**。

### 2.4 共通部品の現状

- `x-sortable-th` を使うビューは **2 本**（`tenant/properties/index` / `tenant/units/index`）
- `x-sort-hidden` も同じ 2 本
- `ListSort` を使うのは `UnitController` / `PropertyController` / `Unit`
- `resources/css/app.css` に **`.sortable-th-link:focus-visible` が既にある**（84-93 行）
  ＝ この部品用の CSS の置き場所は決まっている
- 両コントローラに `private const SORTABLE` があるが **形が違う**
  （`UnitController` は `[SQL 式, 「—」を末尾へ]`、`PropertyController` は属性名の文字列）

### 2.5 ビル名で並べるための材料（2026-08-28 追加分）

- `area_buildings` の列は `name VARCHAR(255) NOT NULL` のみで、**読みがな列は無い**。
  照合順序は `utf8mb4_unicode_ci`（テーブル定義で確認）
- **`baseQuery()` は既に `ORDER BY area_buildings.name, area_buildings.id` で引いている。**
  `rows()` の `map()` / `filter()` は順序を保つので、
  **PHP 側の `sortByDesc()` を外すだけでビル名の昇順になる**（新しい並べ替えを書かない）
- `mapUnlocated()` は `$rows` の順をそのまま使う（`reject` → `map` → `values`）
- `_map.blade.php` の `ensureInLocateList()` は復帰した棟を
  **`AREA_MAP_UNLOCATED` の末尾に `push` する**（576-588 行）。
  `data-locate-index` と `onclick` の引数が添字に依存しているため、途中に差し込むと全行の振り直しが要る
- 既存テストの `sortedJa()` は `usort($names, 'strcmp')` ＝ **コードポイント順**

### 2.6 テストヘルパの制約

`tests/Concerns/ParsesSortLinks.php::sortLinkFor()` は

```
/<a\b[^>]*\bhref="([^"]*)"[^>]*>\s*ラベル\s*</u
```

＝ **`<a>` の直後にラベルのテキストが来ること**を要求する。
案A の点線下線はラベルだけに掛けたいので `<span>` で包む必要があり、この正規表現は 1 トークン広げる（§7）。

⚠ 「`<a>` 自身に `text-decoration` を掛けて `<span>` を足さない」案は**採らない**。
`<a>` は flex コンテナで、矢印は flex アイテム。**装飾が flex アイテムへ伝播して
矢印の下にも線が出るかどうかがブラウザ挙動に依存**し、しかも
`getComputedStyle` は伝播した装飾を子孫に対して必ず `none` と報告するため
**DOM 実測では決着しない**（2026-08-28 に確認）。決められないものに賭けない。

---

## 3. 方式の決定

| 論点 | 決定 | 理由 |
|---|---|---|
| 並び替えの実行場所 | `AreaBuildingListService::rows()` の中（PHP） | 既にそこで全件並べ替えている。SQL に写すと率の二重実装になる（Bug #41） |
| URL / 3 状態 / hidden / ページャ | **前設計書のまま流用** | 3 画面目で流儀を変えない |
| 意匠 | **案A ＋ 案C のバー**（見出しの塗り分けはしない） | モックで実物を比較して利用者が決定 |
| ラベルの持ち方 | **画面ごとに 1 つのマップへ統合** | 見出しとバーで別々に持つと食い違う（Bug #41 / #46） |
| 既定の並び順 | **ビル名の昇順**（従来の空室率降順は並び替え列として残す） | 利用者の依頼。`baseQuery()` が既に名前順なので**並べ替えを 1 行も書かずに実現できる**（§4.4） |
| 未登録リストの順 | **常にビル名の昇順で固定**（表の並び替えに追従しない） | 187 棟を上から順に登録する作業なので、順番が勝手に変わらないことが要る |

---

## 4. 設計① 周辺ビル調査の並び替え

### 4.1 対象列と URL

```
/tenant/area-buildings?sort=occupancy&dir=desc
/tenant/area-buildings?sort=month&dir=asc&occupancy=under75&year=2026
```

| `sort` の値 | 列 | 降順（1 回目）の言い方 | 昇順（2 回目） |
|---|---|---|---|
| `floors` | 総階数 | 多い順 | 少ない順 |
| `operating` | 営業 | 多い順 | 少ない順 |
| `vacant` | 空き | 多い順 | 少ない順 |
| `unknown` | 不明 | 多い順 | 少ない順 |
| `occupancy` | 入居率 | 高い順 | 低い順 |
| `vacancy` | 空室率 | 高い順 | 低い順 |
| `month` | 最終調査 | 新しい順 | 古い順 |

並び替え**しない**列: ビル名 / 位置 / 操作。

⚠ 「多い / 少ない」「高い / 低い」「新しい / 古い」を**列ごとに持つ**。
率に「大きい順」、日付に「多い順」は日本語として不自然で、バー（§6）にそのまま出る。

### 4.2 「—」は昇順でも降順でも末尾

前設計書 §4.3-2 の規約どおり。§2.2 のとおり **7 列すべて画面表示が `—`** なので、
判定は「その列の並べ替えキーが `null` か」で一致する。

実装は**フラグ付きの複合キーではなく分割**で書く:

```php
[$known, $blank] = $rows->partition(fn (array $r) => $this->sortValue($r, $sort->key) !== null);

$rows = ($sort->isAscending()
        ? $known->sortBy(fn (array $r) => $this->sortValue($r, $sort->key))
        : $known->sortByDesc(fn (array $r) => $this->sortValue($r, $sort->key)))
    ->concat($blank)
    ->values();
```

⚠ `[null 判定, 値]` の複合キーを `sortBy` / `sortByDesc` に渡す形は
**向きによって null フラグの 0/1 を反転させないと末尾に行かない**（降順では
「値あり」を 1、昇順では 0 にする必要がある）。**書いた本人しか気づけない取り違え**なので、
分割して連結する形にする。読んだだけで正しいと分かることを優先する。

### 4.3 入居率は空室率の符号を反転して並べる（値を 2 本持たない）

```php
'occupancy' => $row['rate'] === null ? null : -$row['rate'],
'vacancy'   => $row['rate'],
```

⚠ **`VacancyRate::occupancyPercent()` を並べ替えに使わない。**
入居率 = 100 − 空室率 なので順序は **厳密に一致**する（`VacancyRate` が
1/10% 単位の同じ整数から両方を作っており、和が 100.0% になることは構造で保証されている）。
別々に計算して比べる形にすると、**画面に並ぶ 2 つの数字と並び順が食い違う余地**を自分で作る（Bug #46）。

### 4.4 既定順を「ビル名の昇順」へ変える

⚠ **これは前設計書 §4.3-1（既定順は現状のまま）からの意図的な逸脱で、利用者の明示の依頼による。**
現在の既定順（空室率の降順）は、位置を登録していく作業から見ると「バラバラ」に映る。

- 実装は **`rows()` の `sortByDesc(...)` を外すだけ。** §2.5 のとおり `baseQuery()` が既に
  `ORDER BY area_buildings.name, area_buildings.id` で引いており、`map()` / `filter()` は
  順序を保つので、**新しい並べ替えを 1 行も書かずにビル名の昇順になる**
- **従来の既定順は失われない。** 空室率は §4.1 で並び替えできる列にしたので、
  見出しを 1 回押せば「空室率が高い順」が戻る
- 同名の行は `id` の昇順（`baseQuery()` のまま）

⚠ **漢字は「読み」ではなく文字コード順に並ぶ。** `area_buildings` に読みがな列は無く、
`utf8mb4_unicode_ci` は CJK に実質コードポイント順の重みを与えるため、
`三 → 久 → 千 → 大 → 武 → 湊 → 相 → 雅` のような並びになる。
**あいうえお順にするには読みがな列の追加が要る**＝本件の対象外（§10）。利用者へは説明済み。

⚠ **テストは SQLite（BINARY 照合）で走る。** 漢字・かな主体の名前なら MySQL の
`utf8mb4_unicode_ci` と同じ順になるが、**大文字小文字だけが違う名前や、
ひらがな／カタカナだけが違う名前をテストデータに使わないこと**
（MySQL は同一視し、SQLite は区別するので**本番とテストで順が変わる**）。
既存の `sortedJa()` は `strcmp` ＝ コードポイント順なので、この前提のもとで一致する。

### 4.4.1 同点の扱い（並び替え中）

- 並び替えは**既定順に並べ終えた列に対して**掛ける。PHP 8 のソートは安定なので、
  **同点の行は既定順＝ビル名順のまま残る**（前設計書 §4.3-3。これが無いとページをまたいで
  行が重複／消失する）
- 日付は `Carbon` のまま比べず **`format('Y-m-d')` の文字列**にしてから並べる（辞書順で一致し、
  比較の意味がオブジェクトの実装に依存しない）

### 4.5 フィルタ・ページャ・地図タブ

- フィルタとの共存・`page` を落とす・「クリア」は全部初期化 … **前設計書 §4.3 のまま**
- ページャは既存の `paginateRows()` がそのまま効く（`array_map(fn ($v) => $v ?? '', ...)` 済み。Bug #31）
- フィルターフォームに `<x-sort-hidden :sort="$sort" />` を足す（並び替え中にフィルタを変えても消えないように）

**地図タブ（`?view=map`）の扱い:**

- **未登録のビル一覧は常にビル名の昇順で固定する**（利用者の選択）。
  `mapUnlocated()` が `$rows` の順をそのまま使うのをやめ、**名前で並べ直してから**配列にする。
  187 棟を上から順に登録していく作業なので、**表で何をしていても順番が変わらない**ことが要る
- 並び順（`?sort` / `?dir`）は**タブを切り替えても持ち回す**（既存のタブリンクは
  `request()->except(['view','page'])` なので何もしなくてよい）。表へ戻ったとき並び順が残る
- 未登録リストが固定され、ピンの描画順は見た目に影響しないので、
  **並び替えは地図タブの見た目を一切変えない** ＝ **バー（§6）は表タブにだけ出す**

⚠ **既知の例外を 1 つ記録する。** 登録モード中に「位置を消す」を押した棟は、
`ensureInLocateList()` が `AREA_MAP_UNLOCATED` の**末尾に `push` する**ので（§2.5）、
その 1 行だけ名前順から外れて最下部に出る。名前順の位置へ差し込むには
`data-locate-index` と `onclick` の引数を全行で振り直す必要があり、
**得られる利益に対して壊す面積が大きい**ので意図してそのままにする
（消す操作は稀で、消した直後はその棟に留まる仕様＝すぐ置き直すのが通常の流れ）。
⚠ **「気づかなかった」と「決めた」を区別できるよう、ここに書いて残す。**

---

## 5. 設計② 見出しの視認性（案A）

`x-sortable-th` と `app.css` だけで完結する。**3 画面に同時に効く。**

| | 現状 | 変更後 |
|---|---|---|
| 未使用の ⇅ | `#D1D5DB`（1.41:1） | **`#6B7280`**（4.63:1） |
| 並び替え中の矢印 | `#059669` | 変えない |
| ラベル | 装飾なし | **点線の下線**（`underline dotted #9CA3AF` / `text-underline-offset: 4px`） |
| ホバー | 背景 `gray-100` | 背景はそのまま ＋ **下線が `#4B5563` に濃くなる** |
| 並び替え中 | 文字 `#047857` | そのまま ＋ **下線が `#059669`** |

- ラベルは `<span class="sortable-th-label">` で包む（§2.6）
- 色の状態変化は **`app.css` の `.sortable-th-link` 配下**に書く。
  部品の docblock が警告しているとおり、`color` は inline style なので
  `hover:text-*` の Tailwind クラスは効かない（inline が勝つ）。**下線の色は CSS に置けば効く**
- **`<th>` の当たり判定は今までどおりセル全体**（padding は `<a>` 側のまま。Bug #43）

⚠ 点線下線そのものは `#9CA3AF` で **2.43:1** ＝ 3:1 に届かない。
これは承知のうえで、**手掛かりの本体は 4.63:1 の矢印**、下線は「文字がリンクである」ことを
補強する装飾として扱う。濃くすると 10 列の見出しが騒がしくなるため、モックで選ばれた濃さを採る。

---

## 6. 設計③ 現在の並び順バー（案C のバー）

表の**すぐ上**（フィルターバー・タブより下）に 1 行足す。新しい部品 `x-sort-bar`。

```
並び替え: 既定（ビル名順）              ⓘ 見出しをクリックすると並び替えできます
並び替え: 入居率 高い順 ▼   [解除]      ⓘ 見出しをクリックすると並び替えできます
```

| 状態 | 出るもの |
|---|---|
| 並び替えなし | グレーのピル「並び替え: 既定（〜順）」＋ ヒント文 |
| 並び替え中 | 緑のピル「並び替え: 〈列〉〈向きの言い方〉〈矢印〉」＋ **解除** ＋ ヒント文 |
| 地図タブ（周辺ビルのみ） | **出さない**（§4.5 のとおり並び替えが地図の見た目を変えないため） |

- **これが現状で唯一どこにも出ていない情報を出す。** 既定順は今まで画面に一切現れていなかった
- **解除は並び順だけを消す**（フィルタは残す）。見出しを 3 回押す操作の近道。
  フィルタごと初期化する既存の「クリア」ボタンとは役割が違う
- 解除の URL は `ListSort::clearUrl($request)` を新設して作る
  （現在のクエリから `sort` / `dir` / `page` を落とし、`null` は `''` に正規化。Bug #31）

各画面の既定順の説明:

| 画面 | 文言 |
|---|---|
| 周辺ビル調査 | 既定（ビル名順） |
| 物件一覧 | 既定（稼働中が先・コード順） |
| 部屋一覧 | 既定（物件・階・部屋番号順） |

⚠ 周辺ビル調査の文言は §4.4 の変更に追従させること。**片方だけ直すと
「既定（空室率が高い順）」と書いてあるのに名前順で並ぶ**という、本設計が最も嫌う形の嘘になる。

---

## 7. 実装の構造

### 7.1 ラベルは画面ごとに 1 つのマップへ集約する

見出し（§5）とバー（§6）が同じ列名を出すので、**2 箇所に文字列を置かない**。
既に各コントローラにある `SORTABLE` を、ラベルと向きの言い方まで持つ 1 つのマップに拡張する。

```php
// AreaBuildingListService
public const SORT_COLUMNS = [
    'floors'    => ['label' => '総階数',   'desc' => '多い順',   'asc' => '少ない順'],
    'operating' => ['label' => '営業',     'desc' => '多い順',   'asc' => '少ない順'],
    'vacant'    => ['label' => '空き',     'desc' => '多い順',   'asc' => '少ない順'],
    'unknown'   => ['label' => '不明',     'desc' => '多い順',   'asc' => '少ない順'],
    'occupancy' => ['label' => '入居率',   'desc' => '高い順',   'asc' => '低い順'],
    'vacancy'   => ['label' => '空室率',   'desc' => '高い順',   'asc' => '低い順'],
    'month'     => ['label' => '最終調査', 'desc' => '新しい順', 'asc' => '古い順'],
];
```

既存 2 画面も同じ形へ揃える（`UnitController` は `expr` / `nullsLast` を、
`PropertyController` は `attribute` を同じ配列の中へ入れる）。
許可リストは従来どおり `array_keys(...)` を `ListSort::fromRequest()` へ渡す。

`x-sortable-th` は **`label` プロップをやめ、`columns` から引く**:

```blade
<x-sortable-th column="occupancy" :sort="$sort" :columns="$sortColumns" align="center"
               link-class="px-4 py-3 lg:px-5 lg:py-3.5" />
```

⚠ 代償: ビューを見ただけでは列の日本語が読めなくなる。また
**`column` を打ち間違えるとその画面が 500 になる**（従来は黙って既定順に落ちていた）。
これは承知のうえで採る —— 3 一覧とも画面を描画する Feature テストがあるので
デプロイ前に必ず落ちるし、この規約の最大の敵は**無音の食い違い**のほうである。

### 7.2 触るファイル（新規 1 / 変更 11）

| | ファイル | 変更内容 |
|---|---|---|
| 新規 | `resources/views/components/sort-bar.blade.php` | §6 のバー |
| 変更 | `app/Support/ListSort.php` | `clearUrl()` を追加 |
| 変更 | `resources/views/components/sortable-th.blade.php` | ラベルを span で包む／矢印 gray-500／`columns` からラベル |
| 変更 | `resources/css/app.css` | `.sortable-th-label` の点線下線とホバー・アクティブ |
| 変更 | `tests/Concerns/ParsesSortLinks.php` | `sortLinkFor()` の正規表現に span を許す |
| 変更 | `app/Services/Tenant/AreaBuildingListService.php` | `SORT_COLUMNS` ＋ `rows()` に並び替え ＋ `sortValue()` ＋ **既定順を `sortByDesc()` の削除でビル名昇順へ** |
| 変更 | `app/Http/Controllers/Tenant/AreaBuildingController.php` | `$sort` を作って `rows()` と view へ渡す ＋ **`mapUnlocated()` をビル名昇順に固定** |
| 変更 | `resources/views/tenant/area-buildings/index.blade.php` | 見出し 7 本 ＋ `x-sort-hidden` ＋ `x-sort-bar` |
| 変更 | `app/Http/Controllers/Tenant/UnitController.php` | `SORTABLE` を新形式へ ＋ `$sortColumns` を view へ |
| 変更 | `app/Http/Controllers/Tenant/PropertyController.php` | 同上 |
| 変更 | `resources/views/tenant/units/index.blade.php` | 見出しの `label` → `columns` ＋ `x-sort-bar` |
| 変更 | `resources/views/tenant/properties/index.blade.php` | 同上 |

**JavaScript は 1 行も足さない。** 見出しも解除もただのリンク。
Alpine も `@json` も使わないので Bug #16 / #23 / #26 / #30 の系統に触れない。

DB の変更は無い。

---

## 8. テスト計画

前設計書と同じ流儀（**挙動と構造を対で見る** / **走査が空振りして緑にならないよう下限を固定する** /
**変異を当てて赤になることを実測する**）。

### 8.1 周辺ビル調査の並び替え（新規 `AreaBuildingListSortTest`）

1. **7 列すべてが自分の列で並ぶ**。⚠ **期待順を既定順と食い違わせる**データで作る
   （揃えると式を取り違えても緑になる。前設計書で実測済みの罠）
2. **3 状態の往復**（既定 → 降順 → 昇順 → 既定）を、画面が描画したリンクを実際に辿って確認。
   ⚠ URL を自分で組み立てない（リンクが壊れていても緑になる。Bug #31 の注記）
3. **「—」が昇順・降順の両方で末尾**（総階数 null の棟／調査回が無い棟）
4. **「調査回はあるが総区画 0」の行**が、入居率・空室率では末尾／営業・空き・最終調査では
   普通に並ぶ（§2.2）。**画面表示と並び順が一致していることの固定**
5. **入居率の降順と空室率の昇順が完全に同じ並びになる**（§4.3 が守られていることの証明）
6. **同点はビル名順**（＝新しい既定順）
7. **フィルタ・ページ送りとの共存**（絞り込んだ中で並び、2 ページ目でも維持される）
8. **不正な `?sort=` で 500 にならない**（`?sort[]=a` の配列を含む）
9. 地図タブで並び順が持ち回されること（表へ戻ると残っている）

### 8.1.1 既定順をビル名昇順へ変えたこと（§4.4）

1. **既定順がビル名の昇順**であること。既存テストのデータ
   （`あ未調査` / `い率10` / `う率50` / `え率0`）は**名前順と率順がわざと食い違う**ので
   そのまま使える。期待は `['あ未調査','い率10','う率50','え率0']`
2. **旧既定順が失われていないこと** — `?sort=vacancy&dir=desc` が
   `['う率50','い率10','え率0','あ未調査']`（＝従来の既定順）を返す
3. **未登録リストが常にビル名昇順**であること。
   ⚠ **表で別の列に並び替えてから `?view=map` へ行っても変わらない**ことまで見る
   （`$rows` をそのまま使う実装に戻す変異が検出できる）
4. ⚠ **`?sort=` を付けない地図タブだけを見るテストにしない。** 既定順が名前順になった以上、
   `mapUnlocated()` が `$rows` を素通ししても結果が同じで、**原理的に区別が付かない**

⚠ **既存 `test_default_order_is_vacancy_rate_desc_with_unsurveyed_last` は名前ごと書き換える**
（削除ではなく、新しい既定順を固定するテストへ）。上の 2 とセットで、
「変えた」と「壊した」を区別できる形にする。

⚠ **`test_occupancy_bands` はこの変更で結果が変わらない**（`入居50 / 入居70 / 入居90` は
率の降順と名前の昇順がたまたま一致する）。**緑のままなので検出力が無い** ——
コメント「⚠ 並びは従来どおり空室率の降順」が嘘になるので**必ず直す**。
⚠ このテストを「既定順を守っている証拠」と読まないこと（実際には何も守っていない）。

### 8.2 意匠（案A）

- `sortable-th` の矢印が **`#6B7280`**、並び替え中は `#059669` であること
- ラベルが `sortable-th-label` で包まれていること（**バーと見出しの両方に効く構造**）
- `app.css` に点線下線の定義があること
  ⚠ **Tailwind クラスではなく `app.css`** なので、Blade の走査では守れない。CSS ファイルを直接見る
- ⚠ **「HTML にクラスが出ているか」だけでは意匠は検証できない**（Bug #28 / #43 / #51）。
  §9 のブラウザ確認を必ず行う

### 8.3 バー（案C）

- 並び替えなしのとき **既定順の説明**が出ること（3 画面それぞれ文言が違う）
- 並び替え中に **列名と向きの言い方**が出ること（`高い順` / `多い順` / `新しい順` を区別）
- **解除リンクが並び順だけを消し、フィルタを残す**こと（⚠ フィルタも消える実装だと
  「クリア」と区別が無くなる。フィルタ付きで踏んで確認する）
- **地図タブには出ない**こと
- **周辺ビル調査の既定順の文言が「ビル名順」**であること（§6 の ⚠。実際の並びと対で見る）
- ⚠ **ヒント文と現在の並び順を別々にアサートする。** まとめて見ると片方が消えても緑になる
  （Bug #43 / #46 / #49 と同型）

### 8.4 ラベルの単一化

- 3 画面それぞれで、**`x-sortable-th` の `column` が `SORT_COLUMNS` のキーに全部載っている**こと
  （＝ 500 になる組み合わせが無い）
- ⚠ **走査が空振りして緑になるのを防ぐため、拾えた見出しの本数（2 / 3 / 7）も固定する**（Bug #45）

### 8.5 既存テストへの影響

- `ParsesSortLinks::sortLinkFor()` の正規表現を広げるので、**広げすぎていないこと**を測る。
  リンクを消す変異で赤になることを確認する（緩めた結果、何にでも一致するようになっては意味がない）
- `AreaBuildingListTest`（23 本）・`UnitListSortTest`・`PropertyListSortTest` が全部緑のままであること

### 8.6 変異テスト（実施は必須）

⚠ **「テストが緑」は検証にならない。** 最低でも以下を当てて赤になること・
**落ちた理由の文言まで**突き合わせることを実測し、結果を実装プランに書き戻す:

- `sortValue()` の `occupancy` の符号反転を消す
- `'—'` を末尾へ回す `partition` を外す
- 既定順への安定ソート依存を壊す（並び替え前の既定順適用を消す）
- **既定順を元の `sortByDesc(空室率)` に戻す**
- **`mapUnlocated()` の名前並べ替えを外して `$rows` を素通しさせる**
  （⚠ **`?sort` 付きで測ること**。素の地図タブでは既定順が名前順なので区別が付かない）
- **バーの既定順の文言だけを「空室率が高い順」に戻す**（実際の並びと文言の食い違いを検出できるか）
- 矢印の色を `#D1D5DB` に戻す
- 解除リンクがフィルタも消すようにする
- バーのヒント文だけを消す／ピルだけを消す
- `x-sort-hidden` を外す

⚠ 作法は Bug #44 のとおり: ①**先にコミット** ②各変異の前に `git status --porcelain` が空
③`git diff --stat` が非空で着弾を確認 ④`git checkout -- <file>` で戻す
⑤赤/緑でなく**落ちた理由**を見る。

---

## 9. ブラウザでの確認（テストでは原理的に守れないもの）

⚠ 以下は HTML を見ても分からない。**実ブラウザで測る**（Bug #28 / #43 / #51 の教訓）:

1. 未使用の ⇅ が**見える**こと（3 画面とも）
2. 点線下線が**ラベルだけ**に掛かり、矢印の下に線が出ていないこと（§2.6 の未決着点の答え合わせ）
3. 見出しセルの**どこを押しても**並び替わること（文字の上だけでないこと）
4. `Tab` でフォーカスしたときにリングが切れずに出ること（既存の `outline-offset: -2px` が生きていること）
5. バーの解除を押して**フィルタが残る**こと
6. 地図タブの**登録モードの作業リストがビル名順**であること。
   ⚠ **表で別の列に並び替えてから**切り替えて確認する（既定のままでは区別が付かない）
7. 表タブの初期表示が**ビル名順**であること（本番の 187 棟で目視。
   ⚠ 漢字が読み順でないのは仕様。§4.4）

---

## 10. やらないこと

- **ビル名を「並び替えできる列」にすること**（既定順にはするが、見出しからは押せない）。
  利用者の選択どおり対象外。降順にしたい要望が出たら、そのとき SQL 側で別扱いにして足す
- **あいうえお（読み）順**。読みがな列が無いので実現できない（§4.4）。
  必要になったら `area_buildings` への列追加＋取込・入力画面の対応が別途要る
- **見出しの塗り分け**（案C の片方。モックで比較して不採用）
- `ensureInLocateList()` で復帰した棟を名前順の位置へ差し込むこと（§4.5 の既知の例外）
- 位置・操作列の並び替え
- 所在地の再表示（2026-08-20 に画面から外した判断は維持）

---

## 11. 本番反映

DB 変更は無い。`./deploy.sh` のみ（`npm run build` で CSS が再生成され、
`view:cache` / `route:cache` が再生成される）。

⚠ **`app.css` を変えるので `npm run build` が要る。** `deploy.sh` に組み込み済みなので
デプロイすれば自動だが、**ローカルで見た目を確認するときは手で `npm run build`** すること
（`public/build` は gitignore 済みで worktree には存在しない）。
